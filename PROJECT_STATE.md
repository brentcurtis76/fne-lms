# PROJECT_STATE.md

> Documento vivo de estado (estructura según §2.2 del itinerario: `docs/planning/GENERA-itinerario-construccion.md`).
> Convenciones durables: CLAUDE.md / AGENTS.md. Cada fase termina actualizando este archivo.
> Phase close requires `docs/planning/reviews/fase-<N>-review-request.md` (see CLAUDE.md executor rules). Reviewer verdict is recorded in the Human-review queue before merge.

## Meta
- Last phase completed: **Z1b — Esquema seguro + jobs + webhook + snapshot de horas** (plan Zoom §15). **CERRADA (2026-08-03)**: Sol `APPROVE` sin hallazgos en la **ronda 10** (2026-08-02) y merge a `main` el 2026-08-03 como **merge-commit `97c36cc`** vía PR [#26](https://github.com/brentcurtis76/fne-lms/pull/26) (rama `feat/zoom-core`; el SHA exacto queda en el ledger §0 del plan Zoom, que es el registro normativo). Trayectoria de revisión: 4 chunks de construcción + 2 rondas de remediación del PM + **9 rondas de remediación de Sol** a lo largo de 10 rondas de revisión (hallazgos 6→4→3→1→2→2→2→3→4 ítems→APPROVE); cada hallazgo remediado en su propia ronda y verificado independientemente por el PM (re-ejecución de *fail-on-old*, controles reconstruidos, réplica de controles negativos, lectura línea por línea de los dobles de test). Estado final: **3789 tests / 240 archivos** (+1054/+29 sobre la línea base de fork `2786fa8`), **pgTAP 139/139** en las suites Zoom, prueba §17 de ticker solapado en CI, **CI 8/8 en cada head revisado**, escaneos de identificadores/credenciales limpios en toda la fase
  - **Lo que aterrizó**: esquema `zoom_internal` expuesto-y-denegado-por-GRANTS (§6) con las 7 tablas, la reserva de concurrencia de host por EXCLUDE (§9) y las RPCs de cola de jobs; proyección pública `session_meetings_public` + RLS §7; `contract_hours_ledger.planned_minutes_snapshot`; capa cliente `lib/zoom/*` (token S2S, cliente REST con taxonomía de reintentos y read-back, firmante JWT puro, verificador de webhooks, seam `ZoomApi` + fake fiel a las trampas); ruta de webhook con **CRC respondido solo tras verificar**; ticker + reconcile con auth dual fail-closed; jobs `host_sync`, `webhook_sweep` y `meeting_provision`; **toda persistencia de aprovisionamiento es atómica y monótona vía RPC SECURITY DEFINER** (`recover_provisioned_meeting`, `adopt_checkpoint_meeting`, `sync_projection_from_meeting`)
  - **Invariantes que la fase deja asentadas**: `zoom_internal` es inalcanzable desde el navegador (GRANTS, no exposición); un host, una ventana (la restricción EXCLUDE decide, no una consulta previa); el uuid de ocurrencia se captura en `meeting.started`, **nunca** al aprovisionar; la deriva §9.4 se lee del read-back de `auto_recording`, jamás de `recording_disclaimer`; el triage indexa por el `kind`/`reason` tipado, **nunca** por cadenas de mensaje; las transiciones de lifecycle son monótonas y guardadas en SQL; las anomalías terminales se resuelven por **identidad** (comparando números), no por existencia
  - **Residuales documentados** (dossier §7 y errata): la ventana create→checkpoint es irreducible sin clave de idempotencia de Zoom; `possible_orphan` es una clase de huérfano con remedio **manual** en Zoom (sin barrido automático); el runbook `docs/runbooks/zoom.md` (§16) sigue sin escribirse, así que el mensaje de rechazo por-razón ES el contrato del operador; el par TS↔SQL de conjuntos *applies-from* es el único invariante mantenido a mano (lo protege pgTAP)
  - **Pendiente humano post-merge (Brent): TODO CERRADO (2026-08-03).** ~~fijar `ZOOM_WEBHOOK_SECRET_TOKEN` + `CRON_SECRET` en Vercel (Production)~~ **HECHO antes del merge (2026-08-02)** · ~~repuntar la suscripción de Marketplace~~ **HECHO (2026-08-03)**: endpoint `https://www.nuevaeducacion.org/api/zoom/webhook` (el dominio propio, no el alias `.vercel.app`), CRC validado y guardado. **Trampa registrada para todo callback futuro:** la URL por-deployment (`fne-<hash>-…vercel.app`) está detrás de Vercel Deployment Protection y responde 401 con el payload SSO de Vercel — indistinguible a simple vista de un fallo de firma; los callbacks de proveedores apuntan siempre a un dominio estable
  - **⚠️ DEFECTO DE CIERRE DE FASE, encontrado y reparado el mismo día (2026-08-03).** El checklist post-merge **no incluía aplicar las migraciones a producción**, y las diez rondas de revisión no lo detectaron porque todos los gates corren contra Postgres local. Lo grave no fue Zoom: `createReservation` (`lib/services/hour-tracking.ts:326`) inserta `contract_hours_ledger.planned_minutes_snapshot`, la columna no existía, y `pages/api/sessions/[id]/approve.ts:88` lo convertía en un 400 duro — **la aprobación de sesiones estuvo rota en producción desde el deploy del merge** (el fallo ocurría antes de cualquier cambio de estado: sin sesiones a medio aprobar, sin filas huérfanas). **Reparación autorizada y ejecutada por Brent**: 7 migraciones pendientes aplicadas en orden de versión (las 6 de Zoom + `20260731140500_add_pasantias_leads`), cada una envuelta en BEGIN/COMMIT con su fila de `schema_migrations` en la misma transacción. **Verificación de producción por el PM (solo lectura)**: `planned_minutes_snapshot` presente · `zoom_internal` = 7 tablas, 9 funciones · `session_meetings_public` con RLS y 3 políticas · constraint EXCLUDE presente · las 7 versiones en `schema_migrations` · **lockdown §6 INTACTO** (anon/authenticated sin USAGE, cero grants, cero EXECUTE; service_role con USAGE y EXECUTE en las 9). Regresión confirmada cerrada con sonda de solo lectura + control negativo, sin sembrar datos en producción
  - **REGLA PERMANENTE que deja este defecto:** "aplicar migraciones a producción" es ítem **obligatorio** del checklist post-merge de toda fase con migraciones, y una fase no está cerrada hasta que el PM verifique el esquema de producción directamente. Verde local/CI prueba corrección del código y **no dice nada** sobre el despliegue. Codificado en §0.1(d) del plan Zoom
- **Z1c — tenant sintético + e2e de CI: CERRADA (2026-08-04).** Dejó la topología que el resto de las fases usa: `scripts/ci/seed-e2e.mjs` con guarda local-only sin flag de override, `e2e-fixtures.json` como fuente única de credenciales y filas, `e2e-mandatory.mjs` como mecanismo bloqueante (un spec obligatorio que se salta rompe el gate), y el job Gate 4 contra un stack Supabase local efímero
- **Z2 — MVP de sesión en modo enlace** (plan Zoom §15). **CERRADA (2026-08-08)**: Sol `APPROVE WITH NOTES` (2026-08-07) y merge a `main` el 2026-08-08 como **merge-commit `34ea4cb6`** vía PR [#45](https://github.com/brentcurtis76/fne-lms/pull/45) (rama `feat/zoom-sess`, abierta 2026-08-04; head revisado y mergeado `ea48f2c4` — el commit exacto que verificó CI). **Treinta y una rondas**: 12 chunks de construcción, los doce ítems de Sol en r21–r28, 4 MAJOR + 6 MINOR en r29–r30, y r31 (absorber `origin/main` en la rama). Estado final sobre el árbol mergeado: **6773 tests / 290 archivos**, **e2e obligatoria 112 / 11 specs sin skips**, **`test:db` 466 / 11 archivos**, **seis compuertas verdes en CI** (run `31265436645`) — **la primera CI que esta rama tuvo en treinta rondas**; todo el verde anterior era de una sola máquina
  - **Los tres ítems de cierre, los tres hechos (2026-08-08)**: (1) **PR abierto y verde en CI**, por la regla vinculante adoptada ese día (`ci.yml` dispara en `pull_request` y `push: [main]`, así que una rama sin PR no tiene cobertura de CI alguna); (2) **ítem 12b — la corrida contra el tenant Zoom real de FNE**, guiada por el PM sobre el stack local sembrado con `ZOOM_MODE=live`, sin tocar datos de producción: OAuth S2S, `host_sync` importando 4 usuarios reales al pool de la organización, una reunión real (`87951919353`) creada, unida y **confirmada borrada preguntándole a Zoom directamente** (`404 — Meeting does not exist`), y **la forma de cable del dial-in verificada campo por campo** (`type · number · country · country_name · city`) contra lo que `ZoomDialInNumber` declaraba desde pura documentación — la suposición se sostuvo; (3) **las SIETE migraciones aplicadas a producción y verificadas read-only** (§0.1(d)), una por una, cada una envuelta en BEGIN/COMMIT con su fila de `schema_migrations`
  - **Lo que Z2 entrega**: intención gestionada durable (`consultor_sessions.is_zoom_managed`, plan §8) y el gate de aprovisionamiento que la lee; la **única apertura autorizada** `POST /api/meet/session/[id]/join` (§5) con sus siete compuertas en orden — autorización **antes** de leer cualquier hecho de la reunión; el interstitial `/meet/session/[id]` renderizando el afordance gestionado vía `JoinMeetingButton` (la URL se pide por clic y **jamás** entra en los props); el bloque de audio-fallback (dial-in) por esa misma respuesta; el kill switch §14; sincronización de reunión ante reprogramación y borrado ante cancelación / cambio de modalidad; y `sessionOffersPlatformJoin` como el único predicado que decide si una superficie ofrece el enlace de plataforma (seis superficies lo consumen: notificaciones, recordatorios, los tres `.ics` y la lista del workspace)
  - **Lo que Z2 NO entrega, y no debe leerse como si lo hiciera**: el **embed** (Component View dentro de GENERA) es **Z3**; la **asistencia**, la **comparación de horas** planificadas vs. reales y la **UI de override** son **Z7**. Z2 es modo enlace: la reunión se abre en Zoom, en pestaña nueva
  - **Revisión independiente: Sol devolvió `REQUEST CHANGES` con DOCE ítems (2026-08-06)** sobre el dossier, el review-request y el diff de 75 archivos. Los tres de mayor severidad fueron verificados contra el código por el PM antes de trocear, y los tres se sostenían (entre ellos un `DROP` en una migración, prohibido por `CLAUDE.md`, que el PM había aprobado dos rondas antes de declarar RED-tier la misma regla). **Remediados a lo largo de las rondas r21–r28**, una ronda por ítem o par de ítems, cada una con ejecutor fresco
  - **Dos defectos de plata, arreglados de paso**: ambos consumidores de horas se movieron de `actual_duration_minutes` al ledger, y `get_bucket_summary` dejó de multiplicar las horas asignadas de un colegio por el número de sesiones reservadas contra ellas — **un bug vivo en producción desde mucho antes de esta fase**, y el único cambio de la fase que mueve hacia abajo un número que el usuario ve
  - **Hallazgo de la corrida en vivo, con ruling del dueño**: los números de dial-in que devuelve la cuenta son **todos de EE.UU.**, y el bloque se renderiza en una pestaña de la que el usuario acaba de ser llevado a Zoom. **Ruling de Brent: no conoce a nadie que use la llamada telefónica.** El código queda tal cual —construido, revisado, aprobado e inofensivo— y **no se invierte nada más en él**: no se configuran países de dial-in y el problema de los números US es nota al pie, no defecto por corregir. Cierra tres ítems que el ledger arrastraba (tensión §2 de canal, números US, orden/preferencia de país)
  - **Residual del despliegue, para quien enlace el próximo checkout**: `supabase db push` es **inusable** en este repo (el historial está squasheado a un baseline `00000000000000` mientras producción lista sus 34 filas originales), y el arreglo que sugiere el propio CLI —`migration repair --status reverted`— **habría escrito en producción que 34 migraciones aplicadas estaban revertidas**. No se corrió. Las migraciones se aplican a mano, una por una
  - **Ítems abiertos que SOBREVIVEN al cierre** — ver *Deudas que arrastra Z2* más abajo, reconciliadas contra el ledger. Ninguno bloquea ya el cierre ni el despliegue
- **Z3 — Experiencia embebida, ESCRITORIO** (plan Zoom §15). **CERRADA (2026-08-10)**: merge a `main` el 2026-08-10 como **merge-commit `9972093d`** vía PR [#47](https://github.com/brentcurtis76/fne-lms/pull/47) (rama `feat/zoom-embed`; **10 commits**, de `65c8114b` a `2a459a33`). Estado final: **7060 tests / 305 archivos** (11 skips documentados), **pgTAP 484**, **seis compuertas de CI en verde**
  - **Lo que aterrizó**: el embed de **Component View** en escritorio detrás de `FEATURE_ZOOM_EMBED`, **apagada por defecto — al mergear no cambió nada para ningún usuario**; `PreJoinCheck`; i18n `es-ES`; **fallback automático al enlace de Z2** cuando el SDK no puede levantar; **ZAK + `role:1` para hosts** bajo la regla §9, con fila de auditoría en `zoom_zak_issuances`; y **todo navegador sin Component View** (móvil, tablet, Firefox, escritorio angosto) recibe el enlace de plataforma **sin descargar SDK alguno**
  - **Migración aplicada a producción ANTES del merge**: `supabase/migrations/20260810120000_zoom_zak_issuances.sql`, verificada por el PM. Es la regla permanente que dejó el defecto de cierre de Z1b (ver arriba), cumplida esta vez en orden
  - **SPLIT de la fase (2026-08-08, decisión del dueño)**: **Client View se movió a una fase nueva, Z3b**, arrastrando **tres hallazgos del reviewer**, y queda condicionada a un **protocolo de terreno revisado, específico de Client View, que todavía no existe**. El razonamiento está en `docs/plan/zoom/PLAN.md` §15.1
  - **Veredicto de hardware §16**: sigue **waived para escritorio** (esta fase, por la misma decisión del dueño) y es **BLOQUEANTE para Z3b** — cierre y encendido por defecto
- **Z7 — asistencia, comparación de horas y override administrativo: IMPLEMENTADA EN RAMA, NO ACEPTADA** (`feat/zoom-hours`, 2026-08-13). Estado actual: **`REVIEW READY` tras la remediación de ronda 5 y pendiente de otra revisión acumulativa independiente**; todavía no está aceptada, mergeada, desplegada ni verificada en producción.
  - **Invariantes entregadas en el diff acumulado:** asistencia webhook provisional con cierre seguro; lote de reporte completo como autoridad y estados `pending → complete|rejected` terminales; paginación de reporte validada como un conjunto coherente (enteros/rangos, conteos, páginas y token terminal), con un único rechazo estable ante contradicciones; UUID de ocurrencia fill-only; datos Zoom solo comparativos y sin escritura automática de facturación; un único RPC autenticado de admin para `effective_minutes`; historial de overrides append-only, reversión encadenada e idempotencia que compara payload canónico dentro de PostgreSQL; el ledger JSON de consultor falla cerrado si no puede resolver facilitadores.
  - **Migraciones solo locales:** las migraciones Z7 `20260811130000`…`20260813120200` se reproducen desde cero en Supabase local. No se aplicó ni inspeccionó schema de producción; la aplicación y verificación de solo lectura siguen siendo un paso humano posterior a merge.
  - **Evidencia local vigente de ronda 5:** type-check y lint estricto sin diagnósticos; Vitest **7.332 passed / 11 skipped en 323 archivos**; pgTAP **671**; build de **156** páginas estáticas; concurrencia real de request-id sin `23505`; selector Chromium obligatorio **117/117** y 11 specs sin skips; UTC/Santiago **7.332/7.332**, Madrid conserva los **8 fallos heredados** de `businessDays.test.ts`. Una falla o respuesta contradictoria de `get_bucket_summary` devuelve 500 genérico sin insertar ledger ni aprobar una sesión individual o un lote; el lote se preflighta completo antes de la primera escritura. El inventario ejecutable recorre todas las raíces TypeScript de producción, usos directos, consumidores RPC/vista/función transitivos y aliases SQL arbitrarios, con cero consumidores inexplicados.
- **Fase en curso: Z7, solo en estado de remediación/rerevisión.** La implementación está lista para otra revisión independiente, pero la fase permanece abierta hasta recibir un veredicto independiente favorable y completar la integración controlada. **Z3b existe pero NO ha comenzado** y conserva sus propios gates de entrada. En el itinerario GENERA, **Fase 1 sigue sin abrirse** (ver *Next phase*).
- Date: **2026-08-13** — actualización de estado de Z7 ronda 5; no es un cierre de fase
- Model used: Claude Opus 5 (ejecución directa; PM = sesión Fable sobre el plan Zoom)
- Commit SHA (`main` al cierre de Z3): **`9972093d`**, el merge-commit de PR #47. Lo posterior en `main` es solo documentación. **Los SHA de cada fase viven en su propia entrada, arriba**; los dos campos siguientes son el residuo de cuando este archivo llevaba un único "head actual", y se conservan como registro histórico de Z1a y Z0B en lugar de reescribirse
- Commit SHA (Z1a): `fix/sess-leak` — base `959c1fe` → `edc1714` (Z1a-1..3) → `2ef3a9e` (docs) → `5d117ca` (Z1a-4, remediación de las 6 observaciones del reviewer) → `62a448d` (Z1a-5, los 2 MAJOR de la re-review R2). 22 commits, 61 archivos, +7427/−769
- Commit SHA (Z0B): `feat/zoom-spike` — base `2786fa8`, **42 commits** en total, verificado con `git rev-list --count`. Desglose que cuadra con el ledger de §0 y con el dossier del PM:
  - **Z0B-1, trabajo: `2c1f72f`…`41281b9` — 19** (incluye 1 commit de docs del PM, `b23e5c7`)
  - **Z0B-1, sello del PM: `0061dbe` — 1**
  - **Z0B-2: `ace0898`…`7cb0b4b` — 12**
  - **Docs del PM tras Z0B-2: `a402c3b`, `aabfeec` — 2**
  - **Z0B-2r1: 8 desde `0c8b498`** (el último es esta misma actualización del ledger; un documento no puede nombrar el commit que lo contiene, así que su SHA no se cita)
  - 19+1+12+2+8 = **42**
  - *Nota sobre una corrección propia:* la primera pasada de esta reconciliación afirmó que `0061dbe` "no es el SHA sellado" y dejó fuera ese commit del desglose, dando 41. Estaba mal en los dos sentidos — `0061dbe` **es** el commit con el que el PM selló Z0B-1, y la redacción anterior de este archivo ("Z0B-1 hasta `0061dbe`, 19 commits, sellado") era correcta en ese punto. Lo que sí estaba desfasado era el resto: no mencionaba Z0B-2 por rango ni los commits posteriores
- Phase anterior: **Fase 0 — Repo hardening, CI, state scaffolding** + correcciones consolidadas pre-PR (bloques 1–3, 2026-07-08), rama `feat/fase0-ci`, base `main` @ `0650746`
- Nota: Z1a **no es una fase del itinerario GENERA**; pertenece al plan de integración Zoom (`docs/plan/zoom/PLAN.md`). El itinerario sigue con Fase 1 — ver Next phase

## Architecture invariants (never violate)
- RLS habilitado en toda tabla del schema `public`; `school_id` en cada fila (multi-tenant por colegio)
- El asesor nunca lee contenido crudo de sesiones ajenas; leadership/FNE ve solo métricas macro
- El sociograma jamás se expone a roles estudiante/familia (bloqueo estructural, no solo norma)
- Ningún dato nuevo de menores se almacena sin registro de consentimiento + referencia EIPD (Ley 21.719; gate = Fase 2)
- Next.js Pages Router + `getServerSideProps`; TypeScript strict; fetching con `fetch()` crudo (no TanStack/SWR)
- API routes: auth → role check → validation → logic
- Cambios de schema solo aditivos; nunca `DROP`/`TRUNCATE`; migraciones vía DB agent
- NO deployments por agentes (RED-tier); Vercel auto-deploya `main`
- Branches ≤20 caracteres
- Solo datos sintéticos en dev/test; PII de estudiantes nunca en prompts, commits ni logs
- **Nuevo (Fase 0):** migración que deshabilite RLS = bloqueada por hook (`scripts/hooks/block-rls-disable.sh`, self-test 4/4) y por CI (`scripts/ci/check-rls-migrations.sh`)
- **Nuevo (Z1a) · Política de divulgación de sesiones:** `lib/utils/session-disclosure.ts` es la ÚNICA fuente de verdad sobre qué contiene el payload de una sesión. Ningún endpoint de sesión reimplementa la autorización en línea: todos construyen `SessionAccessContext` y llaman `canViewSession()`. Los correos personales, el `meeting_link` crudo, el `meeting_transcript` y los informes `facilitators_only` solo llegan a quien la política lo permite; el resto recibe `has_meeting` + `join_path`
- **Nuevo (Z1a) · Roles fail-closed:** una consulta EXITOSA a `user_roles` es autoritativa y final — cero filas significa cero roles. `user_roles_cache` solo se consulta cuando la consulta autoritativa FALLA (error de DB), y sus filas se marcan `is_active: null` + `from_cache: true`. **Desde Z1a-5 esas filas no autorizan EN ABSOLUTO:** `getHighestRole()` descarta toda fila `from_cache`, así que una lista de roles solo-caché resuelve `highestRole = null` y cada vía de autorización deniega por su rama de 403 existente. El fallback se conserva únicamente para que el usuario no quede como un desconocido ante el shell durante una caída; es inerte para autorización por construcción. Revocar un rol surte efecto en la siguiente petición, sin depender de refrescar la vista materializada
- **Nuevo (Z1a) · Alcance de listas = alcance de detalle:** el scope de toda colección de sesiones es la UNIÓN de lo que concede `canViewSession()`, expresada en la consulta (`.or(...)`) para no romper paginación ni `count: 'exact'`. **Desde Z1a-5 esa traducción vive una sola vez** en `lib/utils/session-scope.ts` (`buildSessionScope` + `hidesDraftSessions`) y la consumen tanto `GET /api/sessions` como el export `.ics` masivo `GET /api/sessions/ical`: código compartido, no copiado, para que no vuelvan a divergir. Lista, export y detalle no pueden discrepar sobre qué sesiones existen
- **Nuevo (Z1a) · Artefactos salientes:** ningún `.ics` ni payload de notificación lleva el enlace crudo de reunión (se usa el link de plataforma `/meet/session/{id}`), ni direcciones de correo salvo para quien `canViewParticipantEmails()` autoriza (canal ATTENDEE incluido). En producción, la URL absoluta de esos artefactos debe venir de configuración: `getAppBaseUrl()` lanza excepción antes que confiar en el encabezado `Host`
- **Nuevo (Z1a) · Facturación en hora local:** `calculateNoticeHours` se ancla en `America/Santiago` vía `getSessionDateTime`; los umbrales de cláusula (48h/336h) se verifican bajo TZ=UTC / Santiago / Madrid
- **Router (decisión 2026-07-08):** los módulos GENERA se construyen en **Pages Router** — consistencia con el codebase; migrar de router a mitad de proyecto es riesgo de timeline inaceptable. Los paths del itinerario que asumen App Router se traducen al despachar cada fase: `/app/api/dsr/*` → `/pages/api/dsr/*`, `/app/(circle-mode)/*` → `/pages/circle/*`, `/app/(asesor)/dashboard/*` → `/pages/asesor/dashboard*`, etc.
- **Idioma (convención Fase 0):** UI y copy de cara al usuario en es-CL; código, commits, migraciones y docs técnicos en inglés

## Data model (current)
- Sin cambios de schema en Fase 0 (scope out, cumplido). Baseline legado FNE-LMS: 38 migraciones (`/supabase/migrations`, última `20260601_create_tractor_signups`)
- Dominios existentes: cursos/learning paths, assessment builder, sesiones de consultor, generaciones + growth communities, licitaciones Ley SEP, hour tracking, contratos, propuestas web/PDF, sistema QA, notificaciones, roadmap, badges, bots, expense reports/fx-rates
- 9 roles (`types/roles.ts`): admin, consultor, equipo_directivo, lider_generacion, lider_comunidad, supervisor_de_red, community_manager, docente, encargado_licitacion
- Tablas GENERA (`persons`, `enrollments`, `consent_records`, planes, sociograma, señales…): no existen aún (Fases 1+)
- Verificación RLS homogénea: ahora automatizada — `supabase/tests/001-rls-enabled.sql` exige `public` limpio salvo la **allowlist legacy 2026-07-08 (22 tablas pre-Fase-0 sin RLS, aprobada por Brent** — ver Open decisions; el objetivo es vaciarla, no crecerla). Toda tabla NUEVA sin RLS sigue rompiendo CI
- **Feature registro genérico (2026-07-20, rama `feat/registro-gen`, no es fase del itinerario):** `tractor_signups.generation_id` (uuid nullable, FK → `generations` ON DELETE SET NULL; migración `20260720134519`). La tabla ahora acepta dos `source`: `lideres_generacion_tractor` y `registro_general`. Convención confirmada: `user_roles.generation_id` es exclusivo de `lider_generacion`; el registro genérico solo escribe `profiles.generation_id` (fill-only-if-empty y solo si el colegio del perfil coincide). Migración aplicada en prod 2026-07-20 (Management API, autorizada por Brent; versión registrada en `supabase_migrations.schema_migrations`)

## Modules (current)
- Sin módulos nuevos de producto. Infra nueva Fase 0:
  - `/.github/workflows/ci.yml` — 6 checks: typecheck, lint (zero warnings), unit, `supabase test db`, e2e smoke + RLS migration guard; todos con `timeout-minutes: 20`
  - `/supabase/tests/` — `000-setup.sql` (pgTAP + helpers `tests.*` vendored API-compatible con basejump), `001-rls-enabled.sql` (DoD), `010-consultor-sessions-rls.sql` (migrado desde `database/tests/`)
  - `/scripts/ci/check-rls-migrations.sh`, `/scripts/hooks/block-rls-disable.sh`, `/.claude/settings.json` (PreToolUse)
  - `/tests/e2e/smoke.spec.ts` (gate e2e de Fase 0), `playwright.config.ts` (CI → servidor de producción)
  - `/.eslintrc.testid.json` + `npm run lint:testid` (advisory hasta limpiar baseline)
  - `CLAUDE.md` reescrito (9 roles reales, <200 líneas) + `AGENTS.md` espejo + `docs/ci-setup.md`
- CI commands: ver `docs/ci-setup.md`. Runners: node 22, npm ci, Supabase CLI latest (setup-cli), Playwright chromium
- **Z1a — Divulgación de sesiones + auth (2026-07-28, `fix/sess-leak`, plan Zoom §15, NO es fase del itinerario):** sin cambios de schema (cero migraciones; una migración en este diff sería rechazo automático).
  - Política nueva: `lib/utils/session-disclosure.ts` (informes por visibilidad, redacción de correos, link/transcripción, `has_meeting`/`join_path`), consumida por los 6 GET de sesión — detalle, lista, `reports`, `reports/[rid]`, `materials`, `attendees` — y por los 3 endpoints `.ics`
  - Superficie nueva: `pages/meet/session/[id].tsx` — interstitial SSR, único lugar que revela un enlace manual heredado, reautorizado en cada visita vía `lib/utils/session-meet-access.ts` (sin oráculo de existencia: todo lo no autorizado colapsa a un `not-found` idéntico)
  - Auth: `middleware.ts` suma `/meet` y `/consultor` al matcher (solo presencia de sesión); el redirect no autenticado lleva `?next=`, que `pages/login.tsx` honra detrás de `lib/utils/safe-redirect.ts`
  - Artefactos: `lib/utils/session-ical.ts` (links de plataforma + VTIMEZONE real de Chile vía `@touch4it/ical-timezones`; ATTENDEE fail-closed), `lib/notificationEvents.ts` + `pages/api/cron/session-reminders.ts` (`join_url` tipado), `lib/utils/app-url.ts`
  - Facturación: `lib/services/hour-tracking.ts` — `calculateNoticeHours` en hora de pared chilena (bug en vivo)
- **Z0B — Spikes técnicos Zoom (2026-07-29, `feat/zoom-spike`, plan Zoom §15, NO es fase del itinerario):** sin cambios de schema, sin migraciones, sin flags nuevos, sin deployments. Todo el contenido de reunión es sintético; cada reunión de prueba se llama `PRUEBA SPIKE — no unirse`.
  - Superficie de producto (única): `pages/meet/diag.tsx` gana el bloque **"Prueba de conexión"** — número + **clave obligatoria** de reunión a mano, join Component View, tiempo hasta entrar medido solo y volcado al JSON copiable. La disponibilidad del bloque se calcula en `getServerSideProps` con **un solo contrato** (`isDiagJoinConfigured()` en `lib/meet/diag-config.ts`: par SDK de servidor **y** allowlist no vacía) y llega como prop `joinAvailable`; cuando es `false` renderiza el placeholder y **el resto del diagnóstico sigue funcionando** (aserción explícita en tests: un despliegue sin credenciales no puede dejar a una consultora en terreno sin instrumento). *Corregido en Z0B-2r1 (Sol ⑧): antes ramificaba sobre `NEXT_PUBLIC_ZOOM_SDK_CLIENT_ID`, que es una variable distinta de la que exige la API — un despliegue con solo la mitad pública mostraba un formulario cuyo submit siempre daba 404*
  - Ruta nueva: `pages/api/meet/diag-signature.ts` — firma el JWT del Meeting SDK detrás de **cuatro** compuertas en este orden: (1) configurado → si falta el par SDK **o** la allowlist `ZOOM_DIAG_MEETING_IDS` responde 404 (función ausente ⇒ ruta ausente); (2) sesión → 401; (3) **rol `admin` o `consultor`** → 403 en cualquier otro caso (operadores del protocolo de terreno, §17); (4) **allowlist de reuniones** → 404 si el número no está listado, elegido en lugar de 403 para no ser un oráculo de existencia sobre el espacio de reuniones de FNE. `role:0` sigue **fijado en el servidor** (§5: el rol que manda el cliente se ignora) y no toca ZAK. *Corregido en Z0B-2r1 (Sol ⑤): antes exigía solo presencia de sesión, de modo que cualquier cuenta autenticada podía emitir una firma para cualquier número sintácticamente válido de la cuenta.* La reemplaza el endpoint de join real en Z2/Z3
  - Instrumentación desechable: `scripts/spikes/zoom/**` (cliente S2S, PoC de customerKey, round trip de grabación, control start/stop, sonda G2, cliente SigV4 S3 propio sin dependencias) y `scripts/spikes/webhook/**` (receptor CRC + generador de fixtures + los dos escáneres). **Interlock:** toda petición Zoom que cambia estado pasa por `destructiveZoomCall()` en `scripts/spikes/zoom/lib.mjs`, que ejecuta `assertSpikeMeeting()` (re-lee la reunión y exige tópico `PRUEBA SPIKE`) **inmediatamente antes de cada llamada — por llamada, no por secuencia**. La única exención es el POST de creación de `create-meeting.mjs`, marcada `interlock-exempt(create)` y fijada por nombre en el test. **Se enforcea estáticamente**: `__tests__/scripts/zoom-spike-interlock.test.ts` (15 tests) rompe el build si algún script emite un `zoomApi()` no-GET, si el método no es un literal legible, si alguien llega a `api.zoom.us` con `fetch` directo, o si aparece una exención no fijada. *Corregido en Z0B-2r1 (Sol ②): la afirmación anterior ("antes de cada llamada destructiva") era **falsa** — el escáner nuevo encuentra **16 sitios no-GET sin interlock** en el head `aabfeec`, entre ellos `probe-scopes.mjs`, rotulado "read-only", emitiendo `recording.stop`*
  - **Fixtures de webhook: sintéticos, verificado por escaneo.** `make-fixtures.mjs` sintetiza **todos** los identificadores que acuña Zoom (account id, user id opaco y numérico, número de reunión, UUID de reunión bajo `uuid` **y** `meeting_id`, ids de archivo de grabación, UUID de participante, customerKey, correos, IPs, URLs de grabación, tokens/claves y las tres cabeceras de trazado), preservando la forma; el UUID sintético lleva `/` y `+` a propósito para no retirar el ejemplar de doble codificación de §6. Mapeo derivado de `sha256(sal‖valor)` ⇒ mismo id real, mismo id sintético, así que los joins entre fixtures siguen siendo coherentes. Dos escáneres commiteados y en verde: `scan-identifiers.mjs` (84 valores reales, 17 clases, contra 1727 archivos versionados → **0 impactos**) y `scan-credentials.mjs` (6 secretos contra archivos versionados **y** los 42 commits de la fase → **0 impactos**). *Corregido en Z0B-2r1 (Sol ③): la afirmación "exclusivamente sintéticos" era **falsa** — en `aabfeec` había **33 impactos** de valores reales acuñados por Zoom en archivos versionados (los 3 headers de trazado en los 7 fixtures, el `user_id` numérico en 2, el UUID de reunión y los 6 ids de archivo en `recording-completed.json`, y 4 valores en el results doc)
  - `supabase/config.toml`: `[storage]` + `[storage.s3_protocol]` (solo local). **`feat/zoom-core` toca el mismo archivo con `[api]` — secuenciar el merge**
  - Sin dependencias nuevas: `@zoom/meetingsdk` fija `peer react@18.2.0` (el repo va en 18.3.1) y `@aws-sdk/client-s3` habría pesado ~15 MB; ambos resueltos sin tocar `package.json` (CDN de Zoom + SigV4 a mano). El pin de React queda como hallazgo para Z3
- **Z1b-4 — Aprovisionamiento de reuniones + barrido de webhooks + prueba de concurrencia (2026-07-30, `feat/zoom-core`, plan Zoom §15):** sin migraciones, sin dependencias nuevas, sin deployments. Servidor puro — no hay copy es-CL en este chunk.
  - `lib/zoom/jobs/meeting-provision.ts` — handler `meeting_provision`. **El INSERT ES la reserva**: la restricción EXCLUDE `zoom_meetings_host_no_overlap` decide, no una consulta previa, así que no hay TOCTOU; un `23P01` significa "host ocupado" y no libera nada porque no llegó a escribirse ninguna fila. Candidatos §9: host del facilitador **líder** (`session_facilitators.is_lead`) → hosts de otros facilitadores → pool de la organización (`profile_id IS NULL`), menos cargado primero y desempate por `zoom_user_id` ascendente para que dos workers en carrera prueben el mismo orden. Candidatos agotados ⇒ fallo **no reintentable** con razón estructurada `no_host_available`. §10: los instantes salen solo de `lib/utils/session-timezone.ts` — `starts_at` UTC para la fila y la proyección, y la cadena de **hora de pared** chilena más `timezone: 'America/Santiago'` para el create de Zoom; `new Date(`${fecha}T${hora}`)` no aparece. La duración prefiere la columna generada `scheduled_duration_minutes` (verificada en la tabla viva) y cae a derivarla de start/end cuando es NULL. §8: settings `{join_before_host:false, waiting_room:false, auto_recording:'none'}`, persistidos **desde la respuesta** porque Zoom refleja valores efectivos ante un desajuste de capacidad; un `auto_recording` distinto de `'none'` es la señal de deriva §9.4 y queda tanto en el resultado del job como en `effective_settings`. `zoom_meeting_uuid` queda **NULL** a propósito (se captura en `meeting.started`), y `consultor_sessions.meeting_link` **nunca** se escribe. §12: la API de creación de Zoom **no acepta clave de idempotencia**, así que "crear exactamente una vez" se reconstruye con dos anclas, en este orden: (1) `zoom_meetings.zoom_meeting_number` en la fila; (2) si falta, un checkpoint `stage: 'created'` en el `stage_state` del propio job, escrito por el heartbeat **inmediatamente después** de que `createMeeting` retorna — una re-ejecución que lo encuentra lo **adopta** (la RPC atómica `adopt_checkpoint_meeting`, con heartbeat previo y CAS `pending`+número-NULL — desde sol5/sol6 TODA persistencia de aprovisionamiento es atómica y monótona vía RPC SECURITY DEFINER; `markProvisioned`/`upsertProjection` ya no existen) en vez de crear. La garantía real es: **una re-ejecución que ve cualquiera de las dos anclas jamás crea una segunda reunión**. **RESIDUAL documentado**: si el proceso muere —o se pierde el lease, con lo que el heartbeat devuelve `false` y no se escribe nada— *entre* el retorno de `createMeeting` y el aterrizaje del checkpoint, el reintento no ve ninguna ancla y **sí** crea una segunda reunión, quedando la primera huérfana en Zoom; es irreducible sin clave de idempotencia del proveedor. Cuando el checkpoint sí aterriza, la huérfana queda **nombrada** en `zoom_jobs.stage_state.meeting.number` del job fallido, que es lo que usa el triage de jobs muertos para cancelarla a mano. Una fila en `error` se reanuda volviendo a `pending`, lo que **re-arma** la restricción EXCLUDE. Ninguna ruta de reanudación asume settings efectivos: la deriva §9.4 se **deriva** de `effective_settings` de la fila (ruta ancla 1) o de los settings del checkpoint (ruta ancla 2), nunca de una constante
  - `lib/zoom/webhook-lifecycle.ts` — el lifecycle extraído de la ruta para que la ruta **y** el barrido compartan una sola implementación en vez de dos copias que derivan. **Corregido en Z1b-sol1 (Sol F1): la afirmación anterior ("toda escritura es una asignación absoluta… eso es lo que hace seguro re-aplicar") era **falsa** como argumento de suficiencia** — idempotente no es lo mismo que seguro ante desorden, y el barrido que el PM enrutó vuelve *alcanzable* el replay fuera de orden: un `meeting.started` tardío reabría una reunión ya `ended`, re-entrando al conjunto activo del EXCLUDE §9 y re-tomando el host. Ahora toda escritura es una **transición guardada**, y el guard es el `WHERE ... status IN (...)` del propio UPDATE **en Postgres** (no un chequeo en proceso, que sería un TOCTOU entre la ruta y un barrido concurrente): `started` solo aplica desde `pending`/`provisioned`/`started`; `ended` aplica desde todo salvo `cancelled`/`deleted`. Cada conjunto contiene su propio destino, así que re-aplicar el mismo evento sigue siendo inofensivo — idempotente **y** ordenado, que es la propiedad verdadera. Ya no es "solo filas": la proyección `session_meetings_public` se mueve **con** el lifecycle (`live`/`ended`) bajo la misma regla, desde las claves de superficie que devuelve el UPDATE guardado, y solo cuando la transición interna sí aplicó
  - `lib/zoom/jobs/webhook-sweep.ts` — handler `webhook_sweep` (hallazgo ⑤ de Z1b-3). Cierra la ventana en que la ruta insertó la fila del ledger y murió antes de aplicar el lifecycle: Zoom no reintenta para siempre, y después del último reintento este barrido es lo único que queda. Acotado por los dos lados: solo filas con más de 15 min (para no competir con una entrega que la ruta está atendiendo ahora) y como mucho 200 por corrida. Una fila cuyo `raw_payload` ya limpió el scrub de retención se marca procesada en vez de barrerse eternamente. Se encola cada hora desde `planReconcileJobs()` con clave `webhook_sweep:<hora UTC>`
  - `lib/zoom/jobs/registry.ts` — ambos tipos registrados en el **mismo commit** que despacha su primer enqueue (regla de secuenciación de Z1b-3). Acepta dependencias opcionales para que una suite pueda ejercitar la **tabla de despacho real** con un Zoom falso: armar un registry propio en el test probaría que el handler funciona y no diría nada sobre si está cableado
  - `lib/zoom/jobs/runner.ts` — el registro estructurado de fallos gana `reason`, un nivel más fino que `kind`, que es como `no_host_available` llega al triage y al panel de salud sin que nadie tenga que hacer match sobre un mensaje
  - `scripts/ci/queue-concurrency-proof.mjs` + `npm run test:queue` + **un paso nuevo en el job `Gate 3` de `.github/workflows/ci.yml`** (la única edición de CI de la fase, puramente aditiva y confinada a ese job). Habla SQL directo porque `supabase db start` levanta **solo Postgres**, sin Kong ni PostgREST: `supabase-js` no tendría con qué hablar ahí. `pg` y `tsx` ya eran dependencias del repo
- **Z2 — MVP de sesión en modo enlace (2026-08-04…08, `feat/zoom-sess`, plan Zoom §15, NO es fase del itinerario; MERGEADA en `34ea4cb6`):** **siete** migraciones, todas aditivas, **aplicadas y verificadas en producción el 2026-08-08** (manifiesto por archivo en las deudas de Z2). Primera superficie Zoom visible para el usuario.
  - Intención: `consultor_sessions.is_zoom_managed` (§8) — durable, independiente del estado de aprovisionamiento; `lib/zoom/provisioning-intent.ts` es el único lugar que decide si se aprovisiona; el PUT de sesión rechaza con 409 escribir `meeting_link` o mover el proveedor en una sesión gestionada
  - Apertura: `pages/api/meet/session/[id]/join.ts` — la ÚNICA salida por la que algo con forma de credencial Zoom abandona el servidor. Siete compuertas en orden, y **el orden ES la propiedad de seguridad**: la autorización se resuelve completa antes de leer un solo hecho de la reunión, o el endpoint se vuelve un oráculo de existencia de reuniones
  - Superficie: `pages/meet/session/[id].tsx` + `components/sessions/JoinMeetingButton.tsx` + `MeetingDialIn` — para una sesión gestionada no hay enlace crudo que renderizar; la URL y el dial-in se piden por clic y viven en estado, nunca en props (los props se serializan en `__NEXT_DATA__`)
  - Dos decisiones, no una (`lib/utils/session-meet-access.ts`): **ver la página** es `canViewSession()`, **poder entrar** es `authorizeMeetingJoin()` (§5, estrictamente más estrecho). Por decisión del dueño (2026-08-06) esa segunda regla gobierna **ambos** proveedores: quien puede ver y no unirse no recibe afordance ni enlace crudo, con cualquier proveedor. Un conjunto cerrado —cancelada, finalizada, ya no online— cierra ruta y página con el mismo predicado
  - `sessionOffersPlatformJoin` (`lib/utils/session-disclosure.ts`): el único predicado que decide si una superficie ofrece `/meet/session/{id}`. Existe porque seis superficies preguntaban por `meeting_link` a secas, y una sesión gestionada lo tiene NULL **por diseño** — es decir, fallaban exactamente para las sesiones que esta fase existe para servir
  - Ciclo de vida: reprogramar sincroniza la reunión (RPC atómica), cancelar y el cambio a `presencial` encolan su borrado; `meeting_delete` distingue "todavía hay un aprovisionador en vuelo" de una anomalía real consultando la cola
  - Cobertura e2e de la sesión **gestionada**: `tests/e2e/zoom-managed-join.spec.ts` (r28), obligatoria. Ver *Test status* para lo que prueba y lo que no
- **Z3 — Experiencia embebida, ESCRITORIO (2026-08-08…10, `feat/zoom-embed`, plan Zoom §15, NO es fase del itinerario; MERGEADA en `9972093d`):** **una** migración aditiva, `20260810120000_zoom_zak_issuances.sql`, **aplicada y verificada en producción ANTES del merge**. Medido en `2a459a33`: **49 archivos, +9548/−83** (30 archivos y +8626/−83 sin contar `docs/`), **cero archivos borrados en toda la fase**, `tests/e2e/` sin cambios. Todo detrás de `FEATURE_ZOOM_EMBED`, **apagada por defecto**.
  - Superficie: `components/sessions/JoinMeetingButton.tsx` (elige la vista **antes** de pedir nada al servidor) + `components/sessions/PreJoinCheck.tsx` — el preflight de dispositivo que **nombra lo que está roto** en vez de mostrar un rectángulo negro
  - Capacidad y carga: `lib/meet/embed-capabilities.ts` (`selectEmbedView()` = `component → client → none`; rechaza viewports bajo 768 px, así que **una ventana de escritorio angosta también sale por enlace**) · `lib/meet/zoom-sdk-loader.ts`
  - Apertura: `pages/api/meet/session/[id]/join.ts` — la misma ruta única de Z2, ahora con la rama SDK. `join_url` **jamás** entra en un payload del SDK; el rol numérico vive solo dentro del JWT firmado; el ZAK de la ruta de request está **acotado a 8 s** (`AbortSignal` que llega al transporte, a los backoff y a los sleeps de `Retry-After`, más un `withBudget` externo por si el transporte ignora la señal) y al agotarse **degrada a modo enlace sin escribir fila de auditoría**
  - Política de host: `lib/utils/meeting-zak-policy.ts` — ZAK y `role:1` **solo** donde §9 lo permite, cada emisión auditada en `zoom_zak_issuances`, y **nunca la identidad personal de un consultor hacia un admin**
  - Cliente Zoom: `lib/zoom/api.ts` · `client.ts` · `token.ts` · `fake.ts` (el falso sigue el mismo contrato)
  - **Código de Client View que se mergeó DELIBERADAMENTE INALCANZABLE**, y es el punto de partida de Z3b, no producto vivo: `lib/meet/zoom-client-view-loader.ts` y `public/meet/zoom-client-view.html`. La inalcanzabilidad se prueba con una **tabla de verdad sobre el selector** (8 ramas no-component, cada una afirmando primero la respuesta de `selectEmbedView()`, luego **una** petición con `{"fallback":"link"}`, luego siete no-llamadas nombradas — `loadClientView`, `setZoomJSLib`, `preLoadWasm`, `prepareWebSDK`, `i18n.load`, `ZoomMtg.init`, `ZoomMtg.join`), **con los dobles de Client View cableados para TENER ÉXITO**, para que una fila verde hable de alcanzabilidad y no de un stub que falló
  - `supabase/tests/002-zoom-internal-isolation.sql` extendido a la tabla nueva; `lib/featureFlags.ts` suma la bandera
- **Registro público (2026-07-20, `feat/registro-gen`):** `/registro` (todas las escuelas + generación opcional) junto a `/registro-tractor` (Santa Marta); ambos alimentan `/admin/tractor-signups` (panel "Registros", filtro por origen, confirmación extra para grants de equipo_directivo). Ambos endpoints comparten `lib/signupSubmission.ts` (dedup re-abre registros descartados); helpers compartidos en `lib/signups.ts` (ex `tractorSignups.ts`); contrato de generación en `deriveGenerationOutcome()`. Página pública nueva `/privacidad` (Ley 21.719, contenido compartido con el modal del footer) — los checkboxes de consentimiento enlazan ahí. Tests: pgTAP `020-tractor-signups-rls.sql`, e2e `tests/e2e/registro.spec.ts` (local, no gate CI), stub supabase compartido `__tests__/helpers/supabaseStub.ts`

## Test status

**Z3 (escritorio) — head revisado y mergeado `2a459a33`.** Las cinco compuertas locales verificadas por el PM, **las seis de CI verdes en ese mismo head** (PR #47, más Vercel y preview), y Sol re-corrió las cinco por su cuenta obteniendo exactamente los mismos números:

| Gate | Resultado |
|---|---|
| `npm run type-check` | limpio |
| `npm run lint` (zero warnings) | limpio |
| `npm test` | **7060 passed / 11 skipped / 305 archivos** |
| `npm run build` | OK |
| `npm run test:db` | **PASS — 484** |
| CI, seis gates sobre PR #47 | **verde** |

Los **11 skips son deliberados y están rotulados `[Z3b, PARKED]`**: 8 de `JoinMeetingButton.prejoin-screen.test.tsx` y 3 de `timeouts.test.tsx`. Los tres ejercitaban Client View **a través del componente**, que es exactamente el camino que la prueba de inalcanzabilidad eliminó; las alternativas eran peores (una puerta de test hacia Client View habría falsificado la inalcanzabilidad que debía probar, y borrarlos destruye el punto de partida de Z3b). **Las suites de MÓDULO de Client View siguen intactas y verdes** — `zoom-client-view-loader`, `client-view-join-deadline`, `client-view-boundary` — así que lo parqueado es integración a nivel de componente de un camino que ya no existe en el producto. **Un test saltado no prueba nada**: si la inalcanzabilidad fuera imperfecta, esa cobertura no está. Des-parquearlos es **criterio de entrada de Z3b**. Delta de la última ronda: 7030/304 → **+30 tests / +1 archivo**.

**Z2 — cifras finales sobre el árbol mergeado**, que son las que valen: **6773 tests / 290 archivos**, **e2e obligatoria 112 passed / 11 specs sin skips**, **`test:db` 466 / 11 archivos**, **seis gates verdes en CI** (run `31265436645`). Subieron respecto de r28 porque el merge con `origin/main` trajo los tests del otro workstream, y **nada que pasara antes falla**. La lista obligatoria quedó en **11 specs** tras resolver por UNIÓN el choque con el workstream de pasantías en los tres archivos de siembra de CI — 7 + 4 → 11, sin perder ni reordenar ninguno.

**Z2 — última ronda de código (r28), rama `feat/zoom-sess`, local, macOS** (registro pre-merge; las cifras que valen son las del árbol mergeado, arriba). Las cinco compuertas más la lista e2e obligatoria, corridas sobre el commit final de código de esa ronda:

| Gate | Resultado |
|---|---|
| `npm run type-check` | limpio |
| `npm run lint` (zero warnings) | limpio |
| `npm test` | **4726 passed / 283 archivos** |
| `npm run build` | OK |
| `npm run test:db` | **PASS — Files=11, Tests=466** (tras `supabase db reset` limpio) |
| e2e obligatoria + guarda de no-skip | **88 passed**, `e2e-mandatory.mjs --check` reporta **7 specs corridos sin skips** |

La e2e obligatoria pasó de **76** a **88** en r28: el spec nuevo `tests/e2e/zoom-managed-join.spec.ts` aporta 12. **La lista obligatoria es la que corre CI** (`npx playwright test $(node scripts/ci/e2e-mandatory.mjs --list)`); `npm run e2e` a secas incluye además specs QA legados (`reservation`, `completion`, `cancellation`, propuestas) que exigen otra siembra, nunca corrieron en CI y no son evidencia de esta fase.

**Lo que la e2e prueba y lo que NO, tras r28.** Hasta r28 el sembrador nunca escribía `is_zoom_managed` — la columna es `NOT NULL DEFAULT false`, así que **toda sesión sembrada era no-gestionada y ningún e2e había tocado jamás una sesión gestionada**: ni el afordance de unirse, ni el kill switch §14, ni los enlaces de plataforma gestionados de r26, ni la divulgación gestionada de r27. Por eso los specs obligatorios pasaron sin cambios a través de cuatro rondas que cambiaron comportamiento gestionado. r28 siembra una tercera sesión **gestionada** (aditiva: las dos anteriores siguen no-gestionadas y toda aserción previa pasa intacta) y cubre extremo a extremo: el afordance aparece para quien puede unirse, no aparece —y no hay enlace alguno en el documento— para quien puede ver pero no unirse, la apertura §5 responde 403 a esa misma persona, y quien no puede ver recibe 404. **Sigue sin cobertura e2e**: el desenlace `mode: 'link'` y el bloque dial-in (el tenant sintético no tiene fila en `zoom_internal`, así que la apertura responde `mode: 'pending'`), y la rama **apagada** del kill switch §14 — la bandera es env de proceso y una corrida de Playwright tiene un solo servidor.

Al cierre de **Z1b-4·r1** — head de implementación **`ae210a5`** (`4b71b3c` → `ccb8fce` → `ae210a5`), local, macOS: **typecheck limpio · lint limpio (zero warnings) · 3646/3646 en 237 archivos · build OK** (las 3 rutas Zoom presentes: `/api/cron/zoom-ticker`, `/api/cron/zoom-reconcile`, `/api/zoom/webhook`) · **`test:db` PASA — 6 archivos, 85 tests** tras `supabase db reset` limpio (las 5 migraciones Zoom aplican desde cero) · **prueba de concurrencia de la cola PASA** contra el stack local. *(La cifra anterior, `4b71b3c` / 3641/237, quedó obsoleta con la remediación r1 y no se había actualizado — corregido en Z1b-sol1 F6.)*

Línea base tras absorber `origin/main` en este chunk: **3617/235** (= 3598/232 del cierre de Z1b-3, más las 19 pruebas y 3 archivos que trajo el track de contratos). Delta propio de Z1b-4: **+24 tests / +2 archivos** — `__tests__/lib/zoom/jobs/meeting-provision.test.ts` (16) · `__tests__/lib/zoom/jobs/webhook-sweep.test.ts` (7) · `+1` en `__tests__/api/cron/zoom-reconcile.test.ts` (clave de dedupe por job). `__tests__/lib/zoom/jobs/provisionHarness.ts` es un helper compartido, no un archivo de pruebas, y por eso no suma al conteo de archivos. r1 sumó **+5** en `meeting-provision.test.ts` (a 21).

**Ronda Z1b-sol1 (remediación Sol R1), head `7d4b062` + este commit de docs:** **typecheck limpio · lint limpio (zero warnings) · 3680/3680 en 238 archivos · build OK** · **`test:db` PASA — 6 archivos, 91 tests** tras `supabase db reset` limpio (la migración `20260729120200` enmendada aplica desde cero) · **`test:queue` PASA** (40 jobs, repartos 21/19). Delta propio de esta ronda: **+34 tests / +1 archivo** sobre `ae210a5` — `meeting-provision.test.ts` 21→**39** · `client.test.ts` 39→**44** · `zoom-ticker.test.ts` 24→**27** · `webhook.test.ts` 17→**21** · `webhook-sweep.test.ts` 7→**9** · nuevo `__tests__/api/zoom/webhook-oversize.server.test.ts` (**2**, servidor HTTP real). pgTAP `002` pasó de 44 a **50** asserts (hint de `Retry-After` + firma nueva de `fail_zoom_job`).

Trayectoria de la fase Z1b: 2740/212 (Z1b-1) → 3533/228 → **3536/228** (Z1b-2·r1) → **3598/232** (Z1b-3) → 3617/235 (merge de `origin/main`) → 3641/237 (Z1b-4) → **3646/237** (Z1b-4·r1, `ae210a5`) → **3680/238** (Z1b-sol1).

**Sobre la prueba §17 (`npm run test:queue`):** siembra 40 jobs en la tabla real y corre **dos bucles `runZoomTick` concurrentes** sobre dos sesiones Postgres distintas, exigiendo que cada job se ejecute **exactamente una vez**. Es la prueba de `FOR UPDATE SKIP LOCKED` que la suite vitest de la cola declina explícitamente en su propia cabecera: ningún doble en proceso puede producir la segunda transacción que salte el lock. Corre en el job `Gate 3` del CI, reusando el stack que ya levantó pgTAP. Repartos locales observados: 21/19 y 19/21. **Control negativo (ejecutor, no commiteado):** sustituir el RPC por un `SELECT`+`UPDATE` sin bloqueo hace que los 40 jobs se ejecuten dos veces y la prueba falle en voz alta — las aserciones muerden.

**Escáneres de identificadores/credenciales:** ambos salen con **código 2** en este worktree porque sus entradas están en `.gitignore` y viven en el worktree del spike (`captures/events.jsonl`, `.env.spike.local`). No escanearon nada, así que un "verde" habría sido vacío; la réplica real la corre el PM. Auditoría manual del diff del chunk: 0 correos, 0 URLs no sintéticas (solo `example-synthetic.test`), 0 secretos; números de reunión todos en el rango sintético `82xxxxxxxxx`; UUIDs de patrón repetido evidente; los UUID Zoom son los `Fk+SyntheticUuid/...` del fake. El único valor no obvio, `86084701483`, es **preexistente** en la biblioteca de fixtures sintéticos (está en `origin/main` y ya estaba en `pages/api/zoom/webhook.ts`): este chunk solo movió ese comentario verbatim al extraer el lifecycle compartido.

Al cierre de **Z0B-2r1** (`feat/zoom-spike`, local, macOS; gates corridos sobre el último head de código, `9a76802`): **typecheck limpio · lint limpio (zero warnings) · 3351/3351 en 222 archivos · build OK** (`/api/meet/diag-signature` y `/meet/diag` presentes en el output). Delta de Z0B-2r1 sobre `aabfeec` (3112/217): **+239 tests / +5 archivos**.

Trayectoria de la fase: 3072/215 (cierre de Z0B-1) → **3112/217** (cierre de Z0B-2) → **3351/222** (Z0B-2r1). *La cifra "3094/3094 en 217" que este archivo declaraba para el cierre de Z0B-2 no correspondía a ningún head: el conteo real ahí era 3112/217, y era una instantánea tomada antes de la última ráfaga del chunk que nunca se reconcilió (Sol ⑦).*

Archivos nuevos de Z0B-2r1: `__tests__/scripts/zoom-spike-interlock.test.ts` (15) · `__tests__/scripts/zoom-spike-redactor.test.ts` (8) · `__tests__/api/meet/diag-signature.test.ts` (34) · `__tests__/pages/meet/diag-ssr.test.ts` (10) · `__tests__/pages/meet/diag-join-passcode.test.tsx` (6).

Conteos por archivo de las suites Zoom en este head (los números anteriores estaban desfasados en casi todos):

| Archivo | Tests | Nota |
|---|---|---|
| `__tests__/lib/zoom/sanitizer.test.ts` | 100 | contrato del sanitizador; +8 por la regla "uncertain ⇒ flagged" |
| `__tests__/lib/zoom/sanitizer.must-catch.test.ts` | 353 | **bloqueante, 100% exigido**; incluye la regresión fail-on-mutant |
| `__tests__/lib/zoom/sanitizer.precision.test.ts` | 40 | bloqueante: 0 redacciones sobre 1886 palabras limpias |
| `__tests__/lib/zoom/sanitizer.adversarial.test.ts` | 3 | monitoreo, sin umbral: **78.8% (26/33)**, 7 misses |
| `__tests__/lib/zoom/webhook-signature-vectors.test.ts` | **41** | vectores HMAC/CRC (incluye §17 "re-serialized-body-must-fail") **+ 7 fixtures × 2 aserciones de identificadores + coherencia entre fixtures + ejemplar `/`+`+``**. *La cifra "14 vectores" contaba solo el bloque de vectores y omitía todo lo dirigido por fixtures* |
| `__tests__/pages/meet-diag-join-section.test.ts` | **6** | render de servidor del bloque de conexión; migrado al prop `joinAvailable`. *La cifra anterior decía 8* |

`test:db` no aplica (cero migraciones); `e2e` no corrido localmente (los specs de `/meet` son stubs `.skip()` hasta Z1c). **La biblioteca de fixtures de webhook tiene 7 archivos y el test imprime el conteo en CI** para que una biblioteca vacía no pase por verde silencioso — *la frase anterior, "la suite de fixtures está vacía", quedó obsoleta el mismo día en que se capturaron los 7 eventos*.

Al cierre de Z1a (`62a448d`, local, macOS):
- **Typecheck** (`npm run type-check`): limpio
- **Lint** (`npm run lint`, zero warnings): limpio
- **Unit/integración (Vitest)**: **2735/2735 en 211 archivos**. Delta de la fase: 2544 (Z1a-1) → 2590 (Z1a-2) → 2641 (Z1a-3) → 2697 (Z1a-4) → **2735 (Z1a-5)**, es decir **+191 tests / +11 archivos** sobre la línea base 2544/200
- **Build** (`npm run build`): OK; `/meet/session/[id]` aparece como ruta SSR dinámica; bundle de middleware 73.7 kB
- **pgTAP** (`npm run test:db`): no ejecutado localmente — Z1a no tiene migraciones; CI lo corre igual
- **E2E** (`npm run e2e`): no ejecutado localmente (sin entorno Supabase sembrado en esta máquina); veredicto = gate e2e del PR
- **Método de prueba Z1a-4:** cada corrección del reviewer tiene un test que FALLA en el head revisado (`4cde531`) — implementar test → revertir el fix → registrar el conteo → restaurar. T1 7/10 fallan, T2 2/10, T3 4, T4 5/12, T5 9/17
- **Método de prueba Z1a-5:** mismo protocolo contra el head re-revisado (`9b8a9b9`), fix por fix. Finding ① (`utils/roleUtils.ts` revertido): `cached-roles-never-authorize` 8/10 fallan + `role-revocation-fail-closed` 2/10. Finding ② (`pages/api/sessions/ical.ts` revertido): `ical-scope-union` 6/13. `session-scope.test.ts` (15) cubre el módulo nuevo, así que no es prueba de regresión contra el head anterior
- Los 13 fallos de abril (assessment-builder `cobertura-gate`, `detalle`) **no reaparecen**: la suite completa está verde
- Known-skipped/xfail: los specs e2e de `/meet`, del round-trip `next=` de login y de los deep links de middleware son stubs `.skip()` hasta Z1c

## Consent & compliance ledger
- Sin cambios: el LMS actual es de formación docente (adultos); ninguna tabla con datos sensibles de menores
- Gate vigente: cero datos de menores hasta arquitectura de consentimiento (Fase 2) + EIPD previa (Art. 15 ter)
- Umbrales Art. 16 quáter: <14 parental todo dato; sensible <16 parental; 16–17 propio explícito. GENERA = sensible ⇒ parental <16
- Sin portal estatal: captura/prueba/revocación propias, auditables, sin casillas premarcadas
- Fixtures: exclusivamente sintéticos (los helpers `tests.create_supabase_user` generan usuarios `@test.local`)

## Decisiones tomadas (log)
- **2026-07-08 · Recorte beta Dic-2026 — CONFIRMADO por Brent:** beta = solo producto institucional B2B con 2–3 colegios piloto; rol familia sin paywall; B2C/Transbank, apps móviles e ingesta masiva RAG → 2027, tras validación del piloto. Reflejado en GENERA-01 §6
- **2026-07-08 · Pages Router para módulos GENERA** (ver Architecture invariants) — paths del itinerario se traducen al despachar cada fase
- **2026-07-28 · Z1a: la política de divulgación vive en un solo módulo.** Los cinco GET de sesión reimplementaban la autorización en línea y cada copia divergía. Se unifican en `canViewSession()` + `session-disclosure.ts`; reintroducir un chequeo de rol en línea en un endpoint de sesión es una regresión, no un atajo
- **2026-07-28 · Z1a: los enlaces crudos de reunión no salen de la plataforma.** Todo artefacto saliente (.ics, notificación, correo) lleva `/meet/session/{id}`; el enlace real se revela solo en el interstitial, reautorizado al abrirlo. Los callers privilegiados (admin/facilitador/consultor con alcance) siguen recibiendo el campo crudo en la API porque los formularios de edición lo requieren
- **2026-07-28 · Z1a-4: fail-closed sobre disponibilidad en revocación de roles.** Ante "consulta exitosa, cero filas" se elige denegar, no consultar caché. Se conserva el fallback SOLO ante error de DB para no expulsar a todos durante una caída, y esas filas no otorgan alcance
- **2026-07-28 · Z1a-5: el riesgo residual del camino degradado NO era aceptable — decisión revertida.** Z1a-4 dejó documentado que una fila `admin` en caché seguía produciendo `highestRole = 'admin'` durante una caída, como concesión a la continuidad. Sol (re-review R2) sostuvo que la continuidad no requiere autorizar desde un rol no verificable y posiblemente revocado, y que un modo degradado donde los usuarios comunes pierden acceso pero los administradores lo conservan es fail-open justo donde importa. Concedido: `getHighestRole()` descarta las filas de caché por completo. El fallback sobrevive solo como dato de presentación, sin poder de autorización
- **2026-07-28 · Z1a-5: una colección de sesiones = un solo constructor de scope.** La traducción de `canViewSession()` a filtro de consulta se escribía en línea en cada endpoint de colección y las copias divergieron (el export `.ics` masivo ni siquiera miraba `is_active`). Vive en `lib/utils/session-scope.ts`; añadir un endpoint que devuelva un conjunto de sesiones con su propio branching de roles es una regresión, no un atajo
- **2026-07-08 · Comparabilidad entre colegios NO es objetivo de GENERA:** métricas macro FNE (frecuencia sesiones, % completitud, salud grupal) operan sobre estructura/slugs canónicos, independientes de etiquetas locales. Conversación Arnoldo reencuadrada a consistencia pedagógica (GENERA-01 §7)

## Open decisions / debts (with owner)

### Deudas que arrastra Z3 (escritorio), abiertas al cierre 2026-08-10
- [Sin dueño · toca a TODO el repo, no solo a Zoom] **Contaminación de estado de Vitest entre archivos.** `vitest.config.ts` fija `threads: false` y los globals de jsdom **se filtran de un archivo a otro**; el PM la midió en **1 de cada 3 corridas** sobre el commit `db9fc6c7` y un ejecutor la encontró por su cuenta en r9, es decir **dos signaturas y dos direcciones independientes**. r9 la **contuvo** dentro del archivo que escribió (un `afterEach` restaurando `innerWidth`/`userAgent`/`platform`/`maxTouchPoints`) — contención, no arreglo. **Lo que la vuelve seria: el gate de Vitest en CI no tiene reintentos**, así que un flake es un gate rojo y el próximo en verlo lo leerá como una regresión suya
- [Z3b · criterio de entrada] **Los 11 tests `[Z3b, PARKED]`** (8 en `JoinMeetingButton.prejoin-screen.test.tsx`, 3 en `timeouts.test.tsx`) deben des-parquearse **antes de la primera ronda de Z3b**, o se pudren como decoración. Y **el worktree `/Users/brentcurtis/dev/wt/zoom-embed` NO debe limpiarse**: Z3b parte de ese código y ahí viven los tests parqueados
- [Z3b · NIT, ruteado explícitamente] **La tabla de verdad no tiene chequeo de exhaustividad contra el selector.** Cada fila afirma la respuesta de `selectEmbedView()`, así que ninguna se pudre en silencio — pero **una rama NUEVA agregada al selector más tarde simplemente no aparecería y nada fallaría**. Se cierra barato (afirmar el número de filas contra el número de ramas, o un `switch` exhaustivo a nivel de tipos), y le toca a la fase que va a tocar ese selector
- [Z12 · observabilidad] **La espera del token está acotada; el fallo del token es invisible.** El presupuesto de 8 s acota lo que **espera el llamador** sobre el grant OAuth compartido, no el fetch del grant. La asimetría es deliberada y correcta —abortar un grant single-flight por el presupuesto de una request haría fallar al cron que va montado en el mismo grant— **pero el costo es real**: cada llamador de la ruta de request degrada a modo enlace puntualmente mientras el grant sigue fallando en silencio para los workers y **nadie levanta la mano**. Va al panel de salud §18, que existe justo para esta clase
- [Producto · consecuencia del split, no defecto] **Mientras Z3b no exista, quien entra por el enlace no genera grabación.** §12 mantiene la grabación apagada en modo enlace y G1 falló definitivamente, así que móvil, tablet, Firefox y escritorio angosto **no producen flujo de grabación Z4 ni insumo para Z5/Z8**. El PM afirmó primero que el split tenía «costo downstream cero» y el reviewer lo desmintió; queda escrito porque es la parte que se equivocó

### Deudas que arrastra Z2 (reconciliadas contra `docs/plan/zoom/LEDGER.md` el 2026-08-10)
- ~~[Brent · BLOQUEA EL CIERRE DE FASE] **Ningún código de Z2 ha corrido jamás contra un tenant Zoom real**~~ — **CERRADA (2026-08-08) por el ítem 12b**, la corrida contra el tenant Zoom real de FNE: OAuth S2S, `host_sync`, aprovisionamiento, una reunión real creada y unida, cancelación confirmada **preguntándole a Zoom** (`404`), y la forma de cable del dial-in observada por primera vez. Ver la entrada de Z2 en Meta. Lo que la corrida NO cubrió y sigue siendo cierto: se ejecutó contra el stack **local** sembrado apuntado al tenant real, no contra producción
- ~~[Brent · BLOQUEA EL DESPLIEGUE] **Las SIETE migraciones de Z2 no están aplicadas a producción**~~ — **CERRADA (2026-08-08)**: aplicadas por Brent una por una en el editor SQL, cada una envuelta en BEGIN/COMMIT con su fila de `schema_migrations`, y **verificadas después de cada paso por el PM con consultas de solo lectura** (§0.1(d); el PM no escribió nada en producción). Barrido final: siete versiones registradas · ambas columnas presentes · tres funciones públicas · dos internas sin sobrecargas · la RPC llamando a la fórmula compartida · **el ensanchamiento prohibido del allowlist ausente** · **cero columnas de dial-in en `public.session_meetings_public`**, así que la frontera de confianza §6/§7 se sostiene en producción igual que en local. El manifiesto se conserva porque **es una lista de nombres y no un conteo** — la lección de Sol r29 M4, que un conteo se desactualiza en silencio:
  - [x] `supabase/migrations/20260804120000_zoom_managed_intent.sql` — `is_zoom_managed` presente, `NOT NULL`, default `false`, 0 sesiones marcadas
  - [x] `supabase/migrations/20260805120000_reschedule_hours_rpc.sql` — `SECURITY DEFINER`, `search_path=""`, y el allowlist de `session_activity_log` **no** ensanchado (el `DROP` que encontró Sol nunca llegó a producción)
  - [x] `supabase/migrations/20260806120000_zoom_dial_in_numbers.sql` — una sola definición por función, sin sobrecarga; `anon` sigue sin poder ejecutarlas
  - [x] `supabase/migrations/20260807120000_backfill_zoom_dial_in_numbers.sql` — 0 filas afectadas (producción no tiene reuniones Zoom), 0 filas quedadas atrás
  - [x] `supabase/migrations/20260808120000_session_reschedule_atomic.sql` — grants exactamente anon NO / authenticated NO / service_role SÍ
  - [x] `supabase/migrations/20260809120000_fix_bucket_summary_fanout.sql` — una definición, ocho columnas de retorno intactas, los tres EXECUTE de baseline intactos
  - [x] `supabase/migrations/20260809120100_reschedule_rpc_uses_bucket_summary.sql` — verificado leyendo `prosrc` en producción
- ~~[Brent/Claude · Z3 o antes] **La forma de cable del dial-in es documental, no observada**~~ — **CERRADA (2026-08-08)**: Zoom devolvió 17 entradas y las claves fueron exactamente `type · number · country · country_name · city`, campo por campo lo que `ZoomDialInNumber` declara y `buildJoinDialIn` whitelistea. La advertencia que arrastraba cada fila del ledger desde Z2-4d («un fake no puede confirmar una forma de cable que le fue dictada») queda retirada. Lo que la corrida encontró en su lugar está en la entrada de Z2: los números son todos de EE.UU. y el dueño no conoce a nadie que use dial-in — ruling: no gastar más
- [Sin dueño · arrastrado tras el cierre de Z3] **El desenlace `mode: 'link'` de la apertura §5 no tiene cobertura e2e**, ni el bloque dial-in que viaja con él: el tenant sintético no siembra `zoom_internal.zoom_meetings` ni la proyección, así que la apertura responde `mode: 'pending'`. Unit e integración sí lo cubren. *Estaba etiquetado `[Claude · Z3]`; Z3 cerró sin tomarlo y el ledger lo registra como no asignado*
- [Sin dueño · arrastrado tras el cierre de Z3] **La rama apagada del kill switch §14 no tiene cobertura e2e.** `FEATURE_ZOOM_MEETINGS` se lee de `process.env` en cada request y una corrida de Playwright levanta un solo servidor, así que la bandera tiene un único valor por corrida. El entorno e2e la deja **encendida** (sin eso no hay afordance gestionado que aserverar). Cerrarla exigiría un segundo `webServer` de Playwright en otro puerto con la bandera ausente
- [Sin dueño · registrados a propósito] **Cinco ítems no dirimidos** que atraviesan la fase y se dejan tal cual: `SESSION_STATUS_FALLBACK`, el nombre de `total_hours_actual`, el `min` UTC-vs-Chile de `pages/admin/sessions/create.tsx`, las cuatro suites dependientes del orden, y `'edit_approval_blocked'`
- [Claude · Z6] **`community_meetings.is_zoom_managed` no existe**: la intención gestionada solo vive en `consultor_sessions`. El espejo para reuniones de comunidad es Z6 por decisión del PM, no un olvido
- **Arrastrados por el cierre de Z2 y NO son de Z2 — el ledger los deja explícitamente abiertos** (los encontró el barrido de producción del 2026-08-08, no el código de la fase):
  - [INSPIRA · sin aplicar] **`supabase/migrations/20260803170000_add_email_marketing_tables` nunca se aplicó a producción** (commit `0df19ad5`, mergeada vía PR #41). **Es la misma clase de defecto que rompió la aprobación de sesiones tras Z1b, desde otro workstream.** La descubrió el `db push --dry-run` que se corrió para Z2; **se dejó deliberadamente fuera de aquel cierre** en lugar de aplicarla de paso — no es de Z2, nada la llama todavía (sus propias tablas están dormidas hasta B4a/B4b) y agruparla la habría escondido. **Es de sus dueños aplicarla**
  - [Brent+DB agent] **Las 22 tablas `public` sin RLS están CONFIRMADAS así en producción** (barrido del 2026-08-08), no solo en el baseline de 2026-07-08 — ver la deuda detallada en *Deudas previas*. Z2 no agregó ninguna tabla a `public`, así que precede por completo a la fase. **Lo que NO se comprobó y es la primera pregunta**: qué tienen `anon` y `authenticated` realmente concedido sobre esas tablas — RLS deshabilitado no implica por sí solo que el dato sea alcanzable. Cuatro de ellas (`student_answers`, `submissions`, `assignments`, `answers`) son trabajo de estudiantes, y `profiles_role_backup` es una tabla de roles. Se planifica un workstream `RLS` aparte

### Deudas que arrastra Z1a
- [Brent · POST-MERGE · watch item] **Confirmar que la producción en Vercel tiene origen configurado.** Desde Z1a-4 `getAppBaseUrl()` LANZA en producción si no hay `NEXT_PUBLIC_BASE_URL` / `NEXT_PUBLIC_SITE_URL` / `NEXT_PUBLIC_APP_URL` ni `VERCEL_PROJECT_PRODUCTION_URL`; ya no cae al encabezado `Host`. Vercel inyecta `VERCEL_PROJECT_PRODUCTION_URL` por defecto, así que se espera satisfecho, pero no está verificado desde la máquina de ejecución. **#24 ya está mergeado**, así que dejó de ser un bloqueo previo y pasó a ser vigilancia en producción: si las descargas `.ics` o el cron de recordatorios devuelven 500, definir `NEXT_PUBLIC_BASE_URL` en Vercel (proyecto fne-lms, Production). Son los únicos consumidores de `getAppBaseUrl`; nada más se ve afectado
- [Claude · ticket aparte] **Migración a `getUser()`**: todo SSR y toda API route del repo (incluidas las nuevas) confían en la cookie de `getSession()` en lugar de validar contra el servidor de auth. Z1a no amplió la deuda, tampoco la cerró
- [Claude · post-Z2] **`/consultor/**` sin gating de rol en servidor**: el middleware ahora exige sesión (estrictamente más que antes), pero la comprobación de rol sigue siendo client-side
- [Brent+DB agent · ticket aparte] **`user_roles_cache` es una VISTA MATERIALIZADA** (`baseline.sql:11406`) con `GRANT ALL … TO anon, authenticated`: no admite RLS, así que los roles de cualquier usuario son legibles con la anon key. Su trigger sobre `profiles` solo hace `pg_notify` y nadie escucha; solo se refresca por RPC explícita. Z1a-4 lo neutralizó en código (fail-closed + filas de caché sin alcance + refresco en los 3 caminos de revocación) y Z1a-5 lo completó (esas filas ya no autorizan nada), pero el arreglo de schema requiere migración → fuera del alcance de esta fase
- ~~[Reviewer/Brent] **Riesgo residual del camino degradado**: si la consulta a `user_roles` falla, una fila `admin` en caché aún produce `highestRole = 'admin'`~~ — **CERRADO en Z1a-5** (`62a448d`): `getHighestRole()` descarta las filas `from_cache`, así que el camino degradado no otorga ningún rol. El fallback se mantiene; lo que se eliminó es su poder de autorización, no su disponibilidad
- [Brent] **Consultores globales (`school_id` NULL) reciben correos de participantes** — coherente con el modelo `canViewSession` existente; cero filas así en producción hoy. Pregunta de producto, no defecto
- [Claude · Z1c] E2E de navegador para `/meet`, el round-trip `next=` de login y los deep links de middleware (hoy stubs `.skip()`); tests de integración contra DB sembrada

### Deudas previas
- [Brent · AHORA] `git push origin feat/fase0-ci` + abrir PR (sandbox sin credenciales GitHub) → los 6 checks corren = DoD verificable. Checklist completo: `docs/ci-setup.md`
- [Brent · tras 1er PR] Activar branch protection en `main` con los 6 checks (nombres exactos en `docs/ci-setup.md`)
- [Brent] Bridge caído: task #1119 `failed` (3 intentos, "Claude Code invocation failed") — revisar supervisor en la Mac; `jb` tampoco existe en el sandbox. Mientras tanto: ejecución directa autorizada caso a caso
- [Brent] Rama `bridge/rescue-1119-20260707T222223` contiene tus 3 archivos rescatados (`businessDays*`, `licitacionService`) — recuperar o descartar
- [Brent/PR] Si Gate 3 falla aplicando migraciones desde cero (schema anterior a 2026-02): baseline `supabase db dump --linked` + archivar las 38 migraciones a `supabase/migrations-archive/` en el MISMO PR (procedimiento completo en `docs/ci-setup.md`)
- [Claude/PR] Si Gate 2 muestra los 13 fallos de abril, o Gate 1b (lint, zero warnings) nace rojo: fix o `.skip()`/triage con TODO vinculado, en el mismo PR
- [Brent] 7 docs `.md` sueltos quedaron visibles como untracked al des-ignorar `docs/**/*.md` (code reviews, notes) — commitear o descartar
- [Brent+DB agent · post-Fase 0] **22 tablas legacy sin RLS en producción** (detectadas por el baseline 2026-07-08, allowlisted en `001-rls-enabled.sql` con OK de Brent): answers, assignments, course_prerequisites, deleted_blocks, deleted_courses, deleted_lessons, deleted_modules, group_assignment_discussions, growth_community_transformation_access, instructors, learning_path_courses, learning_paths, menu_permissions, metadata_sync_log, modules, profiles_role_backup, propuesta_rate_limits, qa_tester_time_logs, questions, quizzes, student_answers, submissions. Sin datos de menores, pero legibles vía REST con anon key. Remediación: policies tabla-por-tabla en PR dedicado (no habilitar RLS sin policies — rompería producción); al habilitar cada una, quitarla de la allowlist
- [Registro Fase 0] El test tier-3 de `010-consultor-sessions-rls.sql` ahora asserta el comportamiento real: consultor con `school_id NULL` NO ve sesiones (no existe policy de acceso global; 0 filas así en producción al 2026-07-08). Si algún día se quiere "consultor global", requiere policy nueva + flip del assert
- [Brent/legal] EIPD + redacción de consentimiento parental — bloquean Fase 2→3
- Eliminado: script roto `test:db:supervisor` (apuntaba a archivo inexistente); `test:db` ahora = `supabase test db`

## Next phase
Dos hilos en paralelo, con dueños distintos:

**(a) Plan Zoom — Z7 implementada, pendiente de rerevisión independiente** (ver `docs/plan/zoom/PLAN.md` §15 y `docs/plan/zoom/LEDGER.md`)
- **Acción inmediata:** revisar el límite acumulado de Z7 `43999499..HEAD`. No tratar la implementación como aceptada, mergeada, desplegada ni verificada en producción antes de ese veredicto y de los pasos humanos posteriores.
- **Z2 cerró el 2026-08-08 y Z3 (escritorio) el 2026-08-10. Z7 es la única fase Zoom en curso**, limitada a remediación/rerevisión; no depende de Z3 ni de Z3b.
- **Z3b — Client View (móvil/tablet/Firefox)**: existe en el plan, **no ha empezado y necesita planificación**. Arrastra M1/M2/M3 de Sol; su criterio de entrada son los 11 tests parqueados; y **su cierre y su encendido por defecto** —no su inicio— están bloqueados por un **protocolo de terreno revisado, específico de Client View**, cuyo alcance quedó fijado en §15.2 el 2026-08-10 pero **que todavía no se ha corrido**. El protocolo viejo no sirve: maneja Component View a través de `/meet/diag` y se decide en escritorio P0
- **Z4** (grabación) y **Z6** (espejo de la intención gestionada para reuniones de comunidad) siguen disponibles; **Z7 ya no está “disponible para empezar”: está implementada y esperando revisión**.
- **`FEATURE_ZOOM_EMBED` sigue APAGADA y encenderla es un acto deliberado y aparte** — requiere **ambas** variables, `FEATURE_ZOOM_EMBED` (servidor) y `NEXT_PUBLIC_FEATURE_ZOOM_EMBED` (navegador). Hasta entonces, todo el mundo entra por el modo enlace de Z2

**(b) Itinerario GENERA — Fase 1: Core data model + multi-tenant RLS + seed sintético**
- **Gate de entrada**: primer PR de `feat/fase0-ci` con 6 checks verdes + branch protection activa (cierre formal del DoD Fase 0)
- Objective: `persons` (identidad estable), `school_tenants`, `enrollments` y `asesor_assignments` time-bounded, enum `developmental_stage`, `guardianship`; `has_role_on_school()` security-definer envuelta en `(select ...)`; políticas RLS 4 roles GENERA × SELECT/INSERT/UPDATE/DELETE; `seed:test` (3 colegios, ~90 estudiantes 3–18, ~6 asesores, ~20 equipos base, familias, 1 leadership)
- Files likely touched: `/supabase/migrations/*`, `/lib/auth/roles.ts`, `/supabase/tests/002-rls-core.sql`, `/scripts/seed-test.ts`
- Dependencies: Fase 0 verde · Context budget ~200K · Model: Opus-class (schema + RLS correctness)
- DoD: matriz pgTAP rol×tabla×operación verde (~40–60 asserts); toda tabla nueva con RLS + columnas de policies indexadas; `seed:test` idempotente

## Human-review queue (batched)
- **Z7: `REVIEW READY` tras ronda 5, pendiente de otro veredicto acumulativo independiente.** Revisar `43999499..HEAD`, con foco en el cierre financiero ante fallas de disponibilidad, preflight completo de aprobación masiva, inventario directo/transitivo de consumidores, paginación, terminalidad de lotes, replay canónico de overrides y aislamiento de facturación. Aún no está aceptada, mergeada, desplegada ni verificada en producción; sus migraciones Z7 existen y se probaron solo en Supabase local.
- ~~**Z2: re-review independiente de Sol sobre las rondas r21–r28**~~ — **CERRADA. Sol `APPROVE WITH NOTES` el 2026-08-07** tras r21–r30 (los doce ítems más 4 MAJOR + 6 MINOR de las dos pasadas siguientes), y **el ítem 12b se corrió el 2026-08-08** contra el tenant Zoom real. El mapa ítem → ronda → prueba quedó en `docs/plan/zoom/reviews/fase-5-review-request.md`
- ~~**Z0B: re-review independiente de Sol sobre los commits de Z0B-2r1**~~ — **CERRADA el 2026-07-30: Sol R2 `APPROVE`, cero hallazgos**, y Brent mergeó por **fast-forward** `2786fa8..f538cf4` vía PR [#25](https://github.com/brentcurtis76/fne-lms/pull/25) — elegido por la política de merge y porque preserva verbatim cada SHA que el ledger cita. *Corrección de conteo: el ff preservó **44** commits de rama; el «42» del desglose en Meta era el conteo correcto en el momento en que se escribió, dos commits antes del tip final, y se conserva como estaba en vez de reescribirlo sin poder redescomponerlo*
- **Z3 (escritorio): Sol `APPROVE`, CERO hallazgos (2026-08-08)** — registrado, no pendiente. Tercera pasada sobre este código y la primera sin nada que reportar. El reviewer re-corrió las cinco compuertas y obtuvo los números del PM exactamente, y **examinó por su cuenta la pregunta de la que dependía toda la revisión** —¿es Client View genuinamente inalcanzable?—: sí, por cinco vías independientes. Por eso M1/M2/M3 pueden pertenecer a Z3b: el split se sostiene sobre el examen del reviewer, no sobre la palabra del PM
- **Z0B: enmienda de DoD aprobada por el owner (F6)** — registrada, no pendiente. Queda como obligación para Z4: medir tamaños reales de MP4/M4A y throughput Vercel→Supabase en la PRIMERA sesión real grabada, antes de citar cualquier cifra de costo (item abierto 21)
- **Z1a: confirmar variables de entorno de producción en Vercel** — ya no bloquea el merge (#24 mergeado); queda como vigilancia post-merge, ver debts
- Verificar en el primer PR: los 6 checks aparecen y corren (evidencia DoD Fase 0) — Brent
- Revisar los pasajes reescritos de GENERA-01 (§4 sociograma, §6 secuencia+ley, §7 categorías, próximos pasos) — calidad de español y tono para Arnoldo/Sandra/Mora — Brent
- EIPD (documento legal) — requerida antes de Fase 2
- Redacción es-CL del consentimiento parental (libre, específica, inequívoca, informada)
- Due diligence copyright corpus RAG — bloquea Fases 10–11
- Ética sociométrica: toda superficie que pudiera revelar estatus de un menor (Fase 6, permanente)
