# A7a — CI evidence (PR #44, run 31264578140)

Branch `phase/a7a-links` @ `3fd3ea7c` (code `66774a58`, merge of `origin/main` @ `781ae16d`) ·
2026-08-08 · https://github.com/brentcurtis76/fne-lms/pull/44

**The first CI run this phase ever had.** A7a reached a Sol PASS on 2026-08-07 with one executor
round and one review, all green on local machines only; `ci.yml` triggers on `pull_request` and
`push: [main]`, and the branch had no PR, so none of the six checks had executed against this
code. Same gap as A8's, on the higher-risk branch of the two in deploy terms — seven public
marketing files including `pages/index.tsx` and `components/Footer.tsx`. See the 2026-08-08
Decision Log row.

## All six checks: pass

| Check | Result | Duration |
|---|---|---|
| Gate 1 — Typecheck | pass | 1m50s |
| Gate 1b — Lint (zero warnings) | pass | 1m12s |
| Gate 2 — Unit (Vitest) | pass | 1m29s |
| Gate 3 — RLS pgTAP (`supabase test db`) | pass | 2m21s |
| Gate 4 — E2E (Playwright, seeded ephemeral Supabase) | pass | 6m25s |
| RLS migration guard | pass | 8s |

Vercel preview deployment also completed. Gate 3 passes on a phase that touches no migration —
recorded because the merge brought A8's and Z2's DB work onto the branch, and this run is the
first to prove those gates green *together with* A7a's changes.

## Second run, same verdict (`31264951943` @ `e27cdbbc`)

Committing this evidence file and the ledger entry advanced the PR head, so CI ran again:
all six checks green a second time, in `1m48s / 1m9s / 1m50s / 2m5s / 6m23s / 9s`. **The code is
byte-identical between the two runs** — `e27cdbbc` is a docs-only commit on top of `3fd3ea7c`
(`git diff 3fd3ea7c e27cdbbc --stat` touches `docs/` only). Recorded so nobody has to wonder why
the ledger names one run and the PR shows another. Any further docs-only commit repeats the same
gates against the same code; that regress is expected and is not evidence of instability.

## What gate 4 proved

104 tests, `104 passed (54.3s)`, zero retries used. A7a's five new nav assertions executed for
the first time anywhere, on a production build:

```
✓  36 › pasantias-page.spec.ts:471:7 › the marketing site links to /pasantias › the homepage nav "PASANTÍAS" lands on the page (951ms)
✓  37 › pasantias-page.spec.ts:479:7 › the marketing site links to /pasantias › the footer "Pasantías" link lands on the page (880ms)
✓  38 › pasantias-page.spec.ts:488:7 › the marketing site links to /pasantias › the /programas nav "PASANTÍAS" lands on the page (487ms)
✓  39 › pasantias-page.spec.ts:495:7 › the marketing site links to /pasantias › the homepage INSPIRA CTA lands on the page instead of opening a flipbook (666ms)
✓  40 › pasantias-page.spec.ts:518:7 › the marketing site links to /pasantias › the Directivos CTA still opens its flipbook (596ms)
```

Test 40 is the one that matters most and is easy to overlook: it is the **control**. Tests 36–39
prove the INSPIRA flipbooks are gone and their triggers now navigate; only 40 proves the purge
stopped where it was supposed to, by opening the surviving Directivos flipbook in a real browser
and finding its `iframe.fp-iframe`. A partial-purge regression — deleting one pair while leaving
the other — is the single most plausible way this phase could have shipped broken, and it is the
gap Sol's S1 says the *source* guard does not close. The browser closes it.

Gate 4 also ran A8's `pasantias-leads-admin.spec.ts` (tests 25–27) green against the merged tree,
so the two phases do not interfere.

## Independent PM verification on the merged tree, before the PR

Re-run locally rather than taken from any report:

| Gate | Result |
|---|---|
| type-check | clean |
| lint | clean, zero warnings |
| Vitest | 6263/6263 across 266 files |
| production build | clean |
| `scripts/check-price-leak.mjs` | OK — 269 files scanned, no commercial data |
| A7a source guard (`pasantias-site-links.test.ts`) | 8/8 |

Acceptance criteria checked against the code, not the report: zero `#pasantias` hits in `pages/`
and `components/`; `id="pasantias"` preserved at `pages/index.tsx:276`; 15 literal `/pasantias`
hrefs; zero "Abril 2026"; both retired INSPIRA Heyzine IDs (`fb8cf2cfb1`, `fef3878d3c`) absent;
both Directivos IDs (`92bf9eb5ee`, `d87d80f309`) present across three uses; both image repoints
in place, originals undeleted (9,869,524 B → 1,539,868 B on the skyline).
