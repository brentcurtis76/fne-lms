# A8 — CI evidence (PR #43, run 31262431697)

Branch `phase/a8-leads-ui` @ `0cb0cf58` · 2026-08-08 · https://github.com/brentcurtis76/fne-lms/pull/43

**The first CI run this phase ever had.** Three executor rounds and three PM verifications were
green on local machines only; `ci.yml` triggers on `pull_request` and `push: [main]`, and the
branch had no PR, so none of the six checks had executed against this code. See the 2026-08-08
Decision Log row.

## All six checks: pass

| Check | Result |
|---|---|
| Gate 1 — Typecheck | pass |
| Gate 1b — Lint (zero warnings) | pass |
| Gate 2 — Unit (Vitest) | pass |
| Gate 3 — RLS pgTAP (`supabase test db`) | pass |
| Gate 4 — E2E (Playwright, seeded ephemeral Supabase) | pass |
| RLS migration guard | pass |

## What gate 4 proved that nothing else could

The seeder INSERT had never met a live Postgres in any round — `wt-a8` and `wt-a7a` share a
`supabase/config.toml` `project_id`, so running it locally would have seeded over the concurrent
A7a phase's database. Sol executed the tuple by hand inside `BEGIN…ROLLBACK` at its review; this
is the first time the seeder itself ran as code:

```
[seed-e2e] pasantias lead created — id 7b8d4aa7-6a59-48b2-bada-75369947b793
```

`tests/e2e/pasantias-leads-admin.spec.ts`, executing for the first time anywhere — all three
tests, on a production build against the seeded stack:

```
✓  25 › pasantias-leads-admin.spec.ts:72:7 › Pasantías leads — anonymous › is sent to the login page (299ms)
✓  26 › pasantias-leads-admin.spec.ts:37:7 › Pasantías leads — admin › opens the triage page and shows the seeded lead (501ms)
✓  27 › pasantias-leads-admin.spec.ts:59:7 › Pasantías leads — docente › is denied the triage page (425ms)
```

Suite total and the anti-skip guard — the spec is on `MANDATORY_SPECS` and genuinely ran, rather
than passing by being skipped:

```
99 passed (1.0m)
[e2e-mandatory] OK — 10 mandatory spec(s) ran with no skips
```

## Local evidence this run confirms

targeted 56/56 · full suite 265 files / 6255 tests · type-check clean · lint zero warnings ·
`lint:testid` clean on the new files · build green with `/admin/pasantia-leads` and
`/api/admin/pasantia-leads` both emitted.
