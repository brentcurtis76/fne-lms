1..164
ok 1 - La tabla email_contacts del schema public debe tener RLS habilitado
ok 2 - La tabla email_campaigns del schema public debe tener RLS habilitado
ok 3 - La tabla email_campaign_sends del schema public debe tener RLS habilitado
ok 4 - La tabla email_suppression del schema public debe tener RLS habilitado
ok 5 - La tabla email_webhook_events del schema public debe tener RLS habilitado
ok 6 - email_contacts: exactly one policy — admin SELECT-only, TO authenticated
ok 7 - email_campaigns: exactly one policy — admin SELECT-only, TO authenticated
ok 8 - email_campaign_sends: exactly one policy — admin SELECT-only, TO authenticated
ok 9 - email_suppression: exactly one policy — admin SELECT-only, TO authenticated
ok 10 - email_webhook_events: exactly one policy — admin SELECT-only, TO authenticated
ok 11 - comms tables: no policy carries a WITH CHECK — no authenticated write path exists
ok 12 - email_contacts / anon: the ACL carries no entry for it — every privilege the server defines, standard or not, is revoked
ok 13 - email_campaigns / anon: the ACL carries no entry for it — every privilege the server defines, standard or not, is revoked
ok 14 - email_campaign_sends / anon: the ACL carries no entry for it — every privilege the server defines, standard or not, is revoked
ok 15 - email_suppression / anon: the ACL carries no entry for it — every privilege the server defines, standard or not, is revoked
ok 16 - email_webhook_events / anon: the ACL carries no entry for it — every privilege the server defines, standard or not, is revoked
ok 17 - email_contacts / authenticated: the ACL carries exactly {SELECT}, NOT grantable — nothing else, whatever privileges this server version defines
ok 18 - email_campaigns / authenticated: the ACL carries exactly {SELECT}, NOT grantable — nothing else, whatever privileges this server version defines
ok 19 - email_campaign_sends / authenticated: the ACL carries exactly {SELECT}, NOT grantable — nothing else, whatever privileges this server version defines
ok 20 - email_suppression / authenticated: the ACL carries exactly {SELECT}, NOT grantable — nothing else, whatever privileges this server version defines
ok 21 - email_webhook_events / authenticated: the ACL carries exactly {SELECT}, NOT grantable — nothing else, whatever privileges this server version defines
ok 22 - email_contacts / PUBLIC: no grantee-0 ACL entry — a PUBLIC grant is inherited by anon and every other role, so per-role pins alone cannot see it
ok 23 - email_campaigns / PUBLIC: no grantee-0 ACL entry — a PUBLIC grant is inherited by anon and every other role, so per-role pins alone cannot see it
ok 24 - email_campaign_sends / PUBLIC: no grantee-0 ACL entry — a PUBLIC grant is inherited by anon and every other role, so per-role pins alone cannot see it
ok 25 - email_suppression / PUBLIC: no grantee-0 ACL entry — a PUBLIC grant is inherited by anon and every other role, so per-role pins alone cannot see it
ok 26 - email_webhook_events / PUBLIC: no grantee-0 ACL entry — a PUBLIC grant is inherited by anon and every other role, so per-role pins alone cannot see it
ok 27 - email_contacts: no ACL entry anywhere on the table is grantable — no role may re-grant it to a third party
ok 28 - email_campaigns: no ACL entry anywhere on the table is grantable — no role may re-grant it to a third party
ok 29 - email_campaign_sends: no ACL entry anywhere on the table is grantable — no role may re-grant it to a third party
ok 30 - email_suppression: no ACL entry anywhere on the table is grantable — no role may re-grant it to a third party
ok 31 - email_webhook_events: no ACL entry anywhere on the table is grantable — no role may re-grant it to a third party
ok 32 - anon: holds TRUNCATE on none of the five tables (RLS would not govern it)
ok 33 - authenticated: holds TRUNCATE on none of the five tables (RLS would not govern it)
ok 34 - anon: holds MAINTAIN on none of the five tables (PostgreSQL 17+)
ok 35 - authenticated: holds MAINTAIN on none of the five tables (PostgreSQL 17+)
ok 36 - email_contacts / service_role: CRUD privileges untouched by the REVOKEs — the only write path survives
ok 37 - email_campaigns / service_role: CRUD privileges untouched by the REVOKEs — the only write path survives
ok 38 - email_campaign_sends / service_role: CRUD privileges untouched by the REVOKEs — the only write path survives
ok 39 - email_suppression / service_role: CRUD privileges untouched by the REVOKEs — the only write path survives
ok 40 - email_webhook_events / service_role: CRUD privileges untouched by the REVOKEs — the only write path survives
ok 41 - email_campaign_sends.provider_batch_key exists (created unused at B3 for B10a)
ok 42 - email_campaign_sends.provider_batch_key is nullable — nothing writes it until B10a
ok 43 - email_webhook_events.detail exists (D-06 allowlisted, PII-free projection written only by process_webhook_event)

ok 44 - admin / email_contacts: SELECT allowed — the policy lets an admin read the seeded row
ok 45 - admin / email_campaigns: SELECT allowed — the policy lets an admin read the seeded row
ok 46 - admin / email_campaign_sends: SELECT allowed — the policy lets an admin read the seeded row
ok 47 - admin / email_suppression: SELECT allowed — the policy lets an admin read the seeded row
ok 48 - admin / email_webhook_events: SELECT allowed — the policy lets an admin read the seeded row
ok 49 - admin / email_contacts: INSERT denied at the privilege layer (no INSERT grant, no WITH CHECK policy)
ok 50 - admin / email_campaigns: INSERT denied at the privilege layer (no INSERT grant, no WITH CHECK policy)
ok 51 - admin / email_campaign_sends: INSERT denied at the privilege layer (no INSERT grant, no WITH CHECK policy)
ok 52 - admin / email_suppression: INSERT denied at the privilege layer (no INSERT grant, no WITH CHECK policy)
ok 53 - admin / email_webhook_events: INSERT denied at the privilege layer (no INSERT grant, no WITH CHECK policy)
ok 54 - admin / email_contacts: UPDATE denied at the privilege layer
ok 55 - admin / email_campaigns: UPDATE denied at the privilege layer
ok 56 - admin / email_campaign_sends: UPDATE denied at the privilege layer
ok 57 - admin / email_suppression: UPDATE denied at the privilege layer
ok 58 - admin / email_webhook_events: UPDATE denied at the privilege layer
ok 59 - admin / email_contacts: DELETE denied at the privilege layer — erasure is anonymize-only
ok 60 - admin / email_campaigns: DELETE denied at the privilege layer — erasure is anonymize-only
ok 61 - admin / email_campaign_sends: DELETE denied at the privilege layer — erasure is anonymize-only
ok 62 - admin / email_suppression: DELETE denied at the privilege layer — erasure is anonymize-only
ok 63 - admin / email_webhook_events: DELETE denied at the privilege layer — erasure is anonymize-only
ok 64 - admin / email_contacts: TRUNCATE denied — the SELECT-only posture survives a command RLS never sees
ok 65 - admin / email_campaigns: TRUNCATE denied — the SELECT-only posture survives a command RLS never sees
ok 66 - admin / email_campaign_sends: TRUNCATE denied — the SELECT-only posture survives a command RLS never sees
ok 67 - admin / email_suppression: TRUNCATE denied — the SELECT-only posture survives a command RLS never sees
ok 68 - admin / email_webhook_events: TRUNCATE denied — the SELECT-only posture survives a command RLS never sees

ok 69 - docente / email_contacts: SELECT returns 0 rows — the admin-only policy filters everything
ok 70 - docente / email_campaigns: SELECT returns 0 rows — the admin-only policy filters everything
ok 71 - docente / email_campaign_sends: SELECT returns 0 rows — the admin-only policy filters everything
ok 72 - docente / email_suppression: SELECT returns 0 rows — the admin-only policy filters everything
ok 73 - docente / email_webhook_events: SELECT returns 0 rows — the admin-only policy filters everything
ok 74 - docente / email_contacts: INSERT denied at the privilege layer
ok 75 - docente / email_campaigns: INSERT denied at the privilege layer
ok 76 - docente / email_campaign_sends: INSERT denied at the privilege layer
ok 77 - docente / email_suppression: INSERT denied at the privilege layer
ok 78 - docente / email_webhook_events: INSERT denied at the privilege layer
ok 79 - docente / email_contacts: UPDATE denied at the privilege layer
ok 80 - docente / email_campaigns: UPDATE denied at the privilege layer
ok 81 - docente / email_campaign_sends: UPDATE denied at the privilege layer
ok 82 - docente / email_suppression: UPDATE denied at the privilege layer
ok 83 - docente / email_webhook_events: UPDATE denied at the privilege layer
ok 84 - docente / email_contacts: DELETE denied at the privilege layer
ok 85 - docente / email_campaigns: DELETE denied at the privilege layer
ok 86 - docente / email_campaign_sends: DELETE denied at the privilege layer
ok 87 - docente / email_suppression: DELETE denied at the privilege layer
ok 88 - docente / email_webhook_events: DELETE denied at the privilege layer
ok 89 - docente / email_contacts: TRUNCATE denied at the privilege layer
ok 90 - docente / email_campaigns: TRUNCATE denied at the privilege layer
ok 91 - docente / email_campaign_sends: TRUNCATE denied at the privilege layer
ok 92 - docente / email_suppression: TRUNCATE denied at the privilege layer
ok 93 - docente / email_webhook_events: TRUNCATE denied at the privilege layer

ok 94 - anon / email_contacts: SELECT denied at the privilege layer (never reaches the policy)
ok 95 - anon / email_campaigns: SELECT denied at the privilege layer (never reaches the policy)
ok 96 - anon / email_campaign_sends: SELECT denied at the privilege layer (never reaches the policy)
ok 97 - anon / email_suppression: SELECT denied at the privilege layer (never reaches the policy)
ok 98 - anon / email_webhook_events: SELECT denied at the privilege layer (never reaches the policy)
ok 99 - anon / email_contacts: INSERT denied at the privilege layer
ok 100 - anon / email_campaigns: INSERT denied at the privilege layer
ok 101 - anon / email_campaign_sends: INSERT denied at the privilege layer
ok 102 - anon / email_suppression: INSERT denied at the privilege layer
ok 103 - anon / email_webhook_events: INSERT denied at the privilege layer
ok 104 - anon / email_contacts: UPDATE denied at the privilege layer
ok 105 - anon / email_campaigns: UPDATE denied at the privilege layer
ok 106 - anon / email_campaign_sends: UPDATE denied at the privilege layer
ok 107 - anon / email_suppression: UPDATE denied at the privilege layer
ok 108 - anon / email_webhook_events: UPDATE denied at the privilege layer
ok 109 - anon / email_contacts: DELETE denied at the privilege layer
ok 110 - anon / email_campaigns: DELETE denied at the privilege layer
ok 111 - anon / email_campaign_sends: DELETE denied at the privilege layer
ok 112 - anon / email_suppression: DELETE denied at the privilege layer
ok 113 - anon / email_webhook_events: DELETE denied at the privilege layer
ok 114 - anon / email_contacts: TRUNCATE denied — the command RLS never sees is stopped by the revoked grant
ok 115 - anon / email_campaigns: TRUNCATE denied — the command RLS never sees is stopped by the revoked grant
ok 116 - anon / email_campaign_sends: TRUNCATE denied — the command RLS never sees is stopped by the revoked grant
ok 117 - anon / email_suppression: TRUNCATE denied — the command RLS never sees is stopped by the revoked grant
ok 118 - anon / email_webhook_events: TRUNCATE denied — the command RLS never sees is stopped by the revoked grant

ok 119 - service_role: INSERT allowed on email_contacts
ok 120 - service_role: INSERT allowed on email_campaigns
ok 121 - service_role: INSERT allowed on email_campaign_sends
ok 122 - service_role: INSERT allowed on email_suppression
ok 123 - service_role: INSERT allowed on email_webhook_events
ok 124 - contacts: a non-anonymized row with no email violates the two-shape identity CHECK
ok 125 - contacts: email_normalized that is not lower(btrim(email)) violates the two-shape identity CHECK
ok 126 - contacts: a live row without an unsubscribe_token violates the identity CHECK (D-08 needs one per recipient)
ok 127 - contacts: a live row whose normalized email is the empty string violates the identity CHECK (email_normalized <> '')
ok 128 - contacts: a live row with an email but no email_normalized violates the identity CHECK (the equality term alone would evaluate to NULL, which a CHECK admits)
ok 129 - contacts: a live row with an email_normalized but no email violates the identity CHECK (same NULL-admits-the-row trap, mirrored)
ok 130 - contacts: anonymized_at set while the email survives violates the identity CHECK — no partial erasure
ok 131 - contacts / anonymized arm: a row with the whole identity set NULL but no anonymized_at marker violates the identity CHECK — erasure must be stamped, not inferred
ok 132 - contacts / anonymized arm: a retained email alone violates the identity CHECK
ok 133 - contacts / anonymized arm: a retained email_normalized alone violates the identity CHECK
ok 134 - contacts / anonymized arm: a retained first_name alone violates the identity CHECK
ok 135 - contacts / anonymized arm: a retained last_name alone violates the identity CHECK
ok 136 - contacts / anonymized arm: a retained organization alone violates the identity CHECK
ok 137 - contacts / anonymized arm: a retained basis_note alone violates the identity CHECK (it can name a person — this is the term round 1 could drop unnoticed)
ok 138 - contacts / anonymized arm: a retained unsubscribe_token alone violates the identity CHECK
ok 139 - contacts: the fully anonymized shape (all identity NULL + anonymized_at) commits
ok 140 - contacts: a live row may carry every identity field — the anonymized arm's NULL requirements do not apply to it
ok 141 - contacts: INSERT without legal_basis violates NOT NULL (no default may manufacture a legal basis)
ok 142 - contacts: INSERT without basis_recorded_at violates NOT NULL (no default)
ok 143 - contacts: legal_basis outside consent_form|customer_relationship|manual_verified violates the CHECK
ok 144 - contacts: consent_form basis without consent_notice_version violates the CHECK (D-12 evidence)
ok 145 - contacts: consent_form basis with its notice version commits
ok 146 - contacts: suppressed_at without suppression_reason violates the suppression CHECK
ok 147 - contacts: suppression_reason without suppressed_at violates the suppression CHECK
ok 148 - contacts: suppression_reason outside bounce|complaint|manual|failed|suppressed violates the CHECK
ok 149 - contacts: duplicate email_normalized violates the unique constraint
ok 150 - contacts: source outside the allowed set violates the CHECK
ok 151 - campaigns: status `failed` is rejected — D-07 removed it; a zero-audience campaign stays draft
ok 152 - campaigns: a freshly inserted campaign defaults to draft
ok 153 - sends: status outside pending|sending|sent|failed|skipped violates the CHECK
ok 154 - sends: a second row for the same (campaign, contact) violates the unique constraint — the ledger is the idempotency key
ok 155 - sends: a freshly inserted send row defaults to pending
ok 156 - sends: deleting a contact that has send history is RESTRICTed — erasure is anonymize-only, never a cascade
ok 157 - sends: deleting a campaign that has send history is RESTRICTed — the draft-only delete route is unaffected (drafts have no sends)
ok 158 - suppression: reason outside bounce|complaint|manual|failed|suppressed violates the CHECK
ok 159 - suppression: email_hash is the primary key — one tombstone per address
ok 160 - webhook events: svix_id is the primary key — a redelivery cannot insert twice (D-08 dedup)
ok 161 - webhook events: event_type is NOT NULL — a ledger row always says what it recorded
ok 162 - webhook events: detail defaults to an empty object — B4b projects the allowlisted subset into it, never a raw payload
ok 163 - email_contacts: set_updated_at trigger refreshes updated_at on UPDATE
ok 164 - email_campaigns: set_updated_at trigger refreshes updated_at on UPDATE
