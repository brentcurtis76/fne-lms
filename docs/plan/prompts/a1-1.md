# EXECUTOR PROMPT — Phase A1, round 1 (INSPIRA Comms)

BRANCH: `phase/a1-cohort`

FIRST ACTIONS (mandatory, in order):
1. Create your own isolated worktree — never work in the shared checkout:
   `git fetch origin && git worktree add ../wt-a1 -b phase/a1-cohort origin/main`
2. Copy `.env.local` from the main checkout into the worktree (the build gate
   dies at "Collecting page data" without it).
3. Work ONLY inside the worktree.

## Context you need

- `docs/plan/PLAN.md` **Phase A1** has your inlined acceptance criteria [A1]–[A5];
  **Appendix A is the normative source for every cohort fact** — dates, schools,
  experts, claims. Frozen decisions D-01 (public/commercial module split) and
  D-02 (prices only in brochure PDF among repo-authored surfaces) bind you.
- Cohort facts (Appendix A, owner-approved): week 1 lun 5–vie 9 oct 2026
  (immersion: each pasante 2,5 días Escola Virolai + 2,5 días Escola Sadako);
  free long weekend sáb 10–lun 12 (lunes 12 = Fiesta Nacional, colegios
  cerrados); week 2 mar 13–vie 16 (visitas 1–2 escuelas/día: El Puig,
  La Maquinista, Octavio Paz, Angeleta Ferrer, Les Vinyes — El Puig y
  Les Vinyes día completo, fuera de Barcelona). 9 visit days. 7 schools total.
  Experts per A-6 (Coral Regí directora, Mora del Fresno coordinadora, Jordi
  Musons director Sadako, Sandra Entrena **Encargada de Innovación** Virolai,
  Boris Mir, Sergi del Moral, Pepe Menéndez, Joan Quintana). 13 objectives +
  day structure + includes/excludes from Appendix A-7's named PPTX — the
  13 objective texts are in the brochure source; transcribe faithfully into
  the module (es-CL).
- Homepage: `pages/index.tsx` "Próximas Expediciones" card (~L300–313) shows
  stale "Abril 2026"/"Noviembre 2026" — replace per [A4]. Do NOT touch nav,
  flipbooks, contact form, or any other section (phase A7a already rewired
  links; A6a builds the landing page).
- CI: `.github/workflows/ci.yml` — wire the leak script into the build path
  per [A2] (a step after `next build` in gate 4, or a dedicated step — match
  the workflow's existing style).

## Scope

`lib/pasantias/cohort-public.ts`, `lib/pasantias/cohort-commercial.ts`,
`scripts/check-price-leak.mjs`, CI wiring, `__tests__/lib/pasantias-cohort.test.ts`,
`pages/index.tsx` (dates card only).

## Acceptance criteria — PLAN.md Phase A1 [A1]–[A5] verbatim; highlights

- Public module: zero monetary fields; two-tier school structure (immersion 2 +
  visits 5); weeks/free-days exactly per Appendix A-2/A-3/A-4.
- Commercial module: prices per A-8 (1000/560/1560, Madrid 810, mín 5, 50%+30d;
  "precios vigentes para la cohorte Octubre 2026" — no fixed validity date),
  `BROCHURE_VERSION`, `BROCHURE_FILENAME`, `COMMERCIAL_SENTINEL='__INSPIRA_COMMERCIAL__'`.
- Leak script: post-build scan of `.next/static/**` for the sentinel + price
  literals (context-scoped to avoid false positives; document exclusions in
  the script); green on this branch; wired into CI.
- Guard test: money math; **no session on 2026-10-12** nor free-weekend days;
  wk1 = 5 consecutive days lun 5–vie 9; wk2 = 4 days mar 13–vie 16; 9 visit
  days total; immersion list = 2 and visit list = 5 (names per A-5); public
  serialization has no monetary keys/values (`/€|price|precio|eur/i`).
- Homepage card: "Octubre 2026 · 5–9 y 13–16 de octubre" rendered from the
  PUBLIC module (no literal date strings left in `index.tsx` for this card),
  no prices; "Abril/Noviembre 2026" may remain ONLY in the Directivos flipbook
  title.

TEST COMMAND: `npx vitest run __tests__/lib/pasantias-cohort.test.ts`, then
`npm run type-check && npm run lint && npm test && npm run build && node scripts/check-price-leak.mjs`

RULES: SOP `docs/plan/AGENT-WORKFLOW.md` + repo `CLAUDE.md` govern. Surgical
changes; match patterns; max 3 red-test iterations then BLOCKED; plan-vs-reality
mismatch → STATUS: FINDINGS, stop coding. es-CL user-facing copy, English code.
Write `docs/planning/reviews/fase-a1-review-request.md`. WHEN DONE: append your
round entry to `docs/plan/LEDGER.md` (in your worktree), push the branch, and
output the EXECUTOR REPORT per SOP §2.3.
