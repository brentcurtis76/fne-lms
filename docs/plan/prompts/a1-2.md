# EXECUTOR PROMPT — Phase A1, round r2 (INSPIRA Comms)

Remediation of round r1's FINDINGS: the A-7 content source was unreachable.
It is now **verbatim inside the plan** — `docs/plan/PLAN.md`, section
"Appendix A-7 (verbatim content)". Round r1's code (commit `b03dd56` on
`phase/a1-cohort`) is verified and stays.

BRANCH: `phase/a1-cohort` (EXISTING — continue it)

FIRST ACTIONS (mandatory, in order):
1. `git fetch origin`. Try `git worktree add ../wt-a1r2 phase/a1-cohort`; if git
   refuses because the branch is checked out in another worktree, use
   `git worktree add --detach ../wt-a1r2 origin/phase/a1-cohort` and later push
   with `git push origin HEAD:phase/a1-cohort`.
2. Copy `.env.local` from the main checkout into the worktree.
3. Work ONLY in the worktree.

## Scope

Fill the pending content exports in `lib/pasantias/cohort-public.ts` from the
Appendix A-7 verbatim block, then retire the pending registry:

1. The 13 objectives — verbatim es-CL from the block (mechanical fixes only;
   do not rewrite copy).
2. Day structure (Mañana 1 / Mañana 2 / Tarde) incl. the workshops venue
   (escuelas y/o Instituto Relacional, Eixample).
3. Includes/excludes lists — per the UPDATED A-7 block: lodging is NOT an
   includes item anymore. Includes end with "comidas incluidas en los días de
   visita y cena de cierre"; no night counts, no per-day meal mapping anywhere.
   Add a test asserting no export matches /PENDIENTE|A-16/i.
3b. **Commercial module restructure (A-8 amended by owner — Decision Log
   2026-07-31):** remove the fixed lodging price and the combined total
   entirely (no `560`, no `1560` anywhere); add
   `lodgingPerNightEur: { min: 70, max: 120 }` + an es-CL descriptor
   ("según el tipo de alojamiento"); keep program €1.000, Madrid values as
   currently specced, mín 5, payment split, cohort-scoped validity. Update the
   guard tests (money-math assert becomes: program=1000, lodging range
   70≤min<max≤120, and **no export named or valued as a combined total**) and
   update `scripts/check-price-leak.mjs`'s PRICE_AMOUNT_PATTERNS to the
   current commercial values (note in a comment that the sentinel remains the
   primary tripwire).
4. Madrid extension school names (IDEO, Santa Gema, Virgen de Europa).
5. Remove `COHORT_CONTENT_PENDING` and any empty-export scaffolding.
6. Extend `__tests__/lib/pasantias-cohort.test.ts`: 13 objectives (count +
   non-empty + first/last spot-checks against the block), day structure fields
   non-empty, includes ≥6 / excludes ≥3 items, no export matches
   /PENDIENTE|A-16/i, and the existing D-01 serialization grep still passes
   (note: the objectives contain no monetary tokens — verify, don't assume).

Out of scope: everything else — homepage, leak script tuning (backlogged),
commercial module values, any other page or phase.

ACCEPTANCE: round r1's [A1] flips to fully met (no pending exports; content
matches Appendix A-7 verbatim); all r1 tests still green plus the new ones;
full gates green (`npm run type-check && npm run lint && npm test &&
npm run build && node scripts/check-price-leak.mjs`).

RULES: SOP `docs/plan/AGENT-WORKFLOW.md` + repo `CLAUDE.md`. Surgical; max 3
red iterations then BLOCKED; source mismatch → FINDINGS. Update
`docs/planning/reviews/fase-a1-review-request.md` with a round-r2 section.
WHEN DONE: LEDGER entry (in your worktree), push, EXECUTOR REPORT per SOP §2.3.
