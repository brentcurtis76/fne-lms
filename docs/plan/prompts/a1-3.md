# EXECUTOR PROMPT — Phase A1, round r3 (INSPIRA Comms) — pricing-model delta

Round r2 was executed faithfully against a prompt version that predated the
owner's lodging amendment (a dispatch race — PM triage in the branch ledger).
This round applies ONLY the missing delta. r2's content work stands.

SOURCE DISCIPLINE: `git fetch origin` FIRST, and read the plan sources from
origin/main HEAD — `git show origin/main:docs/plan/PLAN.md` (Appendix A-8
amended row + "Appendix A-7 (verbatim content)" section + Decision Log row
"Lodging pricing model changed") — never from a stale checkout.

BRANCH: `phase/a1-cohort` (EXISTING — continue it)

FIRST ACTIONS: 1) worktree per the usual rule (`git worktree add ../wt-a1r3
phase/a1-cohort`, or `--detach origin/phase/a1-cohort` + push
`HEAD:phase/a1-cohort` if the branch is held elsewhere); 2) copy `.env.local`
in; 3) work only there.

## The amendment, plainly (owner, 2026-07-31)

Barcelona lodging is no longer a fixed €560-double package and there is no
"€1.560 total" anywhere. New model: programa €1.000 por persona; alojamiento
variable **€70–120 por persona por noche, según el tipo de alojamiento**.
Prices are commercial data (D-01/D-02): the range lives in the COMMERCIAL
module only — the public module carries no lodging pricing and no lodging
includes-item at all.

## Scope — delta only

1. `lib/pasantias/cohort-commercial.ts`: remove the fixed lodging price and
   any combined total (no `560`, no `1560` in any form); add
   `lodgingPerNightEur: { min: 70, max: 120 }` and an es-CL note string
   ("Alojamiento en Barcelona: entre €70 y €120 por persona por noche, según
   el tipo de alojamiento."). Program €1.000, Madrid values, mín 5, payment
   split, cohort-scoped validity: unchanged.
2. `lib/pasantias/cohort-public.ts` includes list: REMOVE the
   "Alojamiento en Barcelona (base doble)" item; ensure the meals item reads
   per the current A-7 block — "Comidas incluidas en los días de visita y cena
   de cierre" (r2 omitted it under the superseded spec). Excludes unchanged.
3. `__tests__/lib/pasantias-cohort.test.ts`: money-math assert becomes —
   program === 1000; 70 ≤ lodging min < max ≤ 120; **no export equals 1560 or
   combines program+lodging into a total**; public serialization contains no
   lodging pricing and no "base doble"; meals item present; all other r1/r2
   asserts stay green (incl. the bounded `\beur(?:os?)?\b` token — keep it).
4. `scripts/check-price-leak.mjs`: update PRICE_AMOUNT_PATTERNS to the current
   commercial values (drop 560/1560; keep the others), with a comment noting
   the sentinel remains the primary tripwire.

ACCEPTANCE: the branch reflects the amended A-8 everywhere; r2's content work
untouched; full gates + leak script green
(`npx vitest run __tests__/lib/pasantias-cohort.test.ts && npm run type-check
&& npm run lint && npm test && npm run build && node scripts/check-price-leak.mjs`).

RULES: SOP + repo CLAUDE.md. Surgical; max 3 red iterations then BLOCKED;
mismatch with the plan sources → FINDINGS. Update
`docs/planning/reviews/fase-a1-review-request.md` with a round-r3 section.
WHEN DONE: LEDGER entry (worktree), push, EXECUTOR REPORT per SOP §2.3.
