# EXECUTOR PROMPT — Phase A1, round r4 (INSPIRA Comms) — Codex remediation

Closes REVIEW-A1.md's two BLOCKING findings (file on the branch; PM triage in
the branch ledger). All prior rounds stand.

BRANCH: `phase/a1-cohort` (EXISTING — continue it)
FIRST ACTIONS: `git fetch origin`; worktree per the usual rule (`--detach
origin/phase/a1-cohort` + push `HEAD:phase/a1-cohort` if the branch is held);
copy `.env.local` in; read plan sources from origin/main HEAD.

## Finding 1 — scanner blind to the band values

`scripts/check-price-leak.mjs` PRICE_AMOUNT_PATTERNS covers 1.000/1e3/810 but
not €70/€120 — the amended A-8's lodging band IS commercial data (D-01/D-02).
Fix INSIDE the scanner's existing currency-context machinery (bare `70`/`120`
would false-positive across every bundle): patterns for 70 and 120 that only
fire in currency-adjacent context (€/EUR window), plus commercial-copy
fragments of the band note ("por persona por noche") as copy patterns. Prove
red-then-green: scratch-leak the note string into a client page, show the
scanner fails, revert (append to docs/plan/evidence/a1/leak-guard.md).

## Finding 2 — serialization guard watches one export

The guard test serializes `COHORT_PUBLIC` only. Rewrite it to
`import * as cohortPublic` and assert the ENTIRE module namespace serialization
carries no monetary keys/values (same bounded `\beur(?:os?)?\b` token) — so a
future standalone export cannot bypass the guard. Keep all existing asserts.

ACCEPTANCE: both findings closed; the red-then-green scanner proof committed;
full gates + leak script green (targeted vitest && type-check && lint &&
npm test && build && node scripts/check-price-leak.mjs).
RULES: SOP + repo CLAUDE.md; surgical; max 3 red iterations then BLOCKED.
Round-r4 section in docs/planning/reviews/fase-a1-review-request.md.
WHEN DONE: LEDGER entry (worktree), push, EXECUTOR REPORT per SOP §2.3.
