# EXECUTOR PROMPT — Phase A1, round r5 (INSPIRA Comms) — owner-directed Madrid removal

Owner decision 2026-08-02 (Decision Log): the "Opcional Madrid" extension was
an accidental carry from the stale PPTX — no Madrid pasantías exist. A1 had
already passed Codex; this round runs on owner authority. Everything else on
the branch stands.

BRANCH: `phase/a1-cohort` (EXISTING — continue it)
FIRST ACTIONS: `git fetch origin`; worktree per the usual rule (detach
fallback + push `HEAD:phase/a1-cohort` if held); copy `.env.local` in; read
plan sources from origin/main HEAD.

## Scope — remove Madrid everywhere on this branch

1. `lib/pasantias/cohort-public.ts`: delete `COHORT_MADRID_SCHOOLS` (and any
   other madrid-named export or field).
2. `lib/pasantias/cohort-commercial.ts`: delete the Madrid values (400 / 810 /
   360 / 50-tren, whatever fields carry them).
3. `scripts/check-price-leak.mjs`: drop the `810` pattern and any Madrid copy
   fragments; note the removal in the pattern comment.
4. Tests: remove Madrid asserts; ADD a guard — no export of either cohort
   module matches /madrid/i (so it cannot silently return without a plan
   change).
5. Greps prove the purge: `grep -rin "madrid" lib/pasantias __tests__/lib/pasantias-cohort.test.ts scripts/check-price-leak.mjs` → empty.

ACCEPTANCE: purge complete + guarded; full gates + leak script green
(targeted vitest && type-check && lint && npm test && build &&
node scripts/check-price-leak.mjs).
RULES: SOP + repo CLAUDE.md; surgical; max 3 red iterations then BLOCKED.
Round-r5 section in docs/planning/reviews/fase-a1-review-request.md.
WHEN DONE: LEDGER entry (worktree), push, EXECUTOR REPORT per SOP §2.3.
