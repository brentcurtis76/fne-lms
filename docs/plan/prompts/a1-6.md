# EXECUTOR PROMPT — Phase A1, round r6 (INSPIRA Comms) — owner-directed label fix

Owner decision 2026-08-02 (Decision Log + Appendix A-1): the live homepage
label "Octubre 2026 · 5–9 y 13–16" reads as TWO pasantías. The headline
date-span everywhere becomes the single span **"Octubre, 5 al 16"**. A1 is
merged, so this micro-round runs on a fresh branch from main.

BRANCH: `phase/a1-r6-label` (from origin/main)
FIRST ACTIONS: 1) `git fetch origin && git worktree add ../wt-a1r6 -b phase/a1-r6-label origin/main`
2) copy `.env.local` in; 3) work ONLY there.

## Scope

1. `lib/pasantias/cohort-public.ts`: change the label derivation
   (`buildCohortDateLabel` or equivalent) to produce the single span — month
   name + first visit day of week 1 + "al" + last visit day of week 2, i.e.
   exactly "Octubre, 5 al 16" for this cohort — still DERIVED from the week
   data (no literal date string). The year stays wherever it already renders
   separately (card title); do not duplicate it into the span.
2. Update every test that pins the old two-range label; add a pin for the new
   exact string and keep the derivation asserts (weekday/ISO checks stand).
3. Nothing else — no card layout changes, no other copy.

ACCEPTANCE: homepage card renders "Octubre, 5 al 16" (with "2026" still
visible on the card as today); label remains data-derived; full gates + leak
script green (targeted vitest && type-check && lint && npm test && build &&
node scripts/check-price-leak.mjs).
RULES: SOP + repo CLAUDE.md; surgical; max 3 red iterations then BLOCKED.
Review-request `docs/planning/reviews/fase-a1r6-review-request.md`.
WHEN DONE: LEDGER entry (worktree), push, EXECUTOR REPORT per SOP §2.3.
