# EXECUTOR PROMPT — Phase A2, round r2 (INSPIRA Comms) — Codex remediation

Remediation of REVIEW-A2.md's two BLOCKING findings (file is on the branch).
Round r1's migration + suite are otherwise sound and stay.

BRANCH: `phase/a2-leads-db` (EXISTING — continue it)

FIRST ACTIONS (mandatory, in order):
1. `git fetch origin`. Try `git worktree add ../wt-a2r2 phase/a2-leads-db`; if
   git refuses (branch checked out elsewhere), use
   `git worktree add --detach ../wt-a2r2 origin/phase/a2-leads-db` and push
   later with `git push origin HEAD:phase/a2-leads-db`.
2. Copy `.env.local` from the main checkout into the worktree.
3. You are the only session using the local Supabase stack.

## The findings, plainly

1. RLS/WITH CHECK do not govern TRUNCATE. Supabase's default grants give
   `anon` and `authenticated` broad table privileges — including TRUNCATE —
   so either role could empty `pasantias_leads` at the SQL layer despite the
   SELECT-only policy. D-04 (amended — see the Decision Log) now requires
   privilege-level revocation on every comms table.
2. The pgTAP anon matrix lacks UPDATE and DELETE denial asserts.

## Scope — the SAME two files, edited in place (nothing is merged/deployed;
   do NOT add a second migration)

1. `supabase/migrations/20260731140500_add_pasantias_leads.sql`:
   after the policy block add explicit privilege hardening —
   `REVOKE ALL ON public.pasantias_leads FROM anon;`
   `REVOKE INSERT, UPDATE, DELETE, TRUNCATE, REFERENCES, TRIGGER ON public.pasantias_leads FROM authenticated;`
   (authenticated keeps SELECT — the RLS policy gates rows; service_role's
   grants untouched). Update the header's access matrix to state the
   privilege layer explicitly.
2. `supabase/tests/030-pasantias-leads-rls.sql`: add asserts —
   `has_table_privilege('anon', 'public.pasantias_leads', 'TRUNCATE')` false,
   same for `authenticated`; anon lacks SELECT/INSERT/UPDATE/DELETE privileges;
   authenticated lacks INSERT/UPDATE/DELETE/TRUNCATE privileges but HAS SELECT;
   plus behavioral anon UPDATE matches-0 and anon DELETE matches-0 (finding 2).
   Update the plan count header accordingly.

ACCEPTANCE: both findings closed; `supabase db reset` + `npm run test:db`
green with the enlarged suite; four JS gates green (they cannot be affected —
run them anyway and say so); evidence TAP refreshed under
`docs/plan/evidence/a2/`.

RULES: SOP + repo CLAUDE.md. Surgical — only what the findings require; max 3
red iterations then BLOCKED. Update
`docs/planning/reviews/fase-a2-review-request.md` with a round-r2 section.
WHEN DONE: LEDGER entry (worktree), push, EXECUTOR REPORT per SOP §2.3.
