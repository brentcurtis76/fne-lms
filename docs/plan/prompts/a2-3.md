# EXECUTOR PROMPT — Phase A2, round r3 (INSPIRA Comms) — MAINTAIN closure

Owner-authorized third round (§1.5 — Decision Log). Closes REVIEW-A2-R2.md's
single remaining finding: the r2 denylist omits PostgreSQL 17's `MAINTAIN`
privilege. PM-verified fact you may rely on: production runs PostgreSQL 15.8,
where `MAINTAIN` does not exist — the fix must be UPGRADE-PROOF, and the
local stack (also PG15) cannot exercise a PG17-only privilege directly.

BRANCH: `phase/a2-leads-db` (EXISTING — continue it)

FIRST ACTIONS: 1) `git fetch origin`; `git worktree add ../wt-a2r3
phase/a2-leads-db` — if refused (branch held elsewhere), use
`--detach origin/phase/a2-leads-db` + push `HEAD:phase/a2-leads-db`.
2) Copy `.env.local` in. 3) You own the local Supabase stack.

## Scope — the SAME two files, edited in place

1. `supabase/migrations/20260731140500_add_pasantias_leads.sql`:
   replace the authenticated denylist with the grant-list form —
   `REVOKE ALL ON public.pasantias_leads FROM authenticated;`
   `GRANT SELECT ON public.pasantias_leads TO authenticated;`
   (`REVOKE ALL ... FROM anon;` stays). Header matrix updated: grant-list
   form is the standard precisely because privileges that do not exist yet
   (like PG17's MAINTAIN) can never leak into an allowlist. Codex has bound
   B3's five tables to this same form.
2. `supabase/tests/030-pasantias-leads-rls.sql`:
   - Replace the grant-set pins with asserts over the REAL ACL —
     `aclexplode(relacl)` on `pg_class` (the SQL-standard information_schema
     view hides PG-specific privileges, which is exactly how r2's pin missed
     this): authenticated's ACL entries for the table = exactly {SELECT};
     anon has none.
   - Add a version-guarded MAINTAIN assert: when
     `current_setting('server_version_num')::int >= 170000`,
     `has_table_privilege` for MAINTAIN is false for anon and authenticated;
     below 17, emit the corresponding pgTAP `skip`s (portable suite — state
     the reason string clearly). Keep the plan count exact.
3. Refresh `docs/plan/evidence/a2/` TAP output; round-r3 section in
   `docs/planning/reviews/fase-a2-review-request.md`.

ACCEPTANCE: grant-list form in place; ACL-level pins prove {SELECT}-only for
authenticated and nothing for anon regardless of server version; suite green
on the PG15 local stack with the MAINTAIN asserts skipping visibly;
`supabase db reset && npm run test:db` green; four JS gates green.

RULES: SOP + repo CLAUDE.md. Surgical; max 3 red iterations then BLOCKED.
WHEN DONE: LEDGER entry (worktree), push, EXECUTOR REPORT per SOP §2.3.
