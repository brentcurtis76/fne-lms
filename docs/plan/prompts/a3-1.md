# EXECUTOR PROMPT — Phase A3, round r1 (INSPIRA Comms)

BRANCH: `phase/a3-pdfgen`
FIRST ACTIONS: 1) `git fetch origin && git worktree add ../wt-a3 -b phase/a3-pdfgen origin/main`
2) copy `.env.local` from the main checkout into the worktree; 3) work ONLY
there; 4) read plan sources from origin/main HEAD (`docs/plan/PLAN.md` Phase
A3 as amended — Appendix A normative; NO Madrid content exists; lodging =
per-person band en base doble with the coordination framing per A-8).

## Context

- Content comes ONLY from the merged cohort modules: `lib/pasantias/cohort-public.ts`
  (13 objectives, day structure, two-tier schools, experts, includes/excludes,
  claims, dates) + `lib/pasantias/cohort-commercial.ts` (SERVER-ONLY — prices,
  lodging band + note, BROCHURE_VERSION/FILENAME). D-01: the commercial module
  may be imported only by the server-side brochure generator. D-05 governs.
- Reuse the house React-PDF kit: `lib/propuestas/fonts.ts`, `styles.ts`,
  `components/{Typography,LightSection,DarkSection,BrandElements}.tsx`;
  `renderToBuffer` pattern in `lib/propuestas/generator.ts` (~L82-111). Do NOT
  modify lib/propuestas/* — write brochure-local components where the kit
  doesn't fit.
- Controller identity for the contact/legal page: `LEGAL_IDENTITY` in
  `lib/legal/privacy-notice.ts` (brand + legal name + RUT + address — never
  brand alone).

## Scope

`lib/pasantias/brochure.tsx`, `lib/pasantias/ficha.tsx` (shared sections under
`lib/pasantias/pdf/` as needed); `lib/pasantias/__tests__/pdf.test.ts`; visual
QA renders committed to `docs/plan/evidence/a3/`.
NOT in scope: serving endpoints/caching (A4), any page, any endpoint.

## Acceptance criteria (PLAN.md Phase A3, amended)

- [A1] `generateBrochure()` → Buffer: portada (INSPIRA · Barcelona · Octubre
  2026), qué es + 13 objetivos, estructura del día, itinerario 2 semanas
  (semana 1 inmersión Virolai+Sadako 2,5 días c/u; fin de semana largo sáb
  10–lun 12 con Fiesta Nacional; semana 2 visitas 1–2 escuelas/día, El Puig y
  Les Vinyes día completo), las 7 escuelas (2 inmersión + 5 visitas), equipo
  (8, titles per A-6), **inversión per amended A-8**: programa €1.000 +
  alojamiento aparte (band €70–120 pp/noche, base doble, coordination framing
  "se coordina con el equipo FNE según tu preferencia"), forma de pago (50% +
  saldo 30 días), "precios vigentes para la cohorte Octubre 2026",
  incluye/no incluye (verbatim from the module), contacto (LEGAL_IDENTITY +
  info@ + WhatsApp +56 9 4162 3577). **No Madrid anywhere. No night counts.**
- [A2] `generateFicha()` → 1–2 page Buffer, NO monetary tokens: qué es,
  fechas, escuelas two-tier, día tipo, equipo destacado, CTA (web + WhatsApp).
- [A3] PDF TEXT-EXTRACTION tests (real extraction, pattern in
  `lib/propuestas/__tests__/generator.test.ts`): brochure text contains
  "1.000" AND "70" AND "120" AND "base doble" AND NO "1.560"/"560"/"Madrid";
  ficha text contains no monetary tokens and no "Madrid"; both start %PDF;
  brochure ≥5 pages, ficha ≤2.
- [A4] Visual QA: every page rendered to PNG at 144 DPI, committed under
  `docs/plan/evidence/a3/`; no clipped/overflowing text, readable hierarchy,
  accented es-CL glyphs correct. The PM inspects the committed renders.
- [A5] es-CL filename constants RFC 5987-safe (consumed by A4's headers).
- [A6] Gates green (targeted vitest && type-check && lint && npm test &&
  build). The D-01 leak script must stay green (the brochure generator is
  server-side — nothing commercial may enter client bundles).

RULES: SOP + repo CLAUDE.md; surgical; max 3 red iterations then BLOCKED;
content mismatch vs the modules/plan → FINDINGS, never invent copy.
Review-request `docs/planning/reviews/fase-a3-review-request.md`.
WHEN DONE: LEDGER entry (worktree), push, EXECUTOR REPORT per SOP §2.3.
