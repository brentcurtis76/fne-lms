# EXECUTOR PROMPT — Phase A3, round r2 (INSPIRA Comms) — retired-label fix

PM visual QA found both documents' subtitle lines rendering the RETIRED
two-range date label ("5–9 y 13–16 de octubre de 2026") — the branch predates
the r6 single-span change on main ("Octubre, 5 al 16"). Root cause: branch
race, not your error. Everything else passed QA.

BRANCH: `phase/a3-pdfgen` (EXISTING — continue it)
FIRST ACTIONS: worktree per the usual rule (detach fallback + push
`HEAD:phase/a3-pdfgen` if held); copy `.env.local` in; `git fetch origin`.

## Scope

1. Merge `origin/main` into the branch (brings r6's derived single-span
   `buildCohortDateLabel` + updated cohort tests). Resolve docs conflicts by
   taking main's PLAN and unioning LEDGER (keep every entry — the PM's union
   pattern is visible in recent ledger entries; if unsure, stop with FINDINGS
   rather than dropping entries).
2. If the brochure/ficha subtitles consume the module's derived label, they
   fix themselves — verify; if any local composition of the two-range string
   exists, replace it with the derived label. The detailed Fechas table
   (semana 1 / fin de semana / semana 2) STAYS — itinerary context is where
   the two-week detail belongs.
3. Re-render ALL evidence PNGs (the QA script), inspect, and re-commit;
   subtitle must read the single span with the year rendered once.
4. Pin the subtitle form in the extraction tests: contains "Octubre, 5 al 16",
   does NOT contain "5–9 y 13–16".
5. Hardening (PM-accepted from your r1 note): make the "560"-absence check
   currency-context-scoped instead of a bare substring, so RUT/phone digit
   coincidences cannot mask a real leak.

ACCEPTANCE: subtitle single-span in both documents (rendered evidence);
extraction pins updated; currency-context 560 check; full gates + leak
script green.
RULES: SOP + repo CLAUDE.md; surgical; max 3 red iterations then BLOCKED.
Round-r2 section in docs/planning/reviews/fase-a3-review-request.md.
WHEN DONE: LEDGER entry (worktree), push, EXECUTOR REPORT per SOP §2.3.
