# EXECUTOR PROMPT — Phase A4, round r2 (INSPIRA Comms) — cache-poisoning fix

Closes the finding YOU raised in r1 (PM accepted it): the `propuestas` bucket is
on the one shared Supabase instance, and `.env.local` sets
`NEXT_PUBLIC_APP_URL=http://localhost:3000`, so a developer hitting the route
locally on a cold cache uploads a PDF printing `localhost:3000/pasantias` into
the path production reads. r1's unconditional upload was per spec; the spec was
wrong.

BRANCH: `phase/a4-pdfsrv` (EXISTING — continue it)
FIRST ACTIONS: `git fetch origin`; worktree per the usual rule (detach fallback
+ push `HEAD:phase/a4-pdfsrv` if held); copy `.env.local` in.

## Scope — `lib/pasantias/pdf/serve.ts` + its tests

1. **Only production writes to the shared cache.** On a cache miss: always
   generate and serve; upload **only** when the runtime is production
   (`process.env.VERCEL_ENV === 'production'`). Everywhere else — local, preview,
   CI — generate-and-serve without uploading, and log once at info level saying
   why, so a developer is not left wondering where the cache went.
2. Cache **reads** stay unconditional in every environment: a warm production
   object is exactly what a preview should serve, and reading cannot poison.
3. State the rule in the module doc comment alongside the D-05 override
   contract — the two interact (an owner-uploaded designed brochure is served
   everywhere; only *generated* bytes are production-gated).
4. Tests: production env uploads then serves; non-production generates and
   serves with **no upload call**; cache hit still serves in both; existing
   degrade-on-upload-failure behaviour unchanged for production.

ACCEPTANCE: no non-production code path can write to the shared bucket; all r1
tests still green plus the new env cases; gates green (`type-check && lint &&
npm test && build && node scripts/check-price-leak.mjs`).
RULES: SOP + repo CLAUDE.md; surgical — nothing outside the shared handler and
its tests; max 3 red iterations then BLOCKED.
Round-r2 section in `docs/planning/reviews/fase-a4-review-request.md`.
WHEN DONE: LEDGER entry, push, EXECUTOR REPORT per SOP §2.3.
