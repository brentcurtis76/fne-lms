# EXECUTOR PROMPT — Phase A5, round r1 (INSPIRA Comms)

BRANCH: `phase/a5-lead-api`
FIRST ACTIONS: `git fetch origin && git worktree add ../wt-a5 -b phase/a5-lead-api origin/main`;
copy `.env.local` in; work ONLY there; read Appendix A + D-12 from origin/main HEAD.

**The `pasantias_leads` table is LIVE in production** (applied 2026-08-02):
`authenticated` = SELECT only, `anon` = no privileges at all, `service_role` =
the sole write path. Your API therefore uses `createServiceRoleClient()` for
every read and write — an anon-key read of this table returns nothing, by design.

## Scope

`lib/pasantias/leads.ts` (transition helper + validation), `pages/api/pasantias/lead.ts`,
`lib/pasantias/emails.ts`, `__tests__/api/pasantias-lead.test.ts`,
`__tests__/lib/pasantias-leads.test.ts`.
NOT in scope: the landing page/form (A6a/A6b), admin triage (A8), PDF routes (A4).

## Acceptance criteria

- [A1] POST-only (405 otherwise); rate-limited (`lib/rateLimit.ts`, 5/min);
  honeypot field `website` returns a **fake success 200 without inserting**.
- [A2] Validation → 400 with es-CL field errors: required first_name, last_name,
  email (`isValidEmail` from `lib/signups.ts`), institution, `consent === true`;
  `cohort` must equal the public module's cohort id; length caps
  (80/80/140/140/40/1000). Optional: phone, role_title, num_people (1–60),
  message, utm_*.
- [A3] **Split consent per D-12:** always persist `consent_accepted_at`
  (server clock) + `consent_notice_version` (`PRIVACY_NOTICE_VERSION` from
  `lib/legal/privacy-notice.ts`). `marketingOptIn` is optional and defaults
  false; when true, also stamp `marketing_opt_in_at` + `marketing_notice_version`.
  **The DB CHECK enforces the two shapes — never send a half-set.**
- [A4] Dedup on `(email_normalized, cohort)`: new → INSERT; existing → update
  contact fields; `dismissed → new` **only** via `canTransitionLead`; a 23505
  race is handled as the duplicate path. **Both paths return an identical
  `200 {success:true}`** (anti-enumeration).
- [A5] Resubmission may SET marketing opt-in true (with fresh evidence) but
  **never silently clears an earlier true** — opt-out is unsubscribe's job.
  Test both directions.
- [A6] Transition helper `canTransitionLead(from,to)` with the D-03 graph:
  `new→contacted`, `new→dismissed`, `contacted→converted`, `contacted→dismissed`,
  `dismissed→new`; `converted` terminal. Test every allowed edge AND that every
  other pair is denied.
- [A7] Emails after persist, best-effort (failure logs, still 200): auto-reply to
  the lead (es-CL, FNE-branded minimal layout — NOT the Genera `emailLayout`),
  containing the brochure link via `buildAbsoluteUrl('/api/pasantias/brochure')`
  and **no prices** (D-02); plus an internal notification to
  `info@nuevaeducacion.org`. `brochure_sent_at` set only on auto-reply success.
  **Every user-supplied string HTML-escaped** at every interpolation (hostile-input
  tests). Auto-reply deduped: not re-sent more than once per 24h per lead.
  Missing `RESEND_API_KEY` → soft-fail (log + 200), as `grant.ts` does.
- [A8] Tests per the above + gates green (`type-check && lint && npm test && build`).
  `npm run test:db` not required (no schema change); e2e belongs to A6b.

RULES: SOP + repo CLAUDE.md; surgical; match the house API patterns
(`lib/api-auth.ts`, `lib/signups.ts`); max 3 red iterations then BLOCKED;
conflict with Appendix A or D-12 → FINDINGS. Review-request
`docs/planning/reviews/fase-a5-review-request.md`.
WHEN DONE: LEDGER entry, push, EXECUTOR REPORT per SOP §2.3.
