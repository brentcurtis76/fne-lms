# EXECUTOR PROMPT — Phase A5, round r2 (INSPIRA Comms) — source_path

Closes the finding YOU raised in r1. `pasantias_leads.source_path` exists on the
live table, but my [A2] optional-field list omitted it, so the column would stay
permanently null and A6b could not attribute where a lead came from. The
omission is the PM's, not yours.

BRANCH: `phase/a5-lead-api` (EXISTING — continue it)
FIRST ACTIONS: `git fetch origin`; worktree per the usual rule (detach fallback
+ push `HEAD:phase/a5-lead-api` if held); copy `.env.local` in.

## Scope — `pages/api/pasantias/lead.ts`, `lib/pasantias/leads.ts`, tests

1. Accept an optional `sourcePath` string alongside the utm fields; validate it
   the way the others are validated, cap it at 200 chars, and persist it to
   `source_path` on INSERT.
2. **Treat it as untrusted client input**: it is a path the browser reports, so
   accept only a same-site relative path (must start with `/`, no scheme, no
   `//host` protocol-relative form, no CR/LF) — reject or drop anything else
   rather than storing it. Say which you chose and why in the report.
3. On the duplicate/update path, refresh `source_path` only when the new
   submission supplies one — never overwrite a stored value with null.
4. Tests: accepted and persisted on insert; rejected/dropped shapes
   (`https://evil.example`, `//evil.example`, `javascript:…`, embedded CRLF,
   over-cap); update path preserves an existing value when the field is absent.

ACCEPTANCE: `source_path` is populated by real submissions and cannot carry an
off-site or injected value; all r1 tests still green; gates green.
RULES: SOP + repo CLAUDE.md; surgical; max 3 red iterations then BLOCKED.
Round-r2 section in `docs/planning/reviews/fase-a5-review-request.md`.
WHEN DONE: LEDGER entry, push, EXECUTOR REPORT per SOP §2.3.
