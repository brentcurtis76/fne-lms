# EXECUTOR PROMPT — Phase A5, round r3 (INSPIRA Comms) — Sol remediation

Closes REVIEW-A5.md's BLOCKING finding and its SHOULD-FIX.

BRANCH: `phase/a5-lead-api` (EXISTING — continue it)
FIRST ACTIONS: `git fetch origin`; worktree per the usual rule; copy `.env.local` in.

## BLOCKING — a stale submission can overwrite a concurrent marketing opt-in

The route reads the existing lead, decides what to write, then writes. Between
that read and that write, another submission (or an admin action) can set
`marketing_opt_in = true`; a decision computed from the stale read then clobbers
it. This is a **consent regression** — the person opted in and the record says
they did not — so it matters more than the ordinary lost-update case.

Fix it so the write cannot depend on the stale read:
- **Never write the marketing columns on the update path unless this submission
  is itself opting in** (`marketingOptIn === true`, with fresh evidence). An
  absent or false checkbox writes nothing at all — not `false`, not NULL.
  Opting out is unsubscribe's job, never a form omission.
- If any code path still needs to clear them, it must be conditional in the
  statement itself (e.g. a guarded UPDATE), not decided in application memory
  from an earlier SELECT.
- Test the race explicitly: simulate the interleaving (read returns
  `marketing_opt_in: false`, the write is issued after a concurrent true) and
  assert the update payload contains **no** marketing keys, so a stale
  submission is structurally incapable of clearing the flag.

## BLOCKING (2) — the 24h auto-reply dedup is not atomic

The route reads `brochure_sent_at`, decides, then sends and writes, so two
submissions inside the window both read null and both send. r1 recorded this as
a bounded courtesy-mail duplicate and **the PM accepted it; Sol blocks on it and
Sol is right** — it is the same read-decide-write shape as the consent bug in
this same file, and shipping two of them is indefensible.

Make it **claim-then-act**, not check-then-act: atomically claim the right to
send with a conditional statement — an UPDATE setting `brochure_sent_at = now()`
guarded by `WHERE id = $1 AND (brochure_sent_at IS NULL OR brochure_sent_at <
now() - interval '24 hours')` that **returns the row**. Send only if the claim
returned a row; if it returned none, another request owns this window, so skip
silently. If the send then fails, state whether you roll the claim back
(re-opening the window) or leave it claimed — either is defensible, but say
which and why, and the failure must still return 200.
Test the interleaving: two concurrent submissions, exactly one send.

## SHOULD-FIX — `sourcePath` trims, contradicting the verbatim contract

r2 claims values are stored byte-identical to what the browser reported, but the
sanitiser trims first. Pick one and make code, tests and comment agree:
either stop trimming (validate and store exactly what arrived), or keep trimming
and correct the contract wording everywhere it appears — including the note in
the r2 review-request section and the assumption recorded in the ledger. State
which you chose and why.

ACCEPTANCE: the interleaving test proves a stale unchecked submission cannot
clear an opt-in; a second concurrent submission cannot produce a second
auto-reply inside the window; contract and behaviour agree on trimming; all
r1/r2 tests still green; gates green.
RULES: SOP + repo CLAUDE.md; surgical; max 3 red iterations then BLOCKED; if the
fix implies a schema or D-12 change → FINDINGS rather than improvising.
Round-r3 section in `docs/planning/reviews/fase-a5-review-request.md`.
WHEN DONE: LEDGER entry, push, EXECUTOR REPORT per SOP §2.3.
