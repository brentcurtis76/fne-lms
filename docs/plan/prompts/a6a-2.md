# EXECUTOR PROMPT — Phase A6a, round r2 (INSPIRA Comms) — data propagation

Closes the finding YOU raised in r1, and it is the PM's miss: Appendix A-6 was
amended on 2026-08-02 with corrected expert titles, but nobody propagated it
into `lib/pasantias/cohort-public.ts`, so four people render as the placeholder
"Experto invitado" on the live page. You were right to render the module rather
than hand-write a title.

BRANCH: `phase/a6a-page` (EXISTING — continue it, so page and data land together)
FIRST ACTIONS: `git fetch origin`; worktree per the usual rule; copy `.env.local`
in; read **Appendix A-6 and §5b of `docs/plan/design/brochure-content-pack.md`
from origin/main HEAD** — both are canonical.

## Scope

1. **Expert titles** — `COHORT_EXPERTS` in `lib/pasantias/cohort-public.ts`, per
   Appendix A-6 verbatim:
   - Coral Regí — Directora del programa INSPIRA *(already correct)*
   - Mora del Fresno — Coordinadora INSPIRA *(already correct)*
   - Jordi Musons — Director, Escola Sadako · anfitrión semana 1 *(correct)*
   - Sandra Entrena — Encargada de Innovación, Escola Virolai · anfitriona
     semana 1 *(correct)*
   - **Boris Mir** — ex-director adjunto, Institut Angeleta Ferrer y Escola
     Nova 21; fundador del Institut Angeleta Ferrer
   - **Sergi del Moral** — Director, Institut Escola Les Vinyes
   - **Pepe Menéndez** — consultor en transformación pedagógica
   - **Joan Quintana** — consultor en procesos de cambio, co-autor de
     «Educación Relacional»
   Keep the shape the page already consumes; no placeholder may survive.
2. **School highlights** — export the owner-approved *aspectos destacados* and
   levels from content-pack §5b for all seven schools (Virolai, Sadako, La
   Maquinista, Octavio Paz, Angeleta Ferrer, El Puig, Les Vinyes; El Puig and
   Les Vinyes are **infantil, primaria y ESO**), and render them on the school
   cards — this is what tells a reader why each school is worth the trip.
3. **Guard the propagation** — add a test that fails if any expert role matches
   `/experto invitado/i` or is empty, and one that every school carries at least
   one highlight. The point is that the next amendment cannot silently not-land.
4. **Downstream** — the brochure and ficha render experts too. Update A3's
   text-extraction pins if they assert a role string, and bump
   `BROCHURE_VERSION` / `FICHA_VERSION` if the rendered copy changes, since
   D-05 keys the PDF cache on them (a stale cached PDF would still show
   "Experto invitado"). Re-render the affected evidence PNGs and inspect them.

ACCEPTANCE: no placeholder role anywhere; all seven schools carry highlights +
levels on the page; the propagation guards fail when reverted (show it); the
r1 e2e spec still passes plus assertions for a real title and a highlight;
gates + leak script green.
RULES: SOP + repo CLAUDE.md; **facts only from Appendix A-6 / pack §5b — if
something is missing there, FINDINGS, never invent**; max 3 red iterations then
BLOCKED. Round-r2 section in `docs/planning/reviews/fase-a6a-review-request.md`.
WHEN DONE: LEDGER entry, push, EXECUTOR REPORT per SOP §2.3.
