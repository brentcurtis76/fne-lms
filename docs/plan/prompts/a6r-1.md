# EXECUTOR PROMPT — INSPIRA A6r r1 (port the delivered design; wire it to the module)

Branch **`phase/a6r-design`** off current `main`. Own worktree; copy `.env.local`
**and confirm it contains `NEXT_PUBLIC_BASE_URL=http://localhost:3000`** — without
it `/pasantias` 500s and six specs go red for no real reason.

Working checkout is **`~/dev/fne-lms`**. `~/Documents/fne-lms-working` is dead and
stale — do not use it.

## What this round is

The redesign was done externally and delivered. **You are porting it, not designing.**

Everything is committed under **`docs/plan/design/a6r-handoff/`**:

- `pasantias.html` — the full page, 590 lines, self-contained
- `tokens/*.css` — the design system: colours, typography, spacing, surfaces, fonts, motion
- `assets/*.png` — FNE logo lockups (horizontal gold, vertical white, symbol, lineal)
- `design-system-readme.md`

PM verification of the delivered file, so you know what you're starting from:
exactly one `h1`; heading sequence never skips a level; **no price token anywhere**;
all seven token names used (`brand_primary`, `brand_accent`, `brand_accent_text`, …);
content complete — 13/13 objectives, 7/7 schools, 8/8 experts, 6/6 includes, 6/6
excludes, 6 FAQ questions. Caps are done with `text-transform` (52 rules) over
sentence-case markup, which is correct — **keep it that way**, don't hardcode caps.

## 1 — Wire every fact to the module (blocking, and the whole point)

The delivered HTML has **all content hardcoded**, which is correct for a mockup and
unacceptable in the page. Port it so **every cohort fact comes from
`lib/pasantias/cohort-public.ts`**: dates, week structure, day structure, both school
tiers with levels and ordered highlights, all eight experts with role/school/host note,
the 13 objectives, includes, excludes, claims.

Add a test that greps the built page source for hardcoded cohort strings and fails.
This is criterion [A1] and the reason this phase exists as a phase: seven rounds of
A6a guards exist because approved facts drifted out of that module, and a redesign
that inlines copy looks identical on day one and rots the moment a fact changes.

If a section needs data the module doesn't expose, **add an export and pin it** — do
not type the text into the page.

**FAQ answers are the exception you must check.** The brief gave Design the six FAQ
*topics* but not the answers, so it wrote its own. **Use the answers from the current
`pages/pasantias.tsx`, not the mockup's** — those were owner-reviewed. If the mockup's
reads better, that's a finding for the owner, not a change you make.

## 2 — The design system

Bring `tokens/*.css` in rather than translating it into Tailwind by hand — they're CSS
custom properties and they'll survive contact with the existing build. Reconcile them
with `tailwind.config.js`, which already carries the same palette under the same names;
where they agree, keep one source. The gold gradient (#FDB833 → #B47410, 135°) is new
and should land in the config.

Mont is **already in `public/fonts/`** with `@font-face` in globals.css — do not add
the handoff's font files. Note the known trap: a global rule forces the system font on
inputs and selects, so `font-mont` won't apply to form controls without dealing with it.

Logo assets go to `public/images/` following the existing convention.

## 3 — Photography (expect gaps, and don't paper over them)

The design references four full-bleed slots and eight headshots:

```
/images/pasantias/hero-barcelona.jpg      /images/pasantias/escuela-interior.jpg
/images/pasantias/barcelona-calle.jpg     /images/pasantias/barcelona-tarde.jpg
/images/pasantias/equipo/<slug>.jpg       (×8)
```

What exists today: **`bcn-skyline.jpg`** (2400×1350, on-brand, use it as the hero),
`pasantes-virolai.jpg` and `educadores-biblioteca.jpg` (both authentic, both usable as
section breaks). Three more sit in `reference/` and are **thumbnails that must not
ship** — read that folder's README.

So: wire what exists, and for the rest **degrade honestly**. A missing photo must not
render as a broken image or an empty black band — use the brand's black surface with
the section still legible, so the page reads as designed-with-fewer-photos rather than
broken. Say in your report exactly which slots are unfilled.

Headshots exist somewhere in the repo already (A6a's team section referenced them) —
find them before assuming they don't.

**Also required:** `pages/pasantias.tsx:395` currently ships `/barcelona-innovation.jpg`,
which is generic stock of identifiable children and is not Barcelona. **It must not
survive this round** — the brand manual prohibits generic stock and identifiable minors
need image-use authorisation on file.

## 4 — The form

Design delivered a full request form with two checkboxes. **A6b owns the form's
behaviour**, not this round. Port its visual treatment, but the working action stays the
interim `info@nuevaeducacion.org` path — **do not ship a form that looks live and does
nothing.** If honouring that means rendering the panel rather than the full form for
now, do that and say so; A6b will bring the real one in using this markup.

## 5 — Guards that must stay green

- **No price token in the rendered page** (D-02, frozen); leak scan passes on a
  production build.
- No `experto invitado` placeholder; every school renders levels + ≥1 highlight.
- One `h1`, valid heading order **across the whole document** including `Footer`.
- **axe: no serious or critical violations.** The delivered design uses yellow heavily
  — verify every text-on-colour pair and state measured ratios for any you add.
- Every `data-testid` the existing e2e depends on survives, or you update the spec in
  the same commit and list exactly what changed.
- Self-contained: no CDN fonts, scripts or remote images.

## Acceptance

- Renders at **390px and 1440px**; evidence PNGs of every section at both widths under
  `docs/plan/evidence/a6r/`.
- Gates: `npm run type-check && npm run lint && npm test && npm run build &&
  node scripts/check-price-leak.mjs`, plus `CI=1 npx playwright test
  tests/e2e/pasantias-page.spec.ts tests/e2e/footer-heading-order.spec.ts
  tests/e2e/smoke.spec.ts`.

## REPORT

Standard executor report. Lead with the evidence PNGs. List every unfilled photo slot,
every `data-testid` you changed, and any place the delivered design and the module
disagreed. Append a LEDGER entry. Do not merge. Report the head SHA.
