# EXECUTOR PROMPT — INSPIRA A6r r2 (FAQ: drop one, add one)

Branch **`phase/a6r-design`**, head **`5cc01e7d`**. Own worktree; copy `.env.local`
and confirm it has `NEXT_PUBLIC_BASE_URL=http://localhost:3000`. Checkout is
`~/dev/fne-lms`.

Small, bounded round. **Scope: the FAQ array in `pages/pasantias.tsx` and its tests.**
Nothing else. r1 is verified and the design is settled — do not revisit it.

## Why

r1 correctly kept the owner-reviewed FAQ whole, which left
**"¿Qué incluye el programa?"** duplicating the new `#incluye` section that r1 added.
The owner has approved a swap.

## 1 — Remove "¿Qué incluye el programa?"

Its answer maps `COHORT_INCLUDES` and `COHORT_EXCLUDES` — exactly what the `#incluye`
section now renders, better and without a click. A FAQ that repeats a section on the
same page teaches readers the FAQ isn't worth opening.

Removing it must not orphan the module values: `COHORT_INCLUDES` and `COHORT_EXCLUDES`
are still consumed by `#incluye`, and the A1 hardcoded-cohort guard should still see
them rendered. Confirm that guard stays green rather than assuming it.

## 2 — Add "¿Necesito hablar catalán?"

A real question for a Chilean buyer — Barcelona schools teach in Catalan — and nothing
on the page answers it. Approved question text:

> **¿Necesito hablar catalán?**

Answer text is **pending the owner's confirmation of one factual claim** (that the
programme runs in Spanish). **Do not invent it and do not ship a placeholder.** The PM
will supply the final string before this round is dispatched; if it is not in this
prompt when you start, stop and ask.

<!-- FINAL ANSWER TEXT: to be pasted here by the PM once the owner confirms. -->

Everything on this page is sourced from `lib/pasantias/cohort-public.ts`, but FAQ copy
lives in the page — that is the existing pattern and correct, since these are answers
rather than cohort facts. Follow it.

## 3 — Keep the count and the order sensible

Six questions before, six after. Place the new one where a reader would meet it — after
"¿Quiénes participan en la pasantía?" is natural, since both are about fit. Use your
judgement and say what you chose.

## Guards

Unchanged and all must stay green: no price token; leak scan clean on a production
build; one `h1` and valid heading order across the whole document; axe no
serious/critical; every `data-testid` the e2e depends on survives. The A6a spec asserts
a FAQ count — check it and update it in the same commit if the number is pinned, saying
so explicitly.

## GATES

`npm run type-check && npm run lint && npm test && npm run build &&
node scripts/check-price-leak.mjs`, plus `CI=1 npx playwright test
tests/e2e/pasantias-page.spec.ts tests/e2e/footer-heading-order.spec.ts
tests/e2e/smoke.spec.ts`.

Re-render only the FAQ evidence PNGs under `docs/plan/evidence/a6r/` — the rest of the
page is untouched and its evidence stands.

## REPORT

Standard executor report. Append a LEDGER entry. Do not merge. Report the head SHA.
