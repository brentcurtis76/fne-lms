# EXECUTOR PROMPT — INSPIRA A6r r6 (Sol FAIL: two blocking, both PM-verified)

Branch **`phase/a6r-design`**, head **`0c4d87bb`**. Work in the worktree
`~/dev/wt-a6r`; read this prompt from `~/dev/fne-lms` (the `main` checkout) — the branch
predates it, so `~/dev/wt-a6r/docs/plan/prompts/` will not show it. If Playwright's
chromium is missing, `npx playwright install chromium`.

Sol's full review is committed at `docs/plan/reviews/REVIEW-A6R.md` on `main`. Read it.

**A6r [A2]–[A7] are verified satisfied at this head** — Sol re-ran every gate itself and
inspected the evidence PNGs at both widths. Do not revisit them. Both blocking findings
are about **[A1]: every fact comes from `cohort-public.ts`**.

**This round changes the page for the first time since r3.** That has consequences the
last two rounds did not: evidence captures, e2e pins, and testids.

---

## B1 — the `weeks` exception blesses a hardcoded cardinality

The guard declares `weeks` uncounted (test file `:1088`) on the reason that the number of
weeks is copy. It is a cohort fact, and the page states it at four buyer-visible sites:

```
pages/pasantias.tsx:96     PAGE_DESCRIPTION — `Dos semanas viviendo escuelas…`  (meta description + og)
pages/pasantias.tsx:429    FAQ — "Entre ambas semanas quedan …"
pages/pasantias.tsx:695    title="Dos semanas, dos modos"
pages/pasantias.tsx:1049   "…las dos semanas, las escuelas y las condiciones de participación."
```

Not buyer-visible, and out of scope unless your mechanism reaches them: the comment at
`:686` and the anchor `id="dos-semanas"` at `:688`. **If you do touch that id, check
inbound links first** — A7a rewires site navigation and a stale anchor fails silently by
scrolling to the top.

There is a structural half too: `const [immersionWeek, visitWeek] = COHORT_WEEKS` (`:388`)
means a third week would **render nothing at all**. That silent drop is worse than the
stale copy, and it is why the exception's own reason ("a third week would render
nothing") describes the defect instead of preventing it.

**Closure — and do not fake a derivation.** The design is two-week by construction: two
cards, two different treatments. A page that printed "Tres semanas" beside two cards
would be a lie of a different kind. Required:

1. The two-week assumption becomes **explicit and enforced**: adding a third week to
   `COHORT_WEEKS` must fail a **named** test rather than render silently.
2. Every buyer-visible week-count site either reads `COHORT_WEEKS.length` or is provably
   covered by that invariant, such that **pinning any one site to the old count fails
   while the others stay wired** — the negative control Sol asked for.
3. The `weeks` exception is removed, or its reason is replaced by one that describes an
   enforced invariant instead of describing the hardcoding.

Copy constraint: **"Dos semanas, dos modos" is the delivered design's heading and stays
as delivered** if the invariant route keeps it honest. If you derive a number-word
instead, the rendered copy at n=2 must be byte-identical to today's.

Scope: `lib/pasantias/cohort-public.ts` is **out of scope for A6r**. Read `.length`; do
not add module fields. If you conclude the only correct fix needs a module change, STOP
and report FINDINGS — that is the right outcome, not a failure.

## B2 — a partial restatement passes both layers

Two instances are on the page today. I verified both at source:

- `:430` restates **"Fiesta Nacional de España"** from `COHORT_FREE_DAYS[2].label`
  (`'Lunes libre — Fiesta Nacional de España, colegios cerrados'`). The full label renders
  at `:740`, so mutating the leaf moves *that* site and the leaf reads as wired.
- `:864` restates **"El orden de las visitas puede variar"** verbatim from
  `COHORT_WEEKS[1].summary`, which renders in full directly above at `:757`.

The source scan looks only for the complete leaf (`:251-260`); the render proof requires
only the complete old leaf to lose an occurrence (`:576-588`). Neither can see a fragment.
This is the same multi-site shape r5 fixed for counts, on strings.

### The copy fixes are owner decisions, already taken. Implement exactly; invent nothing.

- **FAQ "¿Qué pasa el fin de semana largo?" — drop the holiday clause.** Target:
  *"Entre ambas semanas quedan {COHORT_FREE_DAYS.length} días libres, del {primero} al
  {último}: tiempo para descansar, recorrer Barcelona o conocer Europa."* Dates from the
  existing `freeDayDates`, not typed. The holiday fact keeps its home in the finde card
  (`:738-740`), and the e2e pin on `pasantias-finde`
  (`tests/e2e/pasantias-page.spec.ts:132`) must stay green — verify it, do not assume it.
- **The italic visit-order note at `:862-865` — delete it.** The week-2 summary above
  already carries the fact. "sujeto a la disponibilidad de las escuelas" is stated by no
  approved source and goes with it.

If either wording reads wrong in Chilean Spanish, **say so in the report and propose** —
do not improvise silently. Inventing copy is the specific failure this project has paid
for three times.

### The guard extension

A partial restatement at one site must fail **even when another site renders the fully
wired leaf**. Sol is explicit: do not solve this with another hand-maintained phrase list.

The shape that fits what r5 already learned: after mutating a string leaf, require that
**no substantial fragment of the pre-mutation value survives** on the page — the string
analogue of `printsStaleSize`'s "the old size must be gone". Whatever fragment floor you
choose must be **derived and stated**, asserted the way `MIN_SCANNED_LENGTH` is, not
tuned until green.

Expect it to surface more than Sol's two. Everything it finds is either **fixed** or
**declared with a reason you checked against the code**. If a legitimate overlap fails —
module strings genuinely share "Barcelona", "escuelas" — that is a classification
problem: declare it honestly. **Do not raise the floor until the noise disappears**; that
is how the hand-maintained list got its holes.

## S1 — fold it in

`printsStaleSize` (`:932`) returns `false` below length two, silently falling back to the
counting half that is known to miss a literal at one of several sites. Assert that every
counted collection has ≥2 elements, so the fallback cannot open silently as the module
evolves. One assertion; Sol asked for it and the PM raised it.

## Out of scope this round

The move of the generic walker/mutation/classification machinery to
`__tests__/support/cohort-contract.ts`. Sol's words: *"mixing the move into the
correctness round would make the next review harder."* It is backlogged and it is not
yours today.

## Evidence

The page changes, so A6r [A4] applies again: 390 px and 1280 px captures under
`docs/plan/evidence/a6r/`. Re-render the ones the copy changes affect. **The in-app
browser pane returns blank frames on this machine** — drive Playwright and shoot the
built page.

## GATES

```
npm run type-check && npm run lint && npm test && npm run build && node scripts/check-price-leak.mjs
```

```
CI=1 npx playwright test tests/e2e/pasantias-page.spec.ts tests/e2e/footer-heading-order.spec.ts tests/e2e/smoke.spec.ts
```

If any `data-testid` moves, A6r [A3] requires the spec updated in the same commit with
the change called out explicitly.

## REPORT

Standard executor report. Lead with:

- the **negative control for each blocking finding** — the mutation, and the named test
  that now fails on it;
- the **exact before/after of every rendered sentence you changed**, so the owner can
  read the copy without reading a diff;
- **everything the fragment rule surfaced beyond Sol's two** — each one fixed or
  declared, with the reason checked against the code.

Append your LEDGER entry to the branch's `docs/plan/LEDGER.md`. Push
`phase/a6r-design`. Do not merge. Report the head SHA.
