# EXECUTOR PROMPT — INSPIRA A6r r7 (Sol's second FAIL: one blocking, one should-fix)

Branch **`phase/a6r-design`**, head **`100ec120`** — Sol committed its review on the
branch and **did not push**, so the branch is one commit ahead of `origin`. Work in
`~/dev/wt-a6r`; read this prompt from `~/dev/fne-lms` (the `main` checkout).

Sol's re-review: `docs/plan/reviews/REVIEW-A6R-R2.md`, on the branch. Read it.

**B2 and the former S1 are closed and confirmed by Sol. A6r [A2]–[A7] remain satisfied.
Do not revisit any of them.** One blocking finding remains, and it is the last known
instance of the [A1] class.

---

## B1 — `Entre ambas semanas` is a cardinality literal nothing can see

`pages/pasantias.tsx:488`. The long-weekend FAQ opens *"Entre ambas semanas quedan
{n} días libres…"*. **"ambas" asserts exactly two weeks.**

r6 declared this site covered by the `DESIGNED_WEEK_COUNT` invariant rather than derived.
That declaration is wrong, and both Sol and the PM verified why:

- Nothing in the suite references `ambas`. `COUNT_WORDS_ES`
  (`__tests__/pages/pasantias-hardcoded-cohort.test.ts:1055`) holds only cardinal number
  words, and the "at every site" proof (`:1728-1747`) only requires `dos` to disappear and
  `tres` to appear. **`ambas` survives a three-week surface untouched.**
- The exact-two invariant is real and it is not the answer here. It stops a *module-only*
  third week, which is good. But a deliberate three-card redesign updates that invariant
  **by design** — that is what it is for — and walks away leaving the FAQ stale. The
  invariant forces attention; it does not make this fact come from the module.
- The `weeks` exception's rewritten reason names this FAQ as one of four sites and says
  the separate test proves every one of them. It does not prove this one. That reason is
  now inaccurate in the same way the r4 `tier` reasons were, and this phase's most
  expensive lesson is that **a wrong reason recorded as fact is worse than an admitted
  gap.**

### The copy fix — owner decision, already taken. Implement exactly; invent nothing.

Make the sentence **count-neutral**, anchored on the long weekend it answers:

> **El fin de semana largo suma {COHORT_FREE_DAYS.length} días libres, del {primero} al
> {último}: tiempo para descansar, recorrer Barcelona o conocer Europa.**

No cardinality of weeks, so there is no grammar to derive and nothing left to go stale.
The free-day count stays derived. The dates stay derived — see the second owner decision
below for their format.

### The guard work

1. **A negative control**, as Sol requires: restore the real `Entre ambas semanas` phrase
   and prove a three-week surface fails. That means the mechanism has to be able to see a
   cardinality asserted as a word other than a number.
   - **Constraint, and read it carefully.** Sol's standing instruction is no
     hand-maintained *phrase* list, and this phase has been burned three times by
     hand-enumerated guards. A small, **declared and reasoned set of es-CL words that
     assert a cardinality** — `ambas`/`ambos` assert two, and the set is closed
     linguistically rather than open-endedly per-page — is acceptable **if** it is
     justified in the file the way `MIN_FRAGMENT_TOKENS` is, and asserted rather than
     assumed. A list of sentences from this page is not acceptable. If you find a
     derivation that beats the declared set, take it and say why.
2. **Rewrite the `weeks` exception reason** so it describes what the two mechanisms
   actually prove, now that the fourth site no longer states a cardinality at all. Sol
   asked for this explicitly. Re-read it against the code before committing it; do not
   carry forward a sentence that was true of r6 and is not true of r7.
3. Re-check the other three derived sites still pass their own control.

## Second owner decision — fold in the repeated month

The sentence currently prints *"del 10 de octubre al 12 de octubre"*. Add a **range
formatter that drops the first month when both dates share one** → *"del 10 al 12 de
octubre"*. Apply it to the finde card at `pages/pasantias.tsx:735` too, which shares the
pattern — that keeps the two surfaces consistent, which is the reason the repetition was
tolerable until now. Both dates stay derived from `freeDayDates`; nothing becomes a
literal.

If the formatter would change any other rendered date surface, say so and stop at the two
named here.

## S1 — the review request, corrected in this round

`docs/planning/reviews/fase-a6r-review-request.md:5-7` says **11 commits and four ledger
entries**. It is **10 and three** — r5's and r6's ledger entries are folded into their
code commits. Sol asked for it in this remediation round. Recompute against the branch as
it stands when you finish, rather than copying these numbers: your own commit changes
them.

## Evidence

The page changes again — the FAQ answer and the finde card's date range. A6r [A4]:
re-render the affected captures at 390 and 1280 under `docs/plan/evidence/a6r/`, and say
in the README which ones moved and which did not, as r6 did. The in-app browser pane
returns blank frames on this machine — drive Playwright.

## GATES

```
npm run type-check && npm run lint && npm test && npm run build && node scripts/check-price-leak.mjs
```

```
CI=1 npx playwright test tests/e2e/pasantias-page.spec.ts tests/e2e/footer-heading-order.spec.ts tests/e2e/smoke.spec.ts
```

A stale `next-server` can hold port 3000 from an earlier Playwright run and fail the e2e
gate with "port already in use". That is environment, not a regression — clear it and
re-run.

## REPORT

Standard executor report. Lead with:

- the **negative control**: `Entre ambas semanas` restored, the three-week surface, and
  the named test that fails on it;
- the **exact before/after** of both rendered sentences;
- the `weeks` exception reason as you rewrote it, and what you checked it against.

Append your LEDGER entry to the branch's `docs/plan/LEDGER.md`. **Push the branch** — it
is currently one commit ahead of `origin` because Sol did not push its review. Do not
merge. Report the head SHA.
