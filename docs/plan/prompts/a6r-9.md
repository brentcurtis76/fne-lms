# EXECUTOR PROMPT — INSPIRA A6r r9 (Sol's third FAIL: the short-string floor)

Branch **`phase/a6r-design`**, head **`159418eb`** — Sol committed its review on the
branch and did not push, so the branch is one commit ahead of `origin`. Work in
`~/dev/wt-a6r`; read this prompt from `~/dev/fne-lms` (the `main` checkout).

Sol's review: `docs/plan/reviews/REVIEW-A6R-R3.md`, on the branch.

**r7 and r8 are confirmed closed by Sol, and [A2]–[A7] remain satisfied. Do not revisit
any of it.** One blocking finding, and it is a hole this project — the PM included —
signed off on twice.

---

## B1 — the 12-character floor leaves a live cohort fact unprotected

`__tests__/pages/pasantias-hardcoded-cohort.test.ts:1191-1199` (`isRestatable`) exempts
every non-date string shorter than `MIN_SCANNED_LENGTH`. `COHORT_LODGING_AREA` is
`'Barcelona'` — nine characters — and the page prints it as the lodging area at
`pages/pasantias.tsx:508` and `:1087`.

Sol replaced **one** of those live occurrences with the literal `Barcelona`, left the
others wired, and ran the whole guard: **41/41 green.** The PM reproduced the same before
this round. No layer rejects it: the source scan and the fragment rule share the floor,
and `provesRendered` only asks the old value to become rarer.

**Why the PM's earlier "accepted at r1" defence does not hold, in Sol's words:** the
tradeoff *"was accepted while the render contract was believed to provide complementary
coverage, and r8 now correctly documents that this coverage is insufficient for multi-site
facts."* That is right. The floor was signed off on a premise r8 disproved. It is not a
standing decision any more.

### Required closure

1. **Apply the quiet-module restatement check to short printable strings too** — or a
   mechanism of equal strength. Dates are already exempt from the floor and numbers are
   already always checked; this is about short strings.
2. **Every legitimate overlap gets an exact `path` / `form` / `sites` declaration with a
   reason you checked against the page line.** The count is what makes it falsifiable:
   gaining a site must fail, exactly as the `9` in `WHATSAPP_DISPLAY` does today.
3. **A negative control that hardcodes exactly one real `COHORT_LODGING_AREA` site** while
   the others stay wired, and requires the guard to fail by name.

**Two closures Sol forbids outright:** a path-level exemption for `lodgingArea`, and
silently moving the length floor. Do not do either.

### What you are walking into, so you can size it before you start

`Barcelona` appears 14 times in `pages/pasantias.tsx`, and **most are not the lodging
area** — they are the product's own name, "Pasantías INSPIRA Barcelona"
(`:127`, `:133`, `:135`, `:139`), the meta description (`:152`), the FAQ's "recorrer
Barcelona" (`:518`), the section eyebrow "Por qué Barcelona" (`:755`), and image alt text
(`:568`, `:664`). Textually identical to the module's value, semantically a different
thing, and no mechanism can tell them apart — which is exactly why they have to be
declared with counts rather than guessed at.

Check `'visita'` and `'inmersion'` (the `tier` enum) the same way before you assume
`lodgingArea` is the only short value that surfaces.

### The honest-cost clause, and it is not optional

This makes the guard sensitive to ordinary copy edits: a future sentence that says
"Barcelona" moves a declared count and fails the suite until someone updates it. That may
be an acceptable price for closing the hole, and it may not.

**Report the real number** — how many declarations the closure needs, and which of them a
routine copy edit would disturb. Do not tune the mechanism until the number looks small;
report it. If the only closures you can find are the two Sol forbade, **stop and report
STATUS: FINDINGS** with what you tried and what you would propose instead. That is the
correct outcome, not a failure, and it goes to the owner rather than to another round.

## Scope

- `__tests__/pages/pasantias-hardcoded-cohort.test.ts`, and `pages/pasantias.tsx` only if
  the stricter rule exposes a genuine restatement there.
- `lib/pasantias/cohort-public.ts` stays out of scope (A6r's own boundary).
- Do not restructure the guard into `__tests__/support/cohort-contract.ts`. It is now
  **2397 lines against a 1235-line page** and that extraction is still deferred, with
  Sol's explicit agreement that it must not ride inside a correctness round.
- The page has not changed since r7 and should not change here unless point 1 above
  forces it. If it stays unchanged, say so and re-render no evidence.

## GATES

```
npm run type-check && npm run lint && npm test && npm run build && node scripts/check-price-leak.mjs
```

```
CI=1 npx playwright test tests/e2e/pasantias-page.spec.ts tests/e2e/footer-heading-order.spec.ts tests/e2e/smoke.spec.ts
```

A stale `next-server` on port 3000 from an earlier Playwright run fails the e2e gate with
"port already in use" — environment, not a regression. Clear it and re-run.

## REPORT

Standard executor report. Lead with:

- the **negative control**: one live `COHORT_LODGING_AREA` site hardcoded, the others
  wired, and the named test that fails;
- **every declaration the closure required**, with the page line each reason was checked
  against, and **which of them a routine copy edit would disturb**;
- whether any *correct* construct now fails, and how you told it apart from a restatement.

Append your LEDGER entry to the branch's `docs/plan/LEDGER.md`. **Push the branch** — it
is one commit ahead of `origin` because Sol did not push its review. Do not merge. Report
the head SHA.
