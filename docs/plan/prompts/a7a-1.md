SESSION: INSPIRA · A7a · r1 · EXEC

You are the executor for **INSPIRA Comms (fne-lms)**, phase **A7a**, round **r1**.
You have no prior context. Everything you need is below or in the repo.

---

## BRANCH AND WORKTREE

**Binding process rule (Decision Log 2026-07-31): run in your own dedicated `git worktree`,
never in the shared `~/dev/fne-lms` checkout.** A concurrent session has been committing
into that checkout all week; two rounds have already lost or misplaced commits to it.

```bash
cd ~/dev/fne-lms
git worktree add ~/dev/wt-a7a -b phase/a7a-links main
cd ~/dev/wt-a7a
```

Base is **local `main`** (not `origin/main`): `origin/main` is at `6824ca0d`, and local `main`
carries one docs commit on top of it — this prompt and its PLAN amendment. Branch from local
`main` so the prompt exists in your worktree. Code-wise the two are identical: A6b is merged,
`/pasantias` with its lead form is live.
Commit as you go. **Never merge to `main`.**

---

## WHAT THIS PHASE IS

`/pasantias` exists, is finished, and is **orphaned** — nothing on the site links to it.
Every "PASANTÍAS" nav and footer link on the marketing site still points at `#pasantias`,
a teaser section on the homepage, and the INSPIRA program is still "explained" by a
Heyzine flipbook of a **retired Abril 2026 brochure** with the wrong dates and the retired
€1.000 price. This round makes the real page the destination and takes the stale flipbooks
off the site.

It also takes two one-line asset swaps on `pages/index.tsx`, approved by the owner on
2026-08-05, because this phase already opens that file.

This is a link/asset round. There is **no new business logic**, and none is wanted.

---

## CONTEXT YOU NEED — the exact inventory, already taken

I greped this myself on `origin/main`. Do not go hunting; verify these lines exist and act
on them.

### 1. The 15 nav/footer links (7 files)

| File | Lines | Current href | Kind |
|---|---|---|---|
| `pages/index.tsx` | 189, 225 | `#pasantias` (`<a>`) | desktop nav, mobile nav |
| `pages/programas.tsx` | 254, 290 | `/#pasantias` (`<a>`) | desktop nav, mobile nav |
| `pages/nosotros.tsx` | 352, 388 | `/#pasantias` (`<Link>`) | desktop nav, mobile nav |
| `pages/noticias.tsx` | 450, 496 | `/#pasantias` (`<Link>`, `<a>`) | desktop nav, mobile nav |
| `pages/noticias/[slug].tsx` | 286, 332 | `/#pasantias` (`<Link>`, `<a>`) | desktop nav, mobile nav |
| `pages/equipo.tsx` | 281, 317 | `/#pasantias` (`<Link>`) | desktop nav, mobile nav |
| `components/Footer.tsx` | 91 | `/#pasantias` (`<Link>`) | footer link, label "Pasantías" |

**`pages/equipo.tsx` and `pages/noticias/[slug].tsx` were missing from the plan's scope
list — I added them by amendment before writing this prompt.** They are in scope.

All 15 become `/pasantias`. Use `next/link` (`<Link href="/pasantias">`) where the file
already imports `Link`; keep a plain `<a>` where the file uses `<a>` and does not import
`Link` — **do not add imports or convert element types beyond what the change needs.**
Visible label text is unchanged everywhere ("PASANTÍAS", footer "Pasantías").

**`pages/index.tsx:277` keeps `id="pasantias"` on its section.** The id is a preserved
criterion — external links and bookmarks point at it. Only the two nav `href`s change.

### 2. The four Heyzine flipbooks — two go, two stay

| Heyzine ID | What it is | Fate |
|---|---|---|
| `fef3878d3c` | INSPIRA brochure (Abril 2026) — iframe, index + programas | **REMOVE** |
| `fb8cf2cfb1` | INSPIRA brochure — "abrir en nueva pestaña" link, index only | **REMOVE** |
| `d87d80f309` | **Directivos** brochure — iframe, index + programas | **KEEP** |
| `92bf9eb5ee` | **Directivos** — "abrir en nueva pestaña" link, index only | **KEEP** |

Sites:

- `pages/index.tsx`
  - `:14` `const [showFlipbook, setShowFlipbook] = React.useState(false);` → **delete**
  - `:15` `showDirectivesFlipbook` state → **keep**
  - `:399` INSPIRA CTA `<button onClick={() => setShowFlipbook(true)}>` labelled
    "Programa para líderes pedagógicos" → **becomes a link to `/pasantias`** (see below)
  - `:408` Directivos CTA button → **keep untouched**
  - `:1073–1120` INSPIRA modal block (`{showFlipbook && (…)}`), including the `<h3>`
    "Programa de Pasantías Barcelona Abril 2026" → **delete the whole block**
  - `:1123–1170` Directivos modal → **keep**, except its `<h3>` at `:1128`
    `Programa Estratégico Para Directivos - Abril 2026` → drop the date:
    **`Programa Estratégico Para Directivos`** (drop the ` - Abril 2026`, nothing else)
- `pages/programas.tsx`
  - `:152` `showFlipbook` state → **delete**; `:153` `showDirectivesFlipbook` → **keep**
  - `:534` INSPIRA CTA button → **becomes a link to `/pasantias`**
  - `:543` Directivos CTA button → **keep untouched**
  - `:652–676` INSPIRA modal block → **delete**
  - `:678–706` Directivos modal → **keep as-is** (its markup carries no date)

**The replacement CTA copy is fixed — do not invent your own and do not add a date:**

> **`Conoce las Pasantías en Barcelona`**

Render it as a link to `/pasantias`, styled **exactly like the button it replaces** (same
classes, same trailing icon svg) so the two CTAs still read as a pair. `data-testid`:
`inspira-pasantias-cta` on both pages. No cohort facts (dates, day counts, school counts,
prices) in this copy or anywhere else you touch — the homepage's only cohort fact is
`{COHORT_HEADLINE}` at `pages/index.tsx:306`, read from the module, and it stays that way.

### 3. The two asset swaps in `pages/index.tsx` (owner-approved 2026-08-05)

- **`:374`** `src="/barcelona-innovation.jpg"` → **`/images/pasantias/educadores-biblioteca.jpg`**
  The current file is generic stock of **identifiable children** and is not Barcelona: the
  brand manual prohibits generic stock, and identifiable minors need image-use authorisation
  on file (Ley 21.719). A6r removed it from `/pasantias`; this is its last live use in the app.
  **The alt text must change with the picture** — it currently says "Innovación Educativa",
  which no longer describes the image. Use: `alt="Educadores en la biblioteca de una escuela en Barcelona"`.
- **`:382`** `src="/barcelona-skyline.jpg"` → **`/images/pasantias/bcn-skyline.jpg`**
  Same photograph, 9.4 MB / 7551×5034 → 1.5 MB / 2400×1350. ~8 MB off a homepage whose
  stated audience is older school hardware. Leave its `alt` as-is — same image.

Both target files exist at those paths on `main`. Verify with `ls` before you edit.

**DELETE NEITHER ORIGINAL — I already checked the references the plan told you to check:**
`public/barcelona-skyline.jpg` is still used by `components/quotes/QuotePublicView.tsx:902`
and `public/public-website-fne.html`; `public/barcelona-innovation.jpg` is still used by
`public/public-website-fne.html`. Both files stay on disk. Retiring them is not this round's
work.

### 4. `public/public-website-fne.html` is out of scope

It is a legacy static snapshot of the old marketing site sitting in `public/`. It contains
`#pasantias` anchors and both old images. **Do not touch it**, and scope your grep proofs to
`pages/` and `components/` so it does not pollute them.

---

## SCOPE

1. All 15 nav/footer links → `/pasantias`.
2. Both INSPIRA flipbooks (modal + state + trigger) removed from `pages/index.tsx` and
   `pages/programas.tsx`, each trigger replaced by the fixed-copy link to `/pasantias`.
3. Directivos flipbooks kept; `Abril 2026` dropped from the one title that carries it.
4. The two `pages/index.tsx` image swaps.
5. The two guards in TESTS below.

## EXPLICITLY OUT OF SCOPE — do not touch, do not "improve while you're in there"

- `pages/api/contact.ts` and the homepage contact form (its fields, its
  `<option value="inspira">`, its consent) — A7b and backlog.
- The homepage `#pasantias` **section content** itself (heading, copy, stats, badge,
  benefit list). Only its two CTA buttons and two images change.
- `pages/pasantias.tsx`, `components/pasantias/**`, `lib/pasantias/**` — finished and
  frozen by A6a/A6r/A6b. If you believe one needs a change, that is a FINDING, not an edit.
- Extracting the duplicated nav/header into a shared component. Five pages inline their own
  nav; that refactor is real, it is tempting because you are in all of them, and it is **not
  authorized here** — it would put a cross-site structural change inside a link round where
  no reviewer is looking for it. Note it under NOT DONE if you want it recorded.
- `public/public-website-fne.html`; deleting either original image; `QuotePublicView.tsx`.
- The `cdn.tailwindcss.com` script tag on the homepage (known backlog item).

---

## ACCEPTANCE CRITERIA — each independently verifiable

- **[A1]** `grep -rn '#pasantias' pages/ components/` returns **exactly one** hit:
  `pages/index.tsx` `id="pasantias"` on the section. No `href` anywhere in `pages/` or
  `components/` targets `#pasantias` or `/#pasantias`.
- **[A2]** `grep -rn 'heyzine' pages/ components/` returns **only** `d87d80f309` and
  `92bf9eb5ee` (Directivos). Neither `fef3878d3c` nor `fb8cf2cfb1` appears anywhere in
  `pages/` or `components/`.
- **[A3]** `showFlipbook`/`setShowFlipbook` no longer exist in either file; the Directivos
  state, buttons and modals are byte-identical to `main` except the one `<h3>` title.
- **[A4]** `grep -rn 'Abril 2026' pages/ components/` returns nothing.
- **[A5]** Both INSPIRA CTAs are links to `/pasantias` with `data-testid="inspira-pasantias-cta"`
  and the exact copy `Conoce las Pasantías en Barcelona`.
- **[A6]** `pages/index.tsx` references `/images/pasantias/educadores-biblioteca.jpg` (with the
  new alt) and `/images/pasantias/bcn-skyline.jpg`, and no longer references
  `/barcelona-innovation.jpg` or `/barcelona-skyline.jpg`. Both original files still exist in
  `public/`.
- **[A7]** All gates green + `node scripts/check-price-leak.mjs` OK.

---

## TESTS YOU MUST WRITE

Greps in a report evaporate the moment someone edits a file. Both proofs below must be
executable and must live in the suite.

### 1. `__tests__/pages/pasantias-site-links.test.ts` (new, Vitest)

A source-scanning guard over `pages/**/*.tsx` + `components/Footer.tsx`, in the style of the
repo's existing derived guards (`__tests__/pages/pasantias-hardcoded-cohort.test.ts` and
`scripts/check-price-leak.mjs` are the patterns to match — read one before you start).
Derive the file list by walking the directory; **do not hand-list the seven files**, because
a page added later must be covered without anyone remembering this test exists.

It must assert:

- no `href` in scope resolves to `#pasantias` or `/#pasantias` — and it must be a real
  scan of `href="…"` occurrences, not a bare `source.includes('#pasantias')` that the
  section `id` would defeat;
- `pages/index.tsx` still carries `id="pasantias"` (the preservation criterion — a future
  cleanup that deletes the anchor fails here deliberately);
- the two INSPIRA Heyzine IDs appear nowhere in scope;
- the two Directivos Heyzine IDs **still appear** (so a later flipbook purge is a deliberate
  act, not a silent one);
- `Abril 2026` appears nowhere in scope;
- `pages/index.tsx` references neither `/barcelona-innovation.jpg` nor `/barcelona-skyline.jpg`.

Each assertion must fail with a message naming the offending **file and line**, not just
"expected false to be true".

### 2. Nav assertions appended to `tests/e2e/pasantias-page.spec.ts` (existing, Playwright)

Put them in the existing spec — it is already on `MANDATORY_SPECS` in
`scripts/ci/e2e-mandatory.mjs`, so it runs in CI. A new spec file would not run there
unless you also edited that list; **do not create a new spec.**

- From `/`, click the desktop nav "PASANTÍAS" → URL is `/pasantias` and the page's `h1`
  renders. Do not assert on the anchor: assert on the landing.
- From `/`, the footer "Pasantías" link → `/pasantias`.
- From `/programas`, the nav "PASANTÍAS" → `/pasantias`.
- The INSPIRA CTA (`[data-testid="inspira-pasantias-cta"]`) on `/` → `/pasantias`, and
  clicking it opens **no modal and no iframe** (`page.locator('iframe.fp-iframe')` count 0
  after the click on the homepage before navigation, or equivalent — the point is that the
  flipbook is gone, not merely unlinked).
- Web-first assertions only. **Never `waitForTimeout`.**

## TEST COMMAND (verbatim, all of it)

```bash
npx vitest run __tests__/pages/pasantias-site-links.test.ts && \
npm run type-check && npm run lint && npm test && npm run build && \
node scripts/check-price-leak.mjs && \
CI=1 npx playwright test tests/e2e/pasantias-page.spec.ts tests/e2e/pasantias-form.spec.ts tests/e2e/footer-heading-order.spec.ts
```

`npm run test:db` is **not** run — this round contains zero SQL.
The full `MANDATORY_SPECS` list cannot run on this machine (it needs CI's seeded ephemeral
Supabase stack). The three specs above are this phase's local standard; say so in your report
rather than implying you ran the list.

---

## RULES

- Surgical changes only. No bulk edits, no reformatting, no import reshuffling. Match the
  patterns already in each file — these are five hand-written pages with inconsistent
  conventions, and consistency between them is **not** this round's job.
- All user-facing copy is Chilean Spanish (es-CL). New interactive elements get `data-testid`.
- Do not grade your own work in prose. Show the diff and the raw test output.
- If tests are red, iterate up to 3 times. If still red, report `STATUS: BLOCKED` with the
  real error. Never report a red gate as complete.
- If you find the plan is wrong — a link that cannot go to `/pasantias`, a flipbook that
  turns out to be load-bearing, an image reference I missed — **STOP CODING** and report
  `STATUS: FINDINGS`. That is the correct outcome, not a failure. Four of this project's
  phases were saved by an executor refusing to work around a bad prompt.
- Do not expand scope. Out-of-scope breakage goes under NOT DONE.
- Write `docs/planning/reviews/fase-a7a-review-request.md` per CLAUDE.md executor rule 6
  before reporting complete.
- If the shared checkout's untracked/foreign files appear in your worktree, leave them
  strictly alone and never `git add -A` blindly.

---

## WHEN DONE

Append your round entry to `docs/plan/LEDGER.md` (append only — never reflow or reorder that
file; another workstream appends to it too), then output exactly:

```
## EXECUTOR REPORT — A7a round r1
STATUS: COMPLETE | BLOCKED | FINDINGS
BRANCH / HEAD SHA:
WHAT I DID:
FILES CHANGED (path, +/-):
CRITERIA: [A1]…[A7], each with the evidence that satisfies it
TESTS: command + raw output (counts, not adjectives)
GATES: type-check / lint / test / build / price-leak / playwright — each with its result
DECISIONS I MADE THAT THE PROMPT DID NOT SPECIFY:
NOT DONE / DEFERRED:
FINDINGS (things the plan or prompt got wrong):
```
