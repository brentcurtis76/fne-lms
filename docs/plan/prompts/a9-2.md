SESSION: INSPIRA · A9 · r2 · EXEC

You are the executor for INSPIRA Comms (repo `fne-lms`), phase **A9 — Track A release
verification**, round **r2**. You have no prior context. Everything you need is below.

Read `CLAUDE.md` at the repo root before you start. Its hard rules outrank this prompt.

**This is a documentation-accuracy round. You will not touch a single line of code or test.**

---

## BRANCH

The work is already on `phase/a9-verify`, worktree **`~/dev/wt-a9`**, head `fb8066e8`.

```bash
cd ~/dev/wt-a9
git status            # must be clean before you start
git log --oneline -1  # expect fb8066e8
```

Do not create a branch and do not cut a new worktree — r1's work is here. Commit on this branch
and push it. **Do not merge, do not open or close a PR, do not run any `vercel` command.**
[PR #46](https://github.com/brentcurtis76/fne-lms/pull/46) is already open; your push updates it.

---

## WHY THIS ROUND EXISTS

Codex Sol reviewed A9 and returned **FAIL with one BLOCKING item**. Its reasoning, which the PM
accepts in full:

> **[B1]** The committed checklist remains stale after CI succeeded. `[A2]` permits only `PASS`,
> `FAIL`, or `OWNER-RUN — PENDING`, but five rows still say `PENDING CI`. […] Update these rows to
> `PASS` with committed run evidence; **A9's principal deliverable is accurate evidence, so this
> contract violation blocks approval.**

That is exactly right, and it overturns a PM call. At r1 the PM deferred flipping those rows to a
single closing pass alongside the owner-run results, reasoning that a remediation round to edit a
markdown table was disproportionate. Sol's counter is stronger: **A9's product is the evidence
file.** A phase whose deliverable is a record of what was verified cannot ship a record that
contradicts what was verified. The deferral was wrong.

Sol also raised two NITs that are the *same defect class* as B1 — factual errors in A9's evidence
artifacts — so they are fixed in this round rather than logged. The PM found three more stale
statements Sol did not cite. All of them are below.

---

## THE FACTS YOU WILL RECORD — use these exact values, invent nothing

Every number here was read from the CI runs and the git history by the PM. **Do not re-derive
them from memory and do not round them.** If something below disagrees with what you observe,
**stop and report `STATUS: FINDINGS`** — a disagreement means one of us is wrong and guessing
would put a third wrong number into the record.

### The code-bearing CI run — this is the one the evidence must name

**Run `31276283612`**, head **`b0cc9728`**, PR #46. **All six gates green:**

| Gate | Result |
|---|---|
| Gate 1 — Typecheck | pass, 1m55s |
| Gate 1b — Lint | pass, 1m16s |
| Gate 2 — Unit (Vitest) | pass, 1m53s — **291 files / 6781 tests passed** |
| Gate 3 — RLS pgTAP (`supabase test db`) | pass, 2m12s |
| **Gate 4 — E2E (Playwright on seeded local Supabase)** | **pass, 7m25s** |
| RLS migration guard | pass, 10s |

Plus the Vercel preview deployment.

**What gate 4 actually proved** — this is the part that matters, because a skipped spec reports as
a success and that is the failure T2 built its guard for:

- `[e2e-mandatory] OK — 12 mandatory spec(s) ran with no skips`
- **121 e2e tests passed**, zero retries, zero flakes
- All four A9 tests appear in the run log **by name**, with these timings:
  - `a real submission persists with split consent evidence and no false brochure stamp` — 674 ms
  - `the optional marketing opt-in is recorded only when the visitor clicks it` — 652 ms
  - `A8's seeded lead is untouched by this spec` — 17 ms
  - `the ficha and the brochure both serve a real PDF` — 1.4 s
- A8's three `pasantias-leads-admin.spec.ts` tests ran green alongside, so the two phases do not
  interfere.

**Why the guard says 12 when `MANDATORY_SPECS` has 11 entries on this branch** — record this, it is
not a discrepancy: CI builds the PR's **merge** commit, and `main` had advanced past this branch's
base when Z2 merged (PR #45, `34ea4cb6`), adding `tests/e2e/zoom-managed-join.spec.ts`. So 11 + 1 =
12, and gate 2's 291 files / 6781 tests exceeds the branch's own local 266 / 6263 for the same
reason. **CI therefore proved A9 against current `main` with Z2 included** — stronger evidence than
the branch head alone.

### The later runs — green, docs-only, code byte-identical

- **`31276783803`** at `9b1ed1dd` — success.
- **`31279074281`** at `fb8066e8` (current head) — success.
- `git diff --stat b0cc9728..fb8066e8` touches **only** `docs/plan/LEDGER.md`, `docs/plan/PLAN.md`
  and `docs/plan/prompts/a9-sol.md`. **The code is byte-identical across all three runs.**
- **Run `31276763085` at `5550de57` was CANCELLED**, superseded by the next push. **Never cite it as
  green.**

Follow A7a's precedent here: name the **code-bearing** run as the evidence, note the later runs
agree, and say the code is byte-identical between them. Your own commit in this round will trigger
yet another run — that is expected. **Do not chase it and do not rewrite the evidence to cite it**;
it is docs-only and the code-bearing run remains `31276283612`.

### Git facts

- `82bc0e7b` (the code commit) is **6 files, +692 / −19**.
- The range Sol reviewed, `7c7059ff..9b1ed1dd`, contains **4 commits**. To current head `fb8066e8`
  it is **5**. Exactly **one** of them carries code: `82bc0e7b`. Everything else is documentation.
- Base: `7c7059ff`.

---

## SCOPE — four files, all documentation

### 1. `docs/plan/evidence/a9/release-checklist.md` — the BLOCKING fix

- **Flip the five `PENDING CI` rows to `PASS`** (lines ~44–48: A2-7a, A2-7b, A2-7c, A2-7d, and the
  `A2-4/6 (CI)` row), each carrying the run evidence that proves it: run id, gate 4's result, and
  the named test with its timing from the table above. A2-7c's evidence should say plainly that the
  claim-and-release contract executed for the first time and `brochure_sent_at` came back `null` as
  the design requires.
- **Update §E "Status of this checklist"** (line ~140) so the counts match: 8 + 5 = **13 rows PASS**,
  **4 OWNER-RUN — PENDING**, zero PENDING CI. Keep the statement that the checklist is **not yet
  fully green** and that `[A3]` therefore does not close the phase — that remains true and is the
  honest headline.
- **Update §D "How the e2e spec was executed."** Its claim that the spec was not run locally is
  still true and stays; but "its CI run is its first execution" is now past tense — record that it
  *has* executed, in run `31276283612`, and what that run proved. Keep the explanation of why a
  local run was refused (it would write to the live GENERA Supabase project).
- Add a line linking the committed run evidence file you create in step 2.

### 2. `docs/plan/evidence/a9/ci-run-31276283612.md` — new

The META rule in `PLAN.md` requires phase evidence to be committed under
`docs/plan/evidence/<phase-id>/` and linked from the ledger, because **no evidence may live only in
a chat transcript**. A9 has none of this committed yet.

**Read `docs/plan/evidence/a7a/ci-run-31264578140.md` and `docs/plan/evidence/a8/ci-run-31262431697.md`
first and match their shape** — this project already has a form for this file and yours should not
invent a different one.

### 3. `docs/planning/reviews/fase-a9-review-request.md` — three stale statements

- **Line 6, `**Commits:** 1`** (Sol's N1). Make it precise rather than merely bigger: **one code
  commit (`82bc0e7b`)** plus documentation commits, 5 in the range to current head. State it so it
  cannot go stale again when another docs commit lands.
- **Line 87**, the test-evidence table's e2e row, says **`NOT RUN` — see Limitations**. It ran.
  Replace with the run id, gate 4's pass, and the four tests' timings.
- **Line 152**, under Known limitations: "**`tests/e2e/pasantias-flow.spec.ts` has not been
  executed.** CI on this branch's PR is its first run." Rewrite to past tense with the outcome.
  The honest residue is worth keeping in some form: it passed on its first-ever execution, which is
  evidence it works and not yet evidence it is stable under repetition.

### 4. `docs/plan/LEDGER.md` — Sol's N2, plus your own round entry

- **Correct the r1 executor entry's diffstat**: it says `82bc0e7b` is "5 files, +665/−19"; it is
  **6 files, +692/−19**. Correct it **in place**, and mark the correction visibly — e.g.
  `6 files, +692/−19 (corrected at r2; the r1 entry said 5 files, +665/−19)`. There is precedent in
  this very phase (`b0cc9728`, "correct the r1 ledger commit SHA"), and a wrong figure left standing
  in an append-only ledger is worse than a traceable correction.
- **Replace the literal `<this commit>`** in that same entry with prose, matching the house style
  used elsewhere in this file ("this commit").
- **Append your own r2 round entry** (format below). Do not reflow, reorder or edit any other
  entry — other sessions append to this file.

---

## EXPLICITLY OUT OF SCOPE

- **Any code or test file.** Not `tests/e2e/pasantias-flow.spec.ts`, not
  `__tests__/pages/pasantias-site-links.test.ts`, not `scripts/ci/e2e-mandatory.mjs`, and nothing
  under `pages/`, `components/` or `lib/`. `git diff --stat fb8066e8..HEAD` must show **only**
  files under `docs/` when you are done. A9 changes no product code (`[A-new-4]`), and the code it
  does contain is CI-proven — editing it re-opens proven ground for no gain.
- **Sol's N3** (each PDF request allows 90 s inside a 120 s test, so the second cannot use its full
  budget). Real, but it is a NIT in a **now-mandatory** spec that is green, and a mandatory spec
  blocking every future PR is precisely the fragility Sol flagged elsewhere. **Logged to the
  backlog, not fixed.** Note it in your ledger entry as deliberately deferred.
- **Sol's S1** (the 1.5 MB `og:image` likely too heavy for a WhatsApp preview). Product asset,
  forbidden to A9 by `[A-new-4]`, and it belongs to the owner-run A2-9 result. Backlog.
- **The four OWNER-RUN — PENDING rows.** Brent runs those. Do not fill them in, do not guess an
  outcome, and do not submit the production lead form — that creates a real lead row and sends real
  mail. Leave the §C instructions exactly as they are.
- **A8's two deferred SHOULD-FIX items**, the inherited conflict markers in
  `docs/plan/prompts/a4-3.md`, `docs/plan/prompts/a5-3.md`, `docs/plan/reviews/REVIEW-A5.md`, and
  the inherited `lint:testid` baseline.

---

## ACCEPTANCE CRITERIA

- **[B1]** Zero occurrences of `PENDING CI` remain in `docs/plan/evidence/a9/release-checklist.md`;
  the five rows read `PASS` with run evidence; §E's counts are arithmetically right (13 PASS,
  4 owner-run) and still state that the checklist is not fully green.
- **[B1-b]** `docs/plan/evidence/a9/ci-run-31276283612.md` exists, matches the shape of A7a's and
  A8's equivalents, and is linked from the checklist and from your ledger entry.
- **[N1]** The review-request states the commit count precisely and cannot be made stale by the next
  docs commit; its e2e row and its Known-limitations bullet both reflect that the spec ran.
- **[N2]** The r1 entry's diffstat reads `6 files, +692/−19` with the correction marked, and no
  literal `<this commit>` remains in `docs/plan/LEDGER.md`.
- **[SCOPE]** `git diff --stat fb8066e8..HEAD` shows only files under `docs/`.
- **[GATES]** `npm run type-check && npm run lint && npm test` still green. No code changed, so
  nothing should move — run them anyway and paste the output; a docs round that breaks a gate has
  done something it did not intend.

## TEST COMMAND — verbatim

```bash
npm run type-check && npm run lint && npm test
```

`npm run build` is not required (no code changed, and the build needs Supabase env a fresh worktree
lacks). `npm run test:db` is not applicable. The e2e re-runs in CI on your push.

---

## RULES

- Surgical edits. Match each file's existing tone and table formatting — the checklist and the
  review-request are deliberately written as evidence a stranger can audit, and they should still
  read that way afterwards.
- **Do not overstate what CI proved.** It proved four tests passed once, on their first execution,
  against the PR merge commit. It did not prove they are stable under repetition, and it did not
  prove anything about the four owner-run rows.
- **Do not grade your own work in prose.** Show the diff and the raw output.
- If a fact in this prompt disagrees with what you observe, **STOP** and report
  `STATUS: FINDINGS`. Do not reconcile it by picking one.
- es-CL for user-facing copy (there is none in this round); English for docs and commits.
- No deployments. No merge. Push the branch and stop.

## WHEN DONE

Append to `docs/plan/LEDGER.md` (append only):

```markdown
### 2026-08-08 — A9 round r2 — <your model> (executor)
- SESSION: `SESSION: INSPIRA · A9 · r2 · EXEC`
- CONTEXT PRESSURE: <comfortable | tight | ran out — at what point?>
- ACTION: <what you changed, per criterion>
- COMMITS: <sha…>
- TESTS: <command> → <pass/fail, counts>
- FINDINGS RAISED: <or none>
- DECISIONS: <including N3 and S1 deliberately deferred>
- BACKLOG ADDED:
- OPEN AFTER THIS ROUND:
```

Then output exactly this report:

```markdown
## EXECUTOR REPORT — A9 round r2
STATUS: COMPLETE | BLOCKED | FINDINGS
CONTEXT PRESSURE: comfortable | tight | ran out — <one line>
BRANCH: phase/a9-verify   BASE: fb8066e8   COMMITS: <sha…>
FILES CHANGED: <path (+x/-y) …>
WHAT I DID: <one bullet per criterion, mapped to its ID>
TEST COMMAND: <verbatim>
TEST OUTPUT: <verbatim tail, unedited>
SCOPE PROOF: <git diff --stat fb8066e8..HEAD — must be docs only>
REMAINING `PENDING CI` OCCURRENCES: <grep -c 'PENDING CI' on the checklist — must be 0>
ACCEPTANCE CRITERIA: <ID: met / not met / partially — with reason>
DEVIATIONS FROM PROMPT: <or "none">
ASSUMPTIONS MADE: <or "none">
NOT DONE / OPEN:
```
