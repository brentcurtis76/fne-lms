SESSION: INSPIRA · A9 · r3 · EXEC

You are the executor for INSPIRA Comms (repo `fne-lms`), phase **A9 — Track A release
verification**, round **r3**. You have no prior context. Everything you need is below.

Read `CLAUDE.md` at the repo root first. Its hard rules outrank this prompt.

**This is a documentation-accuracy round. You will not touch a single line of code or test.**
Three files, and the whole defect is four sentences.

---

## BRANCH

Work is already on `phase/a9-verify`, worktree **`~/dev/wt-a9`**.

```bash
cd ~/dev/wt-a9
git status            # must be clean
git log --oneline -1  # record what you actually get
```

No new branch, no new worktree. Commit here and push. **Do not merge, do not open or close a PR,
do not run any `vercel` command.** [PR #46](https://github.com/brentcurtis76/fne-lms/pull/46) is
open; your push updates it.

---

## WHY THIS ROUND EXISTS, AND WHY IT IS THE THIRD

Codex Sol has **FAILed A9 twice**. Round 1's BLOCKING (five checklist rows stuck in an illegal
`PENDING CI` state) was fixed at r2 and Sol confirms it discharged. Round 2 found this:

> The evidence now falsely says the spec has executed "exactly once" and that run `31276283612` was
> its "so far only" execution. By head `f7845196`, gate 4 had succeeded **five times** against
> byte-identical code. […] The CI evidence even lists later successful runs immediately after
> claiming the tests passed only once. […] A9's deliverable is accurate evidence, so this repeats
> B1's defect class.

Sol is right. The PM verified it per-job: gate 4 succeeded at 20:11:14, 20:23:38, 21:20:28,
23:45:10 and 00:02:37, and the two most recent both report
`[e2e-mandatory] OK — 12 mandatory spec(s) ran with no skips` with the A9 test present. **The spec
has executed five times against five fresh ephemeral stacks.**

**This is the PM's error, not a previous executor's, and you should know that before you touch the
files.** The extra runs exist *because of the PM's own documentation commits* — each advanced the PR
head and re-ran gate 4. The r2 prompt then explicitly forbade the round from accounting for them
("Do not chase it and do not rewrite the evidence to cite it"), and the r1 verification had praised
the phrase "one green run proves one green run" as good calibration. The r2 executor wrote what it
was told; what it was told had stopped being true.

The SOP caps Codex at 2 rounds. **Brent has explicitly overridden that cap for this one round**
(Decision Log, 2026-08-08). You are the last remediation round A9 gets.

### The trap you must not walk into

A9 has now failed twice on **one mechanism: an evidence document asserting a volatile count that the
phase's own later commits invalidate.**

- Round 1: `**Commits:** 1` — actually 4.
- Round 2: "executed exactly once" — actually 5.

**Your own commit will trigger a sixth gate-4 run before anyone reads your work.** So a round that
simply writes "five times" has authored the third instance of the same defect. **Do not write a bare
count anywhere.**

The cure was found at r1 and never generalised: **name the invariant instead of counting.** The
review-request's commit line was fixed by naming `82bc0e7b` rather than counting commits, and that
line is still correct today. Do the same for runs. This is now a binding plan rule (Decision Log,
2026-08-08: *evidence artifacts state invariants, never bare volatile counts*).

---

## THE FACTS — verified by the PM against the run logs and git

Use these. **Do not re-derive them from memory.** If anything below disagrees with what you observe,
**stop and report `STATUS: FINDINGS`** — a disagreement means one of us is wrong and guessing puts a
fourth wrong statement into the record.

### What is invariant, and therefore safe to assert

- **The spec has never run locally, and still has not.** Local Playwright starts `npm run dev:unsafe`,
  which loads `.env.local`, which points at the **live shared GENERA Supabase project** — and the
  spec POSTs leads. A local run would write synthetic rows into the production leads table.
- **Every execution it has ever had was CI gate 4**, on a freshly created, seeded, ephemeral Supabase
  stack that is destroyed with the runner.
- **It has passed in every run it has been part of. Zero retries, zero flakes, no skips** — the
  anti-skip guard reports `12 mandatory spec(s) ran with no skips` in each.
- **The code-bearing run is `31276283612` @ `b0cc9728`** (which contains the code commit `82bc0e7b`).
  **Every later run is a documentation-only commit against byte-identical code**, provable with
  `git diff --stat b0cc9728..HEAD -- . ':!docs'` → empty.
- **The execution count is a function of how many times the branch was pushed, not a property of the
  phase** — every push re-runs gate 4. That sentence is the invariant that makes any tally obviously
  provisional, and it is the one the last two rounds were missing.

### The tally, if you state one — stamped, never bare

As of **`5f35bc37`** (the head before this round began), gate 4 had succeeded **five** times:

| Run | Head | Gate 4 |
|---|---|---|
| `31276283612` | `b0cc9728` | success — **the code-bearing run** |
| `31276783803` | `9b1ed1dd` | success |
| `31279074281` | `fb8066e8` | success |
| `31284678453` | `bcc9e142` | success |
| `31285321076` | `f7845196` | success |

Plus **`31276763085` @ `5550de57`, which was CANCELLED** — superseded by the next push. It must
never be counted or cited as green; keep it recorded as cancelled so nobody reading the PR's check
history mistakes it for a failure.

Any tally you write must (a) carry the "as of `<sha>`" stamp, and (b) say that later pushes add to
it. Stamp it with **`5f35bc37`** — the head before your work. Do **not** try to stamp it with your
own commit SHA, which you cannot know before making it; that is the `<this commit>` trap this phase
has already hit once.

### What the repetition now does and does not prove — the direction changes

The old framing ("one green run is not evidence of stability under repetition") is now wrong in the
**opposite** direction: it understates. Five consecutive clean runs on fresh stacks with zero
retries **is** meaningful evidence — of **determinism**. What it is still not: evidence of
robustness to change. Every one of those runs executed the **same code** against the **same seeded
fixture set**. Say that precisely. Do not swap one overstatement for another.

---

## SCOPE — three files, documentation only

### 1. `docs/plan/evidence/a9/release-checklist.md`

- **§D, around line 121** — the sentence "Its first — and so far only — execution anywhere was CI
  run `31276283612`" and the closing "one green run proves one green run". Rewrite §D on the
  invariants above: never local (and why), always CI gate 4 on a fresh ephemeral stack, passed every
  time with zero retries, the code-bearing run named, later runs docs-only against byte-identical
  code, a stamped tally, and the determinism-not-robustness distinction.
- **Line 50 (Sol's N4)** — "All five are registered in `MANDATORY_SPECS`" is imprecise: five
  *evidence rows* are registered nowhere. **One spec file (`tests/e2e/pasantias-flow.spec.ts`),
  carrying four tests, is registered, and it is what supports all five rows.** Fix the sentence;
  keep the point it was making about the anti-skip guard.
- Do not touch §A, §B's row states, §C, or §E's arithmetic. They are discharged and correct.

### 2. `docs/plan/evidence/a9/ci-run-31276283612.md`

- **"What it did not prove", around line 54** — "Four tests passed once, on their first execution."
  Same rewrite. This file is the worst instance because it lists the later successful runs about
  eleven lines below that sentence, so it contradicts itself on one page.
- Keep the file anchored on the **code-bearing** run — that is deliberate and follows A7a's
  precedent. It does not become a running log of every run; it becomes a file that states what the
  code-bearing run proved and correctly situates the later ones.

### 3. `docs/planning/reviews/fase-a9-review-request.md`

- **Around line 156** — "has now been executed exactly once, and it passed." Same rewrite.
- Its **e2e row** in the test-evidence table (around line 87) cites the run and the four timings.
  Check whether it also implies a single execution; fix it if so, leave it if not.

### 4. `docs/plan/LEDGER.md` — your round entry (append only)

Do not reflow, reorder, or edit any other entry — other sessions append here.

---

## EXPLICITLY OUT OF SCOPE

- **Any code or test file.** `git diff --stat <your base>..HEAD -- pages components lib tests __tests__ scripts`
  must be **empty**. The spec is CI-proven five times over; editing it now would invalidate that and
  restart the argument.
- **Sol's N3** — each PDF request allows 90 s inside the test's shared 120 s budget. Sol itself calls
  the deferral reasonable. **Backlog. Do not fix it**, and do not touch `pasantias-flow.spec.ts` for
  any reason.
- **Sol's S1** — the 1,539,868-byte `og:image`. Product asset, forbidden to A9 by `[A-new-4]`,
  belongs to whatever the owner-run A2-9 row returns. Backlog.
- **The four OWNER-RUN — PENDING rows** (A2-9, A2-11, A2-12, A2-13). Brent runs those. Do not fill
  them in, do not guess an outcome, **do not submit the production lead form** — it creates a real
  lead row and sends real mail. Leave §C exactly as it is.
- **`PLAN.md`** — the PM has already written the two Decision Log rows and the B11b amendment for
  this override. Do not touch it.
- A8's two deferred SHOULD-FIX items; the inherited conflict markers in `docs/plan/prompts/a4-3.md`,
  `docs/plan/prompts/a5-3.md`, `docs/plan/reviews/REVIEW-A5.md`; the inherited `lint:testid` baseline.

---

## ACCEPTANCE CRITERIA

- **[B1]** No statement in any of the three files claims or implies the spec ran once, or that a
  single run is all the evidence there is. Each rewritten passage rests on the invariants; any tally
  carries the `as of 5f35bc37` stamp and the note that later pushes add to it. **Self-check that
  bites:** `grep -rniE "only run|so far only|passed once|executed (exactly )?once|first and only" docs/plan/evidence/a9/ docs/planning/reviews/fase-a9-review-request.md`
  returns nothing.
- **[B1-b]** No file contradicts itself: `ci-run-31276283612.md` must not describe a single execution
  above a list of five runs.
- **[B1-c]** No bare volatile count anywhere in the three files — every count is stamped or removed.
- **[N4]** The `MANDATORY_SPECS` sentence correctly says one registered spec, four tests, five rows.
- **[HONESTY]** The determinism-not-robustness distinction is stated. Five clean runs are not sold as
  proof the spec is robust to change; they all ran the same code against the same fixtures.
- **[SCOPE]** No code or test file changed.
- **[GATES]** `npm run type-check && npm run lint && npm test` green. Nothing should move — run them
  anyway; a docs round that breaks a gate has done something it did not intend.

## TEST COMMAND — verbatim

```bash
npm run type-check && npm run lint && npm test
```

`npm run build` is not required (no code changed, and a fresh worktree lacks the Supabase env it
needs). `test:db` is not applicable. The e2e re-runs in CI on your push — which will make the tally
six, which is exactly why it is stamped.

---

## RULES

- Surgical edits. Match each file's voice — these are documents written so a stranger can audit them,
  and they must still read that way.
- **Do not overstate and do not understate.** Both have now cost this phase a review round. The
  standard is: a reader who knows nothing should finish with an accurate picture of what was proven,
  by what, and what remains open.
- **Do not grade your own work in prose.** Show the diff and the raw output.
- If a fact here disagrees with what you observe, **STOP** and report `STATUS: FINDINGS`.
- No deployments. No merge. Push the branch and stop.

## WHEN DONE

Append to `docs/plan/LEDGER.md`:

```markdown
### 2026-08-08 — A9 round r3 — <your model> (executor)
- SESSION: `SESSION: INSPIRA · A9 · r3 · EXEC`
- CONTEXT PRESSURE: <comfortable | tight | ran out — at what point?>
- ACTION:
- COMMITS: <sha…>
- TESTS: <command> → <pass/fail, counts>
- FINDINGS RAISED:
- DECISIONS:
- BACKLOG ADDED:
- OPEN AFTER THIS ROUND:
```

Then output exactly this report:

```markdown
## EXECUTOR REPORT — A9 round r3
STATUS: COMPLETE | BLOCKED | FINDINGS
CONTEXT PRESSURE: comfortable | tight | ran out — <one line>
BRANCH: phase/a9-verify   BASE: <the sha you actually started from>   COMMITS: <sha…>
FILES CHANGED: <path (+x/-y) …>
WHAT I DID: <one bullet per criterion, mapped to its ID>
TEST COMMAND: <verbatim>
TEST OUTPUT: <verbatim tail, unedited>
SCOPE PROOF: <git diff --stat <base>..HEAD -- pages components lib tests __tests__ scripts — must be empty>
STALENESS SELF-CHECK: <the [B1] grep and its output — must be empty>
ACCEPTANCE CRITERIA: <ID: met / not met / partially — with reason>
DEVIATIONS FROM PROMPT: <or "none">
ASSUMPTIONS MADE: <or "none">
NOT DONE / OPEN:
```
