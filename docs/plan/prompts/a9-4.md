SESSION: INSPIRA · A9 · r4 · EXEC

You are the executor for INSPIRA Comms (repo `fne-lms`), phase **A9 — Track A release
verification**, round **r4**. You have no prior context. Everything you need is below.

Read `CLAUDE.md` at the repo root first. Its hard rules outrank this prompt.

**This is a mechanical documentation round. One file. Two locations. Five sentences. No code.**
Every defect has already been located by the PM; there is nothing to discover and nothing to judge.

---

## BRANCH

```bash
cd ~/dev/wt-a9
git status            # must be clean
git log --oneline -1  # record what you actually get
```

No new branch, no new worktree. Commit here and push. **Do not merge, do not open or close a PR,
do not run any `vercel` command.** [PR #46](https://github.com/brentcurtis76/fne-lms/pull/46) is
open; your push updates it.

---

## WHY THIS ROUND EXISTS — read this, it changes how you should work

Codex Sol has **FAILed A9 three times, all three on the same fact**: whether
`tests/e2e/pasantias-flow.spec.ts` has executed. It has — **gate 4 has run it successfully many
times** (the stamped list is in `docs/plan/evidence/a9/ci-run-31276283612.md`).

The three failures were not three different defects. They were **one defect, patched three times too
narrowly**:

| Round | Reviewer found | PM's fix | Why the next round failed |
|---|---|---|---|
| 1 | five rows stuck in an illegal `PENDING CI` state | flipped those rows | other passages still said the spec had run once |
| 2 | "so far only execution", "passed once" | rewrote those passages | a *third* set of passages still said it had never run |
| 3 | "not run anywhere yet", "has never executed", "unverified code" | ← **you are here** | — |

**The cause was the verification method.** Each round was given line numbers plus a `grep` pattern
built from the *previous* reviewer's wording, and the next instance always used different words. A
grep derived from the last finding cannot catch the next one. This is now a binding plan rule
(Decision Log 2026-08-08): **a reviewer finding is a symptom, not a work order — reconcile the whole
fact, not the cited passage.**

So your job is not "edit five sentences and grep". It is: **make every statement in the file
consistent with the fact that the spec has executed and is verified — by reading the file end to
end.** The five sentences below are what the PM found doing exactly that; they are a floor, not a
ceiling. If you find a sixth, fix it and say so.

---

## THE FACTS

- `tests/e2e/pasantias-flow.spec.ts` **has executed, repeatedly, and passed every time** — all four
  tests, zero retries, zero flakes, anti-skip guard clean, on a freshly created seeded ephemeral
  Supabase stack each time.
- It has **never run locally**, and never will: local Playwright loads `.env.local`, which points at
  the live shared GENERA Supabase project, and the spec POSTs leads. That statement is still true and
  must stay.
- The **code-bearing run is `31276283612` @ `b0cc9728`** (code commit `82bc0e7b`). Every later run is
  a documentation-only commit against byte-identical code.
- The `brochure_sent_at === null` assertion — which the file currently describes as an unverified
  three-step *inference* — **is now an observation.** It ran and it passed. The chain (CI has no
  `RESEND_API_KEY` → `sendLeadAutoReply` returns `failure:'not_configured'` →
  `canReleaseAutoReplyClaim` is true → the claim is restored) was confirmed by execution, not reasoned
  about.

### ⚠️ Do not write a run count, and do not touch the existing ones

`release-checklist.md` §D and `ci-run-31276283612.md` carry a tally stamped **"as of `5f35bc37`"**,
framed as a lower bound ("read the list as 'at least these', never as 'these and no more'"). **Those
are correct at their stamp and are out of scope. Do not update them, do not re-count, and do not add
a count of your own** — your own push adds another run, which is exactly how the last three rounds
manufactured their successor's defect. Where you need to refer to how much the spec has run, say
"repeatedly", "in every run it has been part of", or point the reader at the evidence file. **No bare
numbers.**

---

## SCOPE — one file

**`docs/planning/reviews/fase-a9-review-request.md`**, plus your ledger entry. Nothing else.

`docs/plan/evidence/a9/release-checklist.md` and `docs/plan/evidence/a9/ci-run-31276283612.md` were
re-read in full by the PM and are **clean — do not touch them.** They are `[A2]`'s actual deliverable
and Sol verified them independently this round.

### Location 1 — line 61

Current text, in "Files, grouped by risk":

> - **`tests/e2e/pasantias-flow.spec.ts`** (new, 259 lines). Four tests. It is the only spec in the
>   suite that inserts rows through a public route, and it is now mandatory, so a flake in it
>   blocks every future PR. **It has not run anywhere yet — see Limitations.**

The final sentence is false. The rest of the bullet is accurate and should stay — the risk it names
(a mandatory spec that writes rows, so a flake blocks every PR) is real and still worth flagging.

### Location 2 — lines 113–121, "Where an independent reviewer should push hardest", item 1

Current text:

> 1. **The new e2e spec has never executed.** This is the honest headline. A local run would have
>    written synthetic leads into the *production* Supabase project (`npm run dev:unsafe` loads
>    `.env.local`), and the prompt's fallback — a local ephemeral stack — was excluded by its own
>    precondition, since a Supabase stack for another worktree was already running on the shared
>    project ref. So every assertion in it is reasoned from source, not observed. **Read it as
>    unverified code and check the reasoning, particularly the `brochure_sent_at === null`
>    assertion**, which depends on a three-step inference: CI sets no `RESEND_API_KEY` →
>    `sendLeadAutoReply` returns `failure:'not_configured'` → `canReleaseAutoReplyClaim` is true →
>    the claim is restored. If any link is wrong the test goes red on the first PR.

**Four false claims here:** "has never executed"; "every assertion in it is reasoned from source, not
observed"; "read it as unverified code"; and the framing of `brochure_sent_at` as an unconfirmed
inference whose failure would show up "on the first PR" — that PR has happened, repeatedly, and it
passed.

**Rewrite the item rather than deleting it.** The section is meant to name the areas a reviewer
should scrutinise hardest, and there is still something real here — it has simply changed. What is
now true and worth a reviewer's attention:

- The spec has executed and passed every time, so it is **no longer unverified code**. The
  never-ran-locally fact stays, with its reason.
- What the repeated passes **do** prove is **determinism** — no dependence on leftover state,
  ordering, or timing. What they **do not** prove is **robustness to change**: every run executed the
  same code against the same seeded fixtures.
- The spec is on `MANDATORY_SPECS`, so if a future change made it flaky that would block every PR,
  not just A9's.
- `brochure_sent_at === null` is the assertion worth understanding, but as an **observed** contract
  now: the claim-and-release path in `lib/pasantias/emails.ts` executed rather than being reasoned
  about.

Keep the item's voice — this file is deliberately written as an honest self-assessment, and it should
still read like one. Do not overcorrect into selling the spec as bulletproof; understating and
overstating have each cost this phase a round.

### Also check, because a floor is not a ceiling

Read the **whole file** end to end and reconcile every other statement about the spec's execution or
verification status. Places worth particular attention, since the PM found live text in all of them
across earlier rounds: the Objective section, the test-evidence table (~line 91), and Known
limitations (~line 156). The PM judged those three currently correct — **confirm that yourself rather
than trusting it**, and report what you checked.

---

## EXPLICITLY OUT OF SCOPE

- **Any code or test file.** `git diff --stat <base>..HEAD -- pages components lib tests __tests__ scripts`
  must be **empty**.
- **The two evidence files.** Clean, Sol-verified, and `[A2]`'s deliverable. Not yours this round.
- **The stamped run tallies.** See the warning above.
- **`PLAN.md`** — the PM has already written this override's Decision Log rows.
- **Sol's N3** (each PDF request allows 90 s inside the test's shared 120 s budget) — backlog, and
  `pasantias-flow.spec.ts` is not to be touched for any reason.
- **Sol's S1** (the 1,539,868-byte `og:image`) — product asset, forbidden by `[A-new-4]`, belongs to
  the owner-run A2-9 result.
- **The four OWNER-RUN — PENDING rows.** Brent runs those. Do not fill them in, do not guess an
  outcome, **do not submit the production lead form** — it creates a real lead row and sends real mail.
- A8's two deferred SHOULD-FIX items; the inherited conflict markers in `docs/plan/prompts/a4-3.md`,
  `docs/plan/prompts/a5-3.md`, `docs/plan/reviews/REVIEW-A5.md`; the inherited `lint:testid` baseline.

---

## ACCEPTANCE CRITERIA

- **[B1]** No statement anywhere in `fase-a9-review-request.md` claims or implies that the spec has
  not run, is unverified, is unobserved, is reasoned-from-source rather than executed, or that its
  first execution is still ahead. Both locations above are fixed, and any further instance you found
  is fixed and named in your report.
- **[B1-b]** The file does not contradict itself: no passage asserting non-execution survives
  alongside the passages that cite the run and its result.
- **[B1-c]** No new run count anywhere, and the existing stamped tallies are untouched.
- **[VERIFIED BY READING]** You read the file end to end and reconciled every execution-status claim.
  **Report the reconciliation as a list** — each statement you assessed and whether you changed it.
  A grep is not the verification here and will not be accepted as one; use it only as a supplement.
- **[HONESTY]** The determinism-not-robustness distinction survives. The spec is not sold as robust
  to change.
- **[SCOPE]** No code, no test, no evidence file changed.
- **[GATES]** `npm run type-check && npm run lint && npm test` green — nothing should move; run them
  anyway.

## TEST COMMAND — verbatim

```bash
npm run type-check && npm run lint && npm test
```

`npm run build` is not required (no code changed, and a fresh worktree lacks the Supabase env).
`test:db` is not applicable.

---

## RULES

- Surgical edits, and match the file's voice — it is an honest self-assessment and must still read as
  one.
- **Do not grade your own work in prose.** Show the diff and the raw output.
- If a fact here disagrees with what you observe, **STOP** and report `STATUS: FINDINGS` — but if the
  disagreement is a stale figure the PM handed you and the fix does not depend on it, correct it,
  flag it, and carry on. That is what r3 did and it was the right call.
- No deployments. No merge. Push the branch and stop.

## WHEN DONE

Append to `docs/plan/LEDGER.md` (append only; do not reflow or edit other entries):

```markdown
### 2026-08-08 — A9 round r4 — <your model> (executor)
- SESSION: `SESSION: INSPIRA · A9 · r4 · EXEC`
- CONTEXT PRESSURE: <comfortable | tight | ran out — at what point?>
- ACTION:
- RECONCILIATION LIST: <every execution-status statement you assessed, and its disposition>
- COMMITS: <sha…>
- TESTS: <command> → <pass/fail, counts>
- FINDINGS RAISED:
- DECISIONS:
- BACKLOG ADDED:
- OPEN AFTER THIS ROUND:
```

Then output exactly this report:

```markdown
## EXECUTOR REPORT — A9 round r4
STATUS: COMPLETE | BLOCKED | FINDINGS
CONTEXT PRESSURE: comfortable | tight | ran out — <one line>
BRANCH: phase/a9-verify   BASE: <the sha you actually started from>   COMMITS: <sha…>
FILES CHANGED: <path (+x/-y) …>
WHAT I DID: <one bullet per criterion, mapped to its ID>
RECONCILIATION LIST: <every execution-status statement in the file, and whether you changed it>
TEST COMMAND: <verbatim>
TEST OUTPUT: <verbatim tail, unedited>
SCOPE PROOF: <git diff --stat <base>..HEAD -- pages components lib tests __tests__ scripts — must be empty>
EVIDENCE FILES UNTOUCHED: <git diff --stat <base>..HEAD -- docs/plan/evidence/a9 — must be empty>
ACCEPTANCE CRITERIA: <ID: met / not met / partially — with reason>
DEVIATIONS FROM PROMPT: <or "none">
ASSUMPTIONS MADE: <or "none">
NOT DONE / OPEN:
```
