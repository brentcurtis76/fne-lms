# EXECUTOR PROMPT — Phase B1b, round 1 (INSPIRA Comms)

BRANCH: `phase/b1b-relay`

FIRST ACTIONS (mandatory, in order):
1. `git fetch origin && git worktree add ../wt-b1b -b phase/b1b-relay origin/main`
2. Copy `.env.local` from the main checkout into the worktree.
3. Work ONLY in the worktree.

## Context you need

- B1a (merged) moved every legitimate mail flow off the unauthenticated relay:
  expense-report notifications now go through
  `pages/api/expense-reports/[id]/notify.ts` + `lib/email/expenseNotifications.ts`,
  and `utils/emailUtils.ts` was deleted. Your phase deletes the relay itself.
- Known greps that are NOT relay callers (do not chase them):
  `utils/meetingUtils.ts` invokes a **Supabase edge function** named
  `send-email` via `supabase.functions.invoke(...)` — a different system,
  out of scope; doc comments in the B1a files mention `/api/send-email`
  historically.
- `@sendgrid/mail` sits in `package.json` and is imported nowhere (verify
  before removing).

## Scope — PLAN.md Phase B1b [A1]–[A2]

1. Delete `pages/api/send-email.ts` and `pages/api/test-email.ts`.
2. Remove `@sendgrid/mail` from `package.json` (+ lockfile via
   `npm uninstall @sendgrid/mail` inside the worktree).
3. Prove no live caller remains: `grep -rn "api/send-email" --include="*.ts" --include="*.tsx" --include="*.js" pages lib utils components scripts`
   → only historical doc comments may remain (list them in the report);
   `grep -rn "sendgrid" --include="*.ts*" .` (excluding node_modules) → empty.
4. Remove/adjust any tests that exercised the deleted routes; full gates green.

ACCEPTANCE: no unauthenticated or browser-controlled arbitrary-recipient send
path exists in the repo; sendgrid dependency gone; gates green
(`npm run type-check && npm run lint && npm test && npm run build`).

RULES: SOP `docs/plan/AGENT-WORKFLOW.md` + repo `CLAUDE.md` govern. Surgical —
deletion phase, do not refactor anything adjacent; max 3 red iterations then
BLOCKED; if you find a live caller B1a missed → STATUS: FINDINGS with the
call chain (do not migrate it yourself). Write
`docs/planning/reviews/fase-b1b-review-request.md`. WHEN DONE: LEDGER entry in
your worktree, push, EXECUTOR REPORT per SOP §2.3.
