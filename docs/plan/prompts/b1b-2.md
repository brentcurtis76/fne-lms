# EXECUTOR PROMPT — Phase B1b, round r2 (INSPIRA Comms) — Codex remediation

Remediation of REVIEW-B1B.md's single BLOCKING finding (file on the branch;
PM triage in the branch ledger). Round r1's deletions stand.

BRANCH: `phase/b1b-relay` (EXISTING — continue it)

FIRST ACTIONS (mandatory, in order):
1. `git fetch origin`. Try `git worktree add ../wt-b1b2 phase/b1b-relay`; if git
   refuses (branch checked out elsewhere), use
   `git worktree add --detach ../wt-b1b2 origin/phase/b1b-relay` and push later
   with `git push origin HEAD:phase/b1b-relay`.
2. Copy `.env.local` from the main checkout into the worktree.

## The finding, plainly

`utils/meetingUtils.ts` exports `sendTaskAssignmentNotifications(...)` (~L690–740),
which builds `{to, subject, html}` IN THE BROWSER and invokes a Supabase edge
function named `send-email`. That is the browser-controlled-send class this
phase exists to eliminate. PM-established facts you may rely on: the edge
function does NOT exist in this Supabase project (invocations have always
404'd into the swallowed console.error), and the only caller is
`components/meetings/MeetingDocumentationModal.tsx` (import ~L45, call ~L778,
fire-and-forget after task assignment). The email "feature" has never worked.

## Scope — delete the dead path

1. Delete `sendTaskAssignmentNotifications` from `utils/meetingUtils.ts`.
2. Remove its import and call from `MeetingDocumentationModal.tsx` — the
   surrounding task-assignment flow must be untouched and keep working
   (locate by content; verify what else the call site does around it).
3. Check whether an IN-APP notification for task assignment exists in that
   flow (lib/notificationService / notifications insert) and state the answer
   in your report — it determines whether the backlogged server-side email
   rebuild is a gap or a nice-to-have. Do NOT build anything.
4. Prove the class is gone: after `npm run build`,
   `grep -rn "functions.invoke('send-email'\|functions.invoke(\"send-email\"" utils lib components pages` → empty, and
   `grep -c "send-email" .next/static/chunks/*.js` (or equivalent sweep of
   .next/static) → 0 matches. Commit the proof into
   `docs/plan/evidence/b1b/` alongside round r1's.
5. Any test touching the deleted function: update/remove; full gates green.

ACCEPTANCE: the finding is closed — no browser-reachable code path in the
repo constructs {to, subject, html} for any send mechanism (Next.js route,
edge function, or otherwise); meeting task assignment still works; gates green
(`npm run type-check && npm run lint && npm test && npm run build`).

RULES: SOP + repo CLAUDE.md. Surgical — do not refactor meetingUtils beyond
the deletion; max 3 red iterations then BLOCKED; if you find yet another
browser send path in the sweep → STATUS: FINDINGS with the chain, do not fix
it silently. Update `docs/planning/reviews/fase-b1b-review-request.md` with a
round-r2 section. WHEN DONE: LEDGER entry (worktree), push, EXECUTOR REPORT
per SOP §2.3.
