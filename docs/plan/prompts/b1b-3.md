# EXECUTOR PROMPT — Phase B1b, round r3 (INSPIRA Comms) — owner-authorized closure

Owner-authorized third round (§1.5). Closes REVIEW-B1B-R2.md's single finding:
the regression guard's sweep omits the active `src/` client tree.

BRANCH: `phase/b1b-relay` (EXISTING — continue it)
FIRST ACTIONS: `git fetch origin`; worktree per the usual rule (detach
fallback + push `HEAD:phase/b1b-relay` if held); copy `.env.local` in.

## Scope — one test file

`__tests__/utils/no-browser-mail-transport.test.ts`: extend the swept
directory set to the FULL client-source surface — at minimum add `src`, and
derive the list from what tsconfig actually includes rather than hand-picking
(state in a comment how the set is derived and why each excluded top-level
dir is excluded, e.g. supabase/ = SQL, scripts/ = node-only). Prove the
extended sweep bites: scratch-add a `functions.invoke('send-email')` call
under `src/`, show the test fails, revert (append proof to
docs/plan/evidence/b1b/dead-edge-relay-removal.md). All suites green.

ACCEPTANCE: the guard covers every tree client code can live in; proof
committed; four gates green.
RULES: SOP + repo CLAUDE.md; surgical — nothing beyond the test + evidence;
max 3 red iterations then BLOCKED. Round-r3 section in the review request.
WHEN DONE: LEDGER entry (worktree), push, EXECUTOR REPORT per SOP §2.3.
