# EXECUTOR PROMPT — Phase B2, round r1 (INSPIRA Comms) — compatibility spike

BRANCH: `phase/b2-spike`
FIRST ACTIONS: 1) `git fetch origin && git worktree add ../wt-b2 -b phase/b2-spike origin/main`
2) copy `.env.local` in; 3) work ONLY there; 4) read `docs/plan/PLAN.md`
Phase B2 + frozen decisions D-06..D-09 from origin/main HEAD.

## Purpose

Lock the REAL contracts the Track-B sending stack will build on, so B3+
phases code against verified shapes instead of memory. NO feature code.

## Scope

1. **Resend SDK (installed 3.5.0 — do not upgrade unless findings demand it):**
   from `node_modules/resend/dist/index.d.ts` + targeted mock tests, lock:
   `batch.send` request/response nesting (`{data:{data:[{id}]}}` suspected —
   verify), per-email `headers` support (List-Unsubscribe pair), error-as-value
   vs thrown behavior for both `emails.send` and `batch.send`, idempotency-key
   support (or its documented absence + implication for D-07's ledger dedup).
   Encode each as a test in `__tests__/lib/resend-contract.test.ts` so a
   future SDK upgrade breaks loudly.
2. **svix verification:** add the `svix` dependency; lock its verify API with
   known-vector tests (valid, tampered, stale, FUTURE timestamp, multi-
   signature header) in the same suite or a sibling.
3. **Vercel cron reality:** determine what cron cadence this project's Vercel
   plan actually allows (vercel.json `crons` support, min interval). Sources:
   the repo's config, Vercel docs, `vercel` CLI if authenticated — do NOT
   deploy anything. If per-minute cron is unavailable, say so plainly with
   the evidence: D-07's invoker gets re-planned by the PM before B10a.
4. **Findings file:** `docs/plan/reviews/fase-b2-findings.md` — every locked
   shape, every open risk, written so B3/B4/B10 executors can cite it.

ACCEPTANCE: contract tests green and meaningful (mock the transport, not the
shapes); svix added with vectors; cron capability documented with evidence;
findings file complete; four gates green.
RULES: SOP + repo CLAUDE.md; no feature code, no deploys, no upgrades without
findings; max 3 red iterations then BLOCKED.
Review-request `docs/planning/reviews/fase-b2-review-request.md`.
WHEN DONE: LEDGER entry (worktree), push, EXECUTOR REPORT per SOP §2.3.
