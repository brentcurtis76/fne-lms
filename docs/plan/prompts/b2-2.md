# EXECUTOR PROMPT — Phase B2, round r2 (INSPIRA Comms) — Sol remediation

Closes REVIEW-B2.md's findings 1 and 2 (file on the branch; PM triage in the
branch ledger). Finding 3 was a PM plan defect — already fixed on main (D-07
+ B10a now mandate a ≥150 ms-paced sender); your only job with it is to make
the findings card cite the amended rule.

BRANCH: `phase/b2-spike` (EXISTING — continue it)
FIRST ACTIONS: `git fetch origin`; worktree per the usual rule (detach
fallback + push `HEAD:phase/b2-spike` if held); copy `.env.local` in.
**Step 1 is `git merge origin/main`** — PR #36 currently conflicts and has no
CI results. Resolve docs conflicts by taking main's PLAN verbatim and
UNIONING the ledger (every `### ` heading of both parents must survive; assert
it, and stop with FINDINGS rather than dropping entries).

## Finding 2 — svix suite lets real mutants through (do this first; it is the
   more serious one: a contract suite that passes a mutation is worse than no
   suite, because later phases will trust it)

Sol found two meaningful mutants staying green. Re-derive the suite so it
actually locks:
- **Raw-byte verification**: prove the payload must be the exact bytes —
  a semantically-identical JSON re-serialization (different key order or
  whitespace) MUST fail verification. Today's assertions do not force this.
- **The exact ±300 s boundary**: assert 300 s exactly (accepted) AND 301 s
  (rejected) on BOTH sides, so a mutant that widens or narrows the tolerance
  by a second dies.
- Then run YOUR OWN mutation set over the svix suite — at minimum: flip the
  tolerance constant, swap raw bytes for a re-serialized body, drop the
  version filter, invert the signature comparison. Every mutant must turn the
  suite red; commit the run's verbatim output as evidence. If any mutant
  survives, the assertion is wrong — fix it, don't note it.

## Finding 1 — idempotency options understate exposure and misprice the upgrade

In the findings card: (a) recompute the **duplicate exposure** honestly for
each option, at the drain's real worst case (bounded + paced per amended
D-07 — read it from origin/main): how many recipients can receive a duplicate
in each failure mode, not just "≤1 batch"; (b) establish the **actual minimum
Resend version** that exposes `idempotencyKey` (check the package's own
changelog/types across versions rather than asserting "current SDKs have it"),
with the concrete upgrade delta for this repo; (c) restate the three options
with those corrected numbers so B10a's decision rests on arithmetic, not
adjectives.

## Also

Update the findings card's cron/pacing section to cite the amended D-07 rule
and Brent's first-hand Pro confirmation (R1 is closed — remove the residue).

ACCEPTANCE: mutation evidence shows every named svix mutant failing; raw-byte
and exact-boundary assertions present; findings card's idempotency section
carries computed exposure numbers and a verified minimum version; card cites
amended D-07; branch merged with main and gates green (type-check, lint,
npm test, build) so PR #36's CI can run.
RULES: SOP + repo CLAUDE.md; no feature code; no SDK upgrade in this round
(the card recommends, B10a decides); max 3 red iterations then BLOCKED.
Round-r2 section in docs/planning/reviews/fase-b2-review-request.md.
WHEN DONE: LEDGER entry (worktree), push, EXECUTOR REPORT per SOP §2.3.
