# EXECUTOR PROMPT — Phase B3, round r2 (INSPIRA Comms) — proof hardening

Closes REVIEW-B3.md's two BLOCKING findings. **Both are test-proof gaps, not
schema defects** — Sol mutated the schema and watched your 313/313 stay green.
The migration is expected to stand; the suite has to become able to fail.

BRANCH: `phase/b3-email-db` (EXISTING — continue it)
FIRST ACTIONS: `git fetch origin`; worktree per the usual rule (detach fallback
+ push `HEAD:phase/b3-email-db` if held); copy `.env.local` in. **Run
`supabase db reset` before testing** — a stale local stack fails this suite
misleadingly (it cost the PM a false alarm).

## Finding 1 — ACL pins ignore `is_grantable` and inherited PUBLIC grants

The pins compare privilege *names* only. Two mutations survive today:
- a grant made `WITH GRANT OPTION` (the holder can re-grant to anyone);
- a privilege reaching anon/authenticated through a **`PUBLIC`** grant rather
  than a direct one.

Tighten the ACL assertions so both die:
- compare the full `aclexplode(relacl)` tuple including **`is_grantable`**, and
  assert it is false everywhere a privilege is legitimately held;
- assert **no `PUBLIC` (grantee 0) entry exists** on any of the five tables — a
  PUBLIC grant is inherited by every role including anon, so per-role pins
  alone cannot see it;
- keep the existing per-role expectations (authenticated = {SELECT}, anon = no
  entry, service_role = full).
Prove it: apply each mutation (`GRANT ... WITH GRANT OPTION`, `GRANT SELECT ...
TO PUBLIC`) inside the test transaction or a scratch migration, show the suite
red, revert, show green. Commit the verbatim runs to `docs/plan/evidence/b3/`.

## Finding 2 — anonymization tests do not pin every identity field

Dropping `basis_note IS NULL` from the anonymized shape survived. Assert the
anonymized row shape **field by field**, each as its own assertion so a single
missing column fails on its own line: `email`, `email_normalized`,
`first_name`, `last_name`, `organization`, `basis_note`, `unsubscribe_token`
all NULL, `anonymized_at` NOT NULL — and the live shape's inverse. Then mutate
the CHECK once per field (drop that field's clause), show the suite red for
that field, restore. Commit the runs.

ACCEPTANCE: every mutation Sol named, plus your per-field mutations, turns the
suite red and the restored schema is green; `supabase db reset && npm run
test:db` green; four JS gates green (they cannot be affected — run them anyway
and say so); evidence refreshed under `docs/plan/evidence/b3/`.
RULES: SOP + repo CLAUDE.md; **do not change the migration unless a mutation
proves it actually wrong** — if that happens, report FINDINGS rather than
silently repairing it; max 3 red iterations then BLOCKED.
Round-r2 section in `docs/planning/reviews/fase-b3-review-request.md`.
WHEN DONE: LEDGER entry, push, EXECUTOR REPORT per SOP §2.3.
