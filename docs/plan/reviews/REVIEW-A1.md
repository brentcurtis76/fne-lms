# CODEX REVIEW — A1 round 1

VERDICT: FAIL

The final branch head implements the amended cohort facts, lodging-band model, and homepage card correctly, and every repository gate is green. A1 is still not mergeable because the two D-01 regression guards do not enforce the boundaries they claim to enforce: the post-build scanner misses the new lodging-band price literals in the exact tree-shaken case it is meant to catch, and the unit guard serializes a hand-maintained aggregate rather than the public module.

BLOCKING:

- [B1] The post-build leak guard does not detect either current lodging-band price literal when it is leaked independently — `scripts/check-price-leak.mjs:57` — Appendix A-8 and A1 [A2] now make €70 and €120 protected price literals, but `PRICE_AMOUNT_PATTERNS` contains only 1.000/`1e3` and 810, while the remaining checks require the sentinel or commercial-only prose. Both can be tree-shaken away when a public consumer imports just `COHORT_LODGING_PER_NIGHT_EUR.min` or `.max` and renders it. I executed the scanner's committed regexes directly: `window.price="€70"` → no finding and `window.price="€120"` → no finding, while `€1.000` is detected. The committed mutation demonstration imports `COHORT_LODGING_NOTE`, so `por persona por noche` catches that richer case and never exercises the isolated band literals. This violates D-01's sentinel-plus-price-literals guarantee and the amended A1 acceptance criterion. Required closure: make the real post-build guard fail for isolated client-bundle leaks of both €70 and €120, and add an automated regression test or committed mutation fixture that proves those two cases fail without relying on the sentinel or lodging prose.
- [B2] The required public-module serialization guard covers only `COHORT_PUBLIC`, not the module's exports — `__tests__/lib/pasantias-cohort.test.ts:290` — the test calls `JSON.stringify(COHORT_PUBLIC)`, a manually assembled object at `lib/pasantias/cohort-public.ts:311`. A future monetary export can therefore be added to `cohort-public.ts` but omitted from `COHORT_PUBLIC`, and the stated “public-module serialization contains no monetary keys/values” guard remains green. That is the precise drift a module-boundary guard must prevent. Required closure: serialize the imported `cohortPublicModule` namespace (or otherwise enumerate every runtime export mechanically) for both the monetary-token and protected-number assertions. Retain the accepted whole-token pattern `\beur(?:os?)?\b`: independently verified that it rejects `EUR`, `euro`, and `euros` while allowing `Colegio Virgen de Europa`.

SHOULD-FIX:

- [S1] Enforce D-01's importer allowlist at source level before A3 adds the first production importer — `lib/pasantias/cohort-commercial.ts:4` — the frozen decision permits this module only in the server-side brochure generator, but the build scan cannot detect an API route, transactional-email template, or other server bundle importing it. The current tree is clean (`rg` finds only the unit test), so this does not independently block A1; an import-graph test should pin the future brochure path as the sole allowlisted production importer.
- [S2] Add a rendered homepage-card regression assertion — `pages/index.tsx:301` — the current test pins `COHORT_HEADLINE` but does not render `HomePage`, so it would stay green if the card stopped using the constant. The fresh prerendered HTML does contain exactly `Octubre 2026 · 5–9 y 13–16 de octubre`, and the only remaining `Abril 2026` strings are the two flipbook titles expressly deferred to A7a, so the current implementation satisfies A1 [A4].

NITS:

- None.

NOTES ON THE PLAN ITSELF:

- Normative source used: `origin/main:docs/plan/PLAN.md` at `7f3c0a0`, not the branch copy. The 2026-08-01 propagation amendment makes A1 [A3] and A3 consistent with Appendix A-8: programme €1.000, Barcelona lodging €70–120 per person per night, and no combined total. No plan defect remains for A1.
- Reviewed the full seven-commit phase from base `baec41a` through final head `e5dc33a`, including all three executor code rounds (`b03dd56`, `5fb547a`, `65aa52b`/`feaa2b5`), PM verification entries, evidence, and the final documentation-only re-dispatch. The worktree and `git diff --check` are clean.
- Appendix A alignment is otherwise verified: October id/label; 5–9 and 13–16 October; free 10–12 October with Fiesta Nacional on the 12th; nine visit days; two 2.5-day immersion schools plus five visit schools; all eight experts with Sandra Entrena as Encargada de Innovación; all 13 objectives; day structure; seven includes/four excludes; Barcelona lodging area; and the three Madrid schools. `COHORT_PRICE_TOTAL` is absent, the commercial aggregate has no `total`, and 560/1.560 occur only in regression assertions/comments documenting the retired model—not in runtime cohort data or built public output.
- The homepage card is correct in the fresh production artifact: one “Próxima cohorte” card, exact October headline sourced from `cohort-public.ts`, no price, and no literal cohort date in `pages/index.tsx`.
- Independent gate evidence on final head: targeted A1 Vitest 34/34; type-check clean; ESLint clean with zero warnings; full Vitest 233 files / 3,479 tests; production build successful with 156 static pages; `node scripts/check-price-leak.mjs` green over 266 client files after that build; pgTAP 4 files / 26 tests on a freshly reset local stack; mandatory Playwright 8/8 with both required specs present and no skips. `lint:testid` remains the documented advisory baseline and fails repo-wide on pre-existing rule-loading errors/warnings; A1's new homepage element does carry `data-testid="cohort-headline"`.
