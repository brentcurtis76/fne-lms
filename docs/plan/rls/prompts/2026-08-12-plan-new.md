# Prompt — `/plan-new RLS` (2026-08-12)

The prompt that produced `docs/plan/rls/PLAN.md`, committed verbatim per the workflow.
Invoked as `/plan-new` with the arguments below; the skill supplied the `SESSION: RLS · plan · PM`
framing and the canonical SOP §2.1 requirements.

---

RLS

Run this from `/Users/brentcurtis/dev/wt/rls-public` (branch `fix/rls-public`, clean at
`c1dcf314`). Confirm with `git rev-parse --show-toplevel` and `git branch --show-current`
before reading anything.

## The discovery phase is already done — do not repeat it

`docs/plan/reviews/rls-public-allowlist-r1-findings.md` (200 lines, on THIS branch and
nowhere else — it is not on `main`) is a completed DISCOVERY output in the lean overlay's
exact sense: evidence plus a revised contract, no implementation smuggled in. It was
produced against production read-only and corrected after an independent review.

Read it first and treat it as the primary input. It already carries:

- §1 reachability, proven with count-only REST probes plus two discriminating controls;
- §3 a per-table classification into A1 dead (14) / A2 service-role-only (1) / A3 broken
  reader (1) / B user-facing (6), with call sites and client identification;
- §4 `profiles_role_backup`;
- §5 a proposed remedy order with per-table policy contracts and the unresolved rulings
  each one needs;
- §6 an explicit **proven vs inferred** split.

**Do not re-derive what §6 marks proven, and do not promote anything it marks inferred to
a fact.** Your job is to turn this into a phased plan with acceptance criteria, not to
re-audit. Where you disagree with it, say so and show the evidence — it has been wrong
once already and was corrected.

**First action: `git mv docs/plan/reviews/rls-public-allowlist-r1-findings.md` into the
plan root's `reviews/` so the workstream is self-contained**, and record the move in the
ledger as round r1's discovery artifact.

## One ruling from the ZOOM PM, carried over with the evidence behind it

**Split the document's "Step 0" in two.** As written it bundles a mechanical
`REVOKE EXECUTE` with a full actor-derivation redesign of six SECURITY DEFINER functions.
Those have wildly different risk and must not share a phase. The caller audit that makes
the split clean was run on `main` @ `43999499`:

| Function | Repository callers | Consequence |
|---|---|---|
| `has_transformation_access`, `get_available_assignment_templates`, `cleanup_propuesta_rate_limits` | **none found** | revoke `EXECUTE` from `PUBLIC`, `anon` **and** `authenticated` |
| `submit_quiz` | **live** — `lib/services/quizSubmissions.js:75` | revoke `PUBLIC` + `anon` only; keeping `authenticated` preserves the student quiz flow |
| `create_full_learning_path` (`lib/services/learningPathsService.ts:69`), `batch_assign_learning_path` (`:313`), `start_learning_path_session` (`pages/api/learning-paths/session/start.ts:86`), + `update_full_learning_path`, `end_learning_path_session` | **live** | same — `PUBLIC` + `anon` only |

So the **first executable phase** should be the largest risk reduction per line available:
revoke anonymous/`PUBLIC` execution on the nine exposed RPCs, and `REVOKE` +
`ENABLE ROW LEVEL SECURITY` (no policy) on `profiles_role_backup`. No policy design, no
application redesign, no behaviour change for any authenticated caller. It closes every
proven anonymous write path and removes the privileged-role roster from the internet.

The actor-derivation redesign of `submit_quiz` and the five learning-path functions is the
**last** phase, not the first — it needs enrollment/ownership rules, a trusted scoring
source (§5 identifies `blocks.payload`), and its own pgTAP matrix.

Everything between follows the document's own order: the six empty student tables (free
today precisely because they are empty — this is the Ley 21.719 item), then the remaining
dead/service-only tables, then the `qa_tester_time_logs` retirement, then Group B one
table or coupled pair at a time with `modules` last.

## Add one item the document does not have

`public.has_global_workspace_access` (`supabase/migrations/00000000000000_baseline.sql:3987`)
is `SECURITY DEFINER` with an unqualified `user_roles` reference and **no fixed
`search_path`**, and it is granted to `anon` (`:23982`). It is not decorative: it sits
inside the `USING` / `WITH CHECK` of three live policies on `public.community_meetings`
(`:18196`, `:18203`, `:18210`, all `TO authenticated`). Same class as the invoker-rights
trap §5 already records for `validate_assignment_instance_course`. Found during ZOOM Z7-1's
Codex review, has no owner, and the Zoom plan's Z6 will build directly on those policies.
Place it in whichever phase you judge correct and give it a named criterion.

## Blocking questions for Brent — none of them block the first phase

State these in the plan as owner decisions with the phase each one gates:

1. Does anything **outside this repository** read `menu_permissions` (104 rows) or
   `deleted_courses` (12 rows)? A Retool/Metabase board, a manual query, a script. §6 is
   explicit that the audit cannot see these. Gates the dead-table phase.
2. Are all 17 `instructors` rows publishable profiles, or are some internal-only? Gates
   the `instructors` policy.
3. May `consultor` assign/revoke transformation access? `isUserAdmin()` treats it as admin
   while the route copy says "solo admins". Gates that table's write policy.
4. Enrollment status semantics: do paused/dropped/expired/completed enrollments retain
   `modules` access? Gates the `modules` phase, which is the riskiest.

## Constraints

- **Separation of duties.** Codex authored the diagnosis, so Codex is the independent
  **reviewer** of this plan and of every phase — never the implementer. Implementation is a
  fresh Claude Code executor in this worktree.
- **Never touch the production database.** All evidence to date is read-only and must stay
  that way; §6's one inferred claim (that anon can write the 22 tables) is not to be
  verified by writing. Migrations are applied to production by Brent, separately, and per
  the Zoom plan's §0.1(d) a phase with migrations is not closed until the production schema
  is verified read-only afterwards.
- **Repo hard rules outrank the plan**: additive schema only, no `DROP`/`TRUNCATE`, and a
  migration disabling RLS is blocked by both a hook and CI. Note that §4's recommendation
  to eventually retire `profiles_role_backup` is therefore a separate owner decision, not a
  phase.
- **Rollback may never be "disable RLS" or "restore the grants"** — both restore the
  vulnerability. Say what rollback actually is for each phase.
- **Every change set removes its tables from the `supabase/tests/001-rls-enabled.sql`
  allowlist in the same commit as its migration and pgTAP coverage.** The allowlist reaching
  `{}` is the workstream's definition of done.
- **Do not touch other worktrees.** `/Users/brentcurtis/dev/wt/zoom-hours` (ZOOM Z7, live),
  `/Users/brentcurtis/dev/wt/zoom-embed` (Z3b's starting point), `wt-a6a`, `wt-a6r`, `wt-a9`.
- **Gate hazard any executor here will hit:** `npm test` reports a green 254 files / 6,575
  tests in the base checkout while silently skipping 51 jsdom files, because `canvas`'s
  native binding fails to load and Vitest 0.34 drops them with exit 0. Any phase contract
  must require proving jsdom initialises (`node -e "require('jsdom')"`, plus a run whose
  summary shows non-zero `environment` time) before a test count is reported as evidence.

## When done

Commit this prompt into the plan root's `prompts/` alongside the plan. Report the files
created, the evidence gathered, the first phase's risk tier, and every assumption still
marked `UNVERIFIED`. Add a note to `~/.claude/agent-workflow/workstreams.md` recording that
this workstream's plan lives only on `fix/rls-public` and not on `main` — three workstreams
have already lost sessions to exactly that, and the registry exists to prevent a fourth.
The plan is not frozen until one independent Codex plan review passes.
