SESSION: ZOOM · Z2 · r10 · EXEC

You are the executor for the Zoom integration in the GENERA repo, **phase Z2, chunk
Z2-5, round r10** (rounds are numbered per phase, so a fresh chunk still advances the
round). You have no prior context. Everything you need is below or at the exact paths
given.

**Your report IS the deliverable.** A round on this phase once completed its work, pushed
it, and never reported — leaving the commit invisible to the ledger the whole process
depends on and costing an extra round to reconstruct. Push if you like, but the round is
not done until you emit the report in §8. Running low on context? Emit it early and say so.

---

## 0. Where you work

**BRANCH: `feat/zoom-sess`, head `118a9db8`**, already pushed. Worktree at
**`/Users/brentcurtis/dev/wt/zoom-sess`** (`node_modules` and `.env.local` in place).
Never put a checkout under `~/Documents` — it is iCloud-synced and evicted the shared
`.git` on 2026-08-05, killing every worktree there at once. Live clone:
`/Users/brentcurtis/dev/fne-lms`.

Confirm first: `git -C /Users/brentcurtis/dev/wt/zoom-sess rev-parse HEAD` must print
`118a9db8…`. If it does not, **stop and report** — do not improvise.

Commit on top; no rebase, no amend, no force-push, no PR.

Overrides of the generic `/exec` skill: **do not write `docs/plan/zoom/LEDGER.md`**
(PM-only here); **never pipe a gate command through `tail`**; never merge or push to
`main`; **never run `vercel` or trigger a deploy**.

**`npm run test:db` may be unavailable** — Docker's engine has been failing to boot on this
host for three rounds. Attempt it. If it fails, report **NOT RUN with the real error**; do
not skip silently, do not adopt an earlier session's `Files=9, Tests=374` as your own, and
do not try to fix Docker. This chunk must add no migration, so `test:db` is a regression
check here, not a proof of new behaviour.

---

## 1. The problem — PM-verified against `118a9db8`

Plan §11: *"every reader of session hours is inventoried and aligned to the ledger/planned-snapshot
source of truth. Known offenders: `lib/services/school-hours-report.ts:200` and
`pages/api/sessions/reports/analytics.ts:233,327` display `actual_duration_minutes` (which is
verifiably only ever a copy of scheduled duration written at finalize)."*

**The PM re-verified that "verifiably" rather than repeating it.** Every write of
`actual_duration_minutes` in the repo:

- `pages/api/sessions/index.ts:230` — set to `null` at creation.
- `pages/api/sessions/[id]/finalize.ts:96` —
  `const actualDuration = session.actual_duration_minutes ?? session.scheduled_duration_minutes;`
  then written at `:108`.

**There is no third write.** Nothing measures anything. So a column named "actual" holds
the *scheduled* duration, and every surface presenting it as real elapsed time is telling
the user something untrue. `pages/api/sessions/reports/analytics.ts:326-328` sums it into
a field literally called `total_hours_actual`.

Meanwhile the real source of truth — `contract_hours_ledger.hours`, the number that bills
the school and pays the consultant (§11, Brent 2026-07-13) — is **already being fetched**
by one of the two offenders and used only for status.

### Verified anchors

| Anchor | What is there |
|---|---|
| `lib/services/school-hours-report.ts:178-181` | **Already queries `contract_hours_ledger`**, selecting `session_id, status, is_over_budget` into `ledgerMap`/`overBudgetMap`. |
| `lib/services/school-hours-report.ts:200-202` | `const durationMinutes = s.actual_duration_minutes ?? s.planned_duration_minutes ?? 0; const hours = durationMinutes / 60;` — the offending derivation, feeding `hours` at `:214`. |
| `pages/api/sessions/reports/analytics.ts:326-328` | `totalHoursActual` = sum of `actual_duration_minutes` / 60. **This file never queries the ledger at all** (verified: zero `contract_hours_ledger` matches). |
| `pages/api/sessions/reports/analytics.ts:24`, `:373`, `:573` | The response key `total_hours_actual`. |
| `pages/consultor/sessions/reports.tsx:57` | The **frontend consumer** of `total_hours_actual`. |
| `contract_hours_ledger` | `hours numeric(6,2)` `CHECK (hours > 0)`, `status IN ('reservada','consumida','devuelta','penalizada')`, plus `planned_minutes_snapshot` (Z1b) and `admin_override`. |

---

## 2. SCOPE — chunk Z2-5: the hours-consumer audit

**S1 — one shared helper** for "billable hours from a ledger row", in
`lib/services/hour-tracking.ts` (where the ledger already lives) or a sibling module if
that file argues otherwise. Every consumer reads billable time through it — **one
implementation, no second opinion**.

**S2 — retarget `school-hours-report.ts`.** Extend the ledger select it *already makes* at
`:178-181` to carry `hours`; derive `hours` at `:200-202` through the S1 helper. **Do not
add a second query** — the round trip is already there.

**S3 — retarget `analytics.ts`.** It must start reading the ledger for the sessions it
already loads, and `total_hours_actual` must be derived through the same S1 helper.

**S4 — deprecate `actual_duration_minutes` in code, not in the schema.** Mark it deprecated
at `lib/types/consultor-sessions.types.ts:98` with a comment saying what it actually
contains and that it must never be displayed as "actual". `finalize.ts` keeps writing it
(compatibility). **No migration, no column drop.**

**S5 — the audit half, which is a real deliverable.** §11 says *every reader of session
hours is inventoried*. Produce that inventory in the review-request file: every site that
reads `actual_duration_minutes`, `scheduled_duration_minutes`, `planned_minutes_snapshot`
or `contract_hours_ledger.hours` and presents it to a human — with, for each, a verdict of
**retargeted / already correct / deliberately left (why)**. Two known offenders are named
above; **find the rest yourself and say how you searched.** If you find a third offender,
report it — do not silently widen scope to fix it.

---

## 3. PM RULINGS — do not choose differently

1. **`effective_minutes` does NOT exist yet.** §11's `coalesce(effective_minutes/60.0, hours)`
   is the Z7 end state; that column is Z7's additive migration. Write the S1 helper to read
   `hours` today and leave a **named SEAM comment** describing exactly what Z7 changes —
   the same pattern `meeting-provision.ts` used for `is_zoom_managed`, which Z2-1 then closed
   cleanly. **Do not add the column, do not add a migration.**
2. **Keep the `total_hours_actual` response key.** `pages/consultor/sessions/reports.tsx:57`
   consumes it; renaming breaks that page and this chunk is not a frontend refactor. Change
   what feeds it, not what it is called. **But** inspect the UI label that renders it and
   fix any wording claiming "real"/"actual" elapsed time — the number stops meaning that.
   Report what the label said and what you changed it to.
3. **A session with no ledger row must not silently become zero.** Legacy sessions and any
   never-approved session have no ledger entry, and a naive retarget would drop their hours
   out of a school's report with no signal. Decide the fallback deliberately, document it at
   the helper, and **test it explicitly**. This is the highest-risk behaviour change in the
   chunk — a school report that quietly under-reports is worse than one that errors.
4. **Cancellation-clause rows are not consumption.** `devuelta` and `penalizada` are a
   separate flow (§11). Whatever the helper does with them, be explicit and test it; do not
   let a cancelled session's clause row silently read as billable time.
5. **No migration.** If you believe one is needed, **stop and report FINDINGS** — Z2-3a's
   migration is still unverified against production, and a second unapplied one would
   compound exactly the failure that broke session approval in Z1b.
6. **Do not touch the sealed chunks' source** (Z2-1, Z2-2a, Z2-2b, Z2-3b) or Z2-3a's RPC.

---

## 4. EXPLICITLY OUT OF SCOPE

The Z7 admin override, `effective_minutes`, `session_hour_overrides`, the comparison UI.
Zoom attendance and the planned-vs-Zoom delta (Z7). `meeting_sync`/`meeting_delete`
(sealed). Notifications, iCal, reminders, dual-zone (Z2-4). The workspace "Sesiones" tab.
Any UI beyond the one label in ruling 2. Any migration. Any frontend refactor.

---

## 5. ACCEPTANCE CRITERIA

- **[A1]** Exactly one helper computes billable hours from a ledger row, and both consumers
  call it. Prove there is no second implementation and say how you checked.
- **[A2]** `school-hours-report.ts` derives `hours` from the ledger, via the **existing**
  query at `:178-181` — no additional round trip. Assert the query shape.
- **[A3]** `analytics.ts` derives `total_hours_actual` from the ledger; the response key is
  unchanged and `pages/consultor/sessions/reports.tsx` still type-checks.
- **[A4]** A session with **no ledger row** behaves per ruling 3 — asserted, in both
  consumers, with the documented fallback.
- **[A5]** `devuelta` / `penalizada` rows behave per ruling 4 — asserted in both consumers.
- **[A6]** An `admin_override` ledger row's `hours` flows through unchanged (it is already
  the adjusted value today; Z7 only adds `effective_minutes` beside it).
- **[A7]** `actual_duration_minutes` is deprecated in the type with an accurate comment, is
  still written by `finalize.ts`, and **is no longer read by either consumer**. Prove the
  second half with a grep in your report.
- **[A8]** The §11 inventory exists in the review-request file, with a verdict per site and
  a statement of how you searched.
- **[A9]** Four TS gates green. PM-measured baselines at `118a9db8`: type-check 0 · lint 0
  (`--max-warnings=0`) · **4479 passed / 274 files** · build 0. Nothing may fall.
  `test:db` per §0 — run it or report the real error.

## 6. TESTS

Extend `__tests__/api/sessions/session-reports-analytics.test.ts` (it already fixtures
`actual_duration_minutes` at `:77,88` — those fixtures must move to ledger rows) and add
coverage for `school-hours-report.ts`. Doubles must be **filter-aware**: a canned row
resolves only when the recorded `.eq()`/`.in()` filters match the scenario. A PM probe
earlier in this phase showed table-name-keyed doubles leaving a security filter entirely
unguarded at 39/39 green — see `__tests__/api/meet/session-join.test.ts` for the shape.

**Fail-on-old is available and required** for [A2] and [A3]: both consumers read
`actual_duration_minutes` today. Revert each consumer alone, re-run, paste the real failure
output, restore, and prove byte-identity by blob hash.

TEST COMMAND (verbatim, from `/Users/brentcurtis/dev/wt/zoom-sess`):

```bash
npm run type-check && npm run lint && npm test && npm run build
```

```bash
npm run test:db
```

---

## 7. RULES

Surgical changes only; match existing patterns. **es-CL** for user-facing copy; English for
code, comments and commits. Do not grade your own work in prose — show the diff and the raw
output. No new time construction (`lib/utils/session-timezone.ts` is the only module allowed
to build session instants). Student PII never enters prompts, commits, logs or fixtures.
**Never touch the production database.** If tests are red, iterate up to 3 times, then report
`STATUS: BLOCKED` with the real error. If a §3 ruling is wrong against the code, or an anchor
in §1 does not match the repo, **STOP CODING** and report `STATUS: FINDINGS` — executors on
this workstream have been right to do that three times, and every time the fault was the PM's.

Append a Z2-5 section to `docs/plan/zoom/reviews/fase-5-review-request.md`; do not rewrite
earlier sections.

## 8. REPORT — the deliverable

Do **not** write `LEDGER.md`. Output exactly this and nothing after it:

```
## EXECUTOR REPORT — Z2 (chunk Z2-5) round r10
STATUS: COMPLETE | BLOCKED | FINDINGS
CONTEXT PRESSURE: comfortable | tight | ran out — <one line: at what point?>
BRANCH: feat/zoom-sess   COMMITS: <sha…>
FILES CHANGED: <path (+x/-y) …>
WHAT I DID: <bullets, one per acceptance criterion, mapped to A1…A9>
HOURS INVENTORY: <every reader found, its verdict, and how you searched>
NO-LEDGER-ROW FALLBACK: <what you chose, why, and where it is tested>
TEST COMMAND: <verbatim>
TEST OUTPUT: <verbatim tail, unedited — and for test:db either the real result or the real error>
FAIL-ON-OLD PROOF: <[A2] and [A3]: the reverted files, real failure output, blob-hash revert check>
ACCEPTANCE CRITERIA: <A1…A9: met / not met / partially — with reason>
DEVIATIONS FROM PROMPT: <or "none">
ASSUMPTIONS MADE: <or "none">
NOT DONE / OPEN:
```
