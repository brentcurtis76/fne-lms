SESSION: ZOOM · Z2 · r28 · EXEC

You are the executor for the Zoom integration in the GENERA repo, **phase Z2, remediation
chunk Z2-S8, round r28**. You have no prior context.

**This is the last code round of phase Z2.** It carries **Sol's items 11 and 12a**, plus the
coverage gap three earlier rounds flagged and none was scoped to fix. **Item 12b — the
staging run against a real Zoom tenant — is the owner's and is not yours.**

---

## 0. Where you work

**BRANCH: `feat/zoom-sess`, head `3e33968d`**, already pushed. Worktree at
**`/Users/brentcurtis/dev/wt/zoom-sess`**. Never put a checkout under `~/Documents`.
Live clone: `/Users/brentcurtis/dev/fne-lms`.

Confirm first: `git rev-parse HEAD` must print `3e33968d…`. If not, **stop and report**.

Commit on top; no rebase, no amend, no force-push, no PR. **Do not write
`docs/plan/zoom/LEDGER.md`** (PM-only). **Never pipe a gate command through `tail`.** Never
merge or push to `main`. Never run `vercel`. **NEVER TOUCH THE PRODUCTION DATABASE** — and
note this round changes the **e2e seeder**, which by design refuses a non-local URL. Do not
weaken that refusal.

PM baselines at `3e33968d`: type-check 0 · lint 0 · **4726 passed / 283 files** · build 0 ·
**`test:db` Files=11, Tests=466, PASS**, and the mandatory e2e list **76 passed** with the
no-skip guard green. If `supabase db reset` hangs, use `npx supabase`.

---

## 1. THE COVERAGE GAP — the largest untested surface in the phase

Rounds r23, r25, r26 and r27 each recorded the same thing: **`scripts/ci/seed-e2e-zoom.mjs`
never sets `is_zoom_managed`**, and `scripts/ci/e2e-fixtures.json` carries no managed
session. `is_zoom_managed` is `NOT NULL DEFAULT false`, so **every seeded session is
unmanaged**, and therefore **no e2e test has ever exercised a Zoom-managed session** —
not the join affordance, not the §14 kill switch, not the managed platform links r26 added,
not the managed disclosure r27 added.

That is why the mandatory specs kept passing unchanged through four rounds that changed
managed behaviour. **Those rounds are unit-tested and integration-tested; they have never
been exercised end to end.**

**THE RULING — add, do not flip.** Seed a **new** managed session alongside the existing
fixtures. **Do not change `is_zoom_managed` on a session an existing spec already asserts
against** — every current assertion must keep passing untouched, and `[Q5]` exists to prove
it. A managed session has `meeting_link = NULL` by design; that is the point, not a defect
to work around.

Then cover, end to end, at least: **the managed join affordance appears for a persona who
may join**, and **it does not appear — with no raw link anywhere in the document — for a
persona who may view but not join.** If the §14 kill switch is reachable from e2e, cover it;
if it is not, **say so plainly** rather than leaving silence.

---

## 2. ITEM 11 — `PROJECT_STATE.md`, truthfully

Update it for Z2. **Truthfully is the operative word**, and it is a repo rule that this file
carries evolving state. It must say what actually happened, including:

- What Z2 delivered, and what it did **not** — the embed is Z3; attendance, hours comparison
  and the override UI are Z7.
- That the phase went to independent review and came back **`REQUEST CHANGES` with twelve
  items**, and that they were remediated across rounds r21–r28.
- **The open items that survive**, in plain terms — chiefly that **no code in this phase has
  ever run against a real Zoom tenant**, that the dial-in wire shape is documentation-based,
  and that the migrations are not applied to production.
- Final test evidence: the five gate figures and the mandatory e2e result, at your final
  commit.

**Do not oversell it.** A future reader deciding whether to trust this phase should be able
to tell from this file alone what is proven and what is merely green.

---

## 3. ITEM 12a — the complete gate run

Run every required gate at your final commit and paste the real output: type-check, lint,
unit, build, `test:db`, and **the full mandatory e2e list plus the no-skip guard**. This is
the evidence the phase closes on, so it must be from the final tree, not an earlier one.

---

## 4. EXPLICITLY OUT OF SCOPE

- **Item 12b — the staging checklist against a real audio-plan Zoom tenant.** It needs real
  credentials and is the owner's. Do not attempt it, do not stub it, and do not describe it
  as done.
- Everything r21–r27 sealed. Do not change page-visibility tiers, the closed set, the
  disclosure predicate or any authorization rule.
- The five standing unruled items (`SESSION_STATUS_FALLBACK`, `total_hours_actual`'s name,
  `create.tsx`'s UTC-vs-Chile `min`, the four order-dependent suites,
  `'edit_approval_blocked'`). **Leave them; they are recorded deliberately.**
- No new feature work. If you find a defect, **report it — do not fix it.** This round exists
  to make the phase's state legible, and a surprise change here would undo that.

---

## 5. ACCEPTANCE CRITERIA

- **[Q1]** A managed session is seeded, **additively**. `e2e-fixtures.json` and the seeder
  agree, and the seeder still refuses a non-local database URL.
- **[Q2]** E2E: the managed join affordance **appears** for a persona who may join.
- **[Q3]** E2E: it **does not appear** for a persona who may view but not join, **and no raw
  link is anywhere in the document** — the standard the denied tier already holds.
- **[Q4]** The §14 kill switch is covered end to end, **or** its unreachability from e2e is
  stated plainly in the report and the review file.
- **[Q5]** **Every pre-existing e2e assertion passes unchanged.** The mandatory list runs with
  the no-skip guard green. If one must move, **stop and report `FINDINGS`.**
- **[Q6]** `PROJECT_STATE.md` reflects §2 — including the open items, in plain language.
- **[Q7]** **All five gates plus the mandatory e2e list, at the final commit**, pasted
  verbatim. Unit and `test:db` counts may rise; nothing may fall.

TEST COMMAND (verbatim, from `/Users/brentcurtis/dev/wt/zoom-sess`):

```bash
npm run type-check && npm run lint && npm test && npm run build
```

```bash
npm run test:db
```

```bash
npm run e2e
```

---

## 6. REVIEW-REQUEST FILE

Append a final **Sol remediation — round r28 (items 11 and 12a)** section to
`docs/plan/zoom/reviews/fase-5-review-request.md`. **Append; do not rewrite.**

**This is the last executor section the independent reviewer will read.** Make the NOT DONE
list complete and honest — it is more useful to that reviewer than anything else in the file.

---

## 7. RULES AND REPORT

Surgical changes only. **es-CL for user-facing copy**, English for code and commits.
**Synthetic data only — never real user or student PII** (Ley 21.719). Seeded personas are
synthetic adults; no minor data anywhere.

If tests are red for a reason other than a deliberate probe, iterate up to 3 times, then
report `STATUS: BLOCKED`. **If any two criteria conflict, STOP and report `STATUS: FINDINGS`.**

Do **not** write `LEDGER.md`. Output exactly this and nothing after it:

```
## EXECUTOR REPORT — Z2 (Sol remediation, items 11 and 12a) round r28
STATUS: COMPLETE | BLOCKED | FINDINGS
CONTEXT PRESSURE: comfortable | tight | ran out — <one line: at what point?>
BRANCH: feat/zoom-sess   COMMITS: <sha…>
FILES CHANGED: <path (+x/-y) …>
WHAT I DID: <bullets, one per acceptance criterion, mapped to Q1…Q7>
E2E COVERAGE ADDED: <what the managed session now proves end to end, and what it still does not>
TEST COMMAND: <verbatim>
TEST OUTPUT: <verbatim — all five gates AND the mandatory e2e list with the guard, at the final commit>
ACCEPTANCE CRITERIA: <Q1…Q7: met / not met / partially — with reason>
DEVIATIONS FROM PROMPT: <or "none">
ASSUMPTIONS MADE: <or "none">
NOT DONE / OPEN: <the complete, honest list — the reviewer's most useful section>
```
