SESSION: ZOOM · Z2 · r29 · EXEC

You are the executor for the Zoom integration in the GENERA repo, **phase Z2, remediation
chunk Z2-S9, round r29**. You have no prior context.

Phase Z2 went to independent review a second time (Sol, 2026-08-07) and came back
**`REQUEST CHANGES`, narrowly**: nine of twelve items closed and closed well; **four MAJOR
findings and six MINOR** remain. **The PM verified all four MAJORs against the code before
writing this — every one holds.** This round fixes them.

---

## 0. Where you work

**BRANCH: `feat/zoom-sess`, head `bf7e7043`**, already pushed. Worktree at
**`/Users/brentcurtis/dev/wt/zoom-sess`**. Never put a checkout under `~/Documents`.
Live clone: `/Users/brentcurtis/dev/fne-lms`.

Confirm first: `git rev-parse HEAD` must print `bf7e7043…`. If not, **stop and report**.

Commit on top; no rebase, no amend, no force-push, no PR. **Do not write
`docs/plan/zoom/LEDGER.md`** (PM-only). **Never pipe a gate command through `tail`.** Never
merge or push to `main`. Never run `vercel`. **NEVER TOUCH THE PRODUCTION DATABASE.**

PM baselines at `ea60941e`/`bf7e7043`: type-check 0 · lint 0 · **4726 passed / 283 files** ·
build 0 · **`test:db` Files=11, Tests=466, PASS** · **mandatory e2e 88 passed, 7 specs, guard
green**. **No migration expected** — if you think one is needed, stop and report `FINDINGS`.

---

## 1. R-A — M1: a checkpointed meeting survives, all green

**PM-verified, and the chain is exact.** Interleaving: reservation INSERT → `createMeeting`
succeeds → post-create checkpoint lands → the attempt dies **retryably** (a crash, or merely
`readSession` throwing once) → the session is cancelled → `meeting_delete` claims, finds the
**numberless** `pending` row and **skips the Zoom call entirely**
(`lib/zoom/jobs/meeting-delete.ts:529` — `if (row.zoom_meeting_number !== null && …)`), marks
it `deleted` and **clears `last_error`** (`:547`), and completes **green** → the provision
retry reaches the eligibility gate, where `strandedCheckpoint`
(`lib/zoom/jobs/meeting-provision.ts:2131-2134`) requires
`ZOOM_MEETING_ACTIVE_STATUSES.includes(held.status)` — and the row is now `deleted`, so it is
**false** — and the job walks away terminal under `session_ineligible`, **a reason that reads
as "handled."**

**End state: cancelled session, live Zoom meeting, green delete job, green provision job, no
loud marker anywhere.** That is the item-5 harm verbatim, in exactly the crash-shaped case the
third compensation site was added to close. It also **disproves** r27's stated premise that
r24's compensation is unconditional — the PM's own dossier flagged that as unproven; Sol
proved it false.

**THE FIX (Sol's R-A, adopted).** Extend the eligibility-gate compensation to fire when
`checkpointHere.meetingId === held.id && held.zoom_meeting_number === null` **and** `held.status`
is `cancelled`/`deleted` — a row retired by a delete that **provably never called Zoom**,
because it was numberless. Issue the compensating Zoom DELETE; **skip the release/republish
for an already-retired row**; **keep the ambiguous-park precedence unchanged**.

**Correct the module header's "no third window" claim** — it is currently false.

## 2. R-B — M2 + m3: three more swallowed reads, in the function r27 repaired

**PM-verified.** `lib/services/school-hours-report.ts` destructures only `data` on **three**
reads: `schools` (`:85`), `clientes` (`:94`), `contratos` (`:105`). A failed `clientes` read
coalesces to `[]` and the function returns `{ programs: [] }` — **a 200 whose whole-school
report shows zero contracts and zero hours, pixel-identical to a school with nothing billed.**
A failed `schools` read becomes a 404 "Escuela no encontrada" for a school that exists.

That is the same defect r27 was dispatched to fix, one step above the reads it fixed, in the
same function. **The PM's prompt quoted specific line anchors and the round stayed on them —
that framing is the PM's error, not the round's.**

**THE FIX.** Destructure and **throw** on `.error` for all three, copying the convention the
function already uses (`console.error` with the PostgREST object, es-CL message naming the
school). **Legitimate-empty must still work**: a school with genuinely no clientes or no
contratos still returns a valid empty report.

## 3. R-C — M3: a failed read gives a session the wrong money status

**PM-verified.** `lib/services/hour-tracking.ts:426`: `const { data: hourType } = await
serviceClient.from('hour_types')…` — no `.error`. On a failed read `modality` silently stays
`'online'`, so a **`presencial`** session cancelled 120 h out is evaluated under online
thresholds — clause 1 (`devuelta`, consultant unpaid) instead of clause 4 (`penalizada`,
consultant paid). **A durably wrong money status written to the ledger row.**

**PM RULING, which Sol explicitly left to the PM and asked to be made explicitly: FIX IT NOW.**
Pre-existing is not a defence when the harm is a wrong billing status persisted to a ledger
row, and r27 already set the precedent of fixing the class rather than the quoted line.
**Throw on the failed read.**

**`getAvailableHours` (`hour-tracking.ts:223`, Sol's m2) is NOT in this round** — it is
display-only, it does not change billed amounts, and it goes to the standing backlog. Do not
touch it.

## 4. R-D — M4: `PROJECT_STATE.md` says four migrations; the branch carries seven

**PM-verified, and this is the finding that forced the verdict.**
`git diff --name-only main...feat/zoom-sess -- supabase/migrations/` returns **seven**:

```
20260804120000_zoom_managed_intent.sql
20260805120000_reschedule_hours_rpc.sql
20260806120000_zoom_dial_in_numbers.sql
20260807120000_backfill_zoom_dial_in_numbers.sql
20260808120000_session_reschedule_atomic.sql
20260809120000_fix_bucket_summary_fanout.sql
20260809120100_reschedule_rpc_uses_bucket_summary.sql
```

`PROJECT_STATE.md` says **"cuatro migraciones"** — in the phase summary, in the
deploy-blocking debt, and in the phase-close checklist. **An operator following that checklist
applies four and misses `apply_session_reschedule` and both bucket-summary functions.
Production code then calls RPCs that do not exist there, and every duration-relevant
reschedule 500s.** That is the Z1b closing defect **re-armed by the very document that records
the permanent rule against it** (`PROJECT_STATE.md:13-14`).

**THE FIX.** Correct **every** occurrence, and — per Sol, and the PM agrees — **replace the
count with an explicit filename manifest**, so the production apply is checked off per file.
A count can drift; a list of filenames cannot drift silently. **DoD:**
`grep -c "20260808\|20260809" PROJECT_STATE.md` ≥ 3, and the manifest matches
`git diff --name-only main...HEAD -- supabase/migrations/` exactly.

## 5. R-E — the MINORs worth closing now

- **m1** — `lib/zoom/jobs/meeting-provision.ts:2735` and the review file claim a failed
  compensation "keeps `pending` so the §9 EXCLUDE keeps blocking the host". **True for two of
  the three triggers**; on a CAS-miss-on-retired-row the row is already `cancelled`/`deleted`,
  so the host is **not** blocked while a possibly-live meeting stands. **Qualify the claim
  where it is written.** Do not restructure the parking.
- **m4** — `pages/api/sessions/edit-requests/[eid].ts:229` passes no `if_updated_at`, so that
  path keeps the racy JS old-value comparison while the RPC's purpose-built, pgTAP-proven
  guard goes unused. **PM ruling: pass `session.updated_at`.** It is free and the guard exists.
  Handle the conflict return the way the admin PUT does.
- **m6** — five shipped artifacts cite "recorded in `PLAN.md` §5", but the **branch copy** still
  carries the pre-amendment sentence; the amendment and the r1–r28 ledger live on `main`.
  **PM ruling: do NOT merge `main` into the branch this round** — it would muddy the fix diff
  Sol re-reviews. **Record the split explicitly in the review file** so a branch-only reader
  knows where the amendment is and that merging brings it.

---

## 6. EXPLICITLY OUT OF SCOPE

`getAvailableHours` (m2) · the five standing unruled items, **including
`'edit_approval_blocked'`** — Sol notes its audit insert swallows its error too; that is the
same class and is **ticketed, not fixed here** · item 12b · anything r21–r28 sealed that these
five fixes do not touch · no new features, no migration, no schema change.

---

## 7. ACCEPTANCE CRITERIA

- **[R1]** The M1 interleaving is driven **deterministically** — checkpoint lands, the **real**
  `meeting_delete` retires the bare reservation green, the provision retry runs — and the fake
  holds **no live meeting** afterwards. A test that merely runs both jobs is not this.
- **[R2]** **Mutation probe on R-A**: restore the ACTIVE-only guard, show `[R1]` failing with
  the live-meeting diff in the output, revert, prove byte-identity by blob hash.
- **[R3]** The ambiguous-park precedence is unchanged, asserted.
- **[R4]** Each of the three `school-hours-report.ts` reads fails the request when it errors —
  asserted **per read** — and the legitimate-empty cases still return a valid empty report.
- **[R5]** A failed `hour_types` read fails the cancellation instead of defaulting to `online`,
  asserted; and a **successful** read still classifies `presencial` and `both` exactly as
  today.
- **[R6]** `PROJECT_STATE.md` carries the seven-file manifest; the two greps in §4 pass.
- **[R7]** `if_updated_at` is passed on the edit-request path, a stale value is rejected with
  nothing written, and the existing edit-request tests still pass.
- **[R8]** **All five gates green plus the mandatory e2e list and the no-skip guard.** Unit
  count may rise; nothing may fall. `test:db` should stay at Files=11, Tests=466 unless you add
  pgTAP — if it moves for any other reason, stop and report.

TEST COMMAND (verbatim, from `/Users/brentcurtis/dev/wt/zoom-sess`):

```bash
npm run type-check && npm run lint && npm test && npm run build
```

```bash
npm run test:db
```

```bash
npx playwright test $(node scripts/ci/e2e-mandatory.mjs --list) --project=chromium
node scripts/ci/e2e-mandatory.mjs --check test-results/e2e-results.json
```

---

## 8. REVIEW-REQUEST FILE

Append a **Sol re-review remediation — round r29 (M1–M4, m1, m4, m6)** section to
`docs/plan/zoom/reviews/fase-5-review-request.md`. **Append; do not rewrite.** Include the m6
note about where the §5 amendment lives.

---

## 9. RULES AND REPORT

Surgical changes only. **es-CL for user-facing copy**, English for code and commits.
**Synthetic data only — never real user or student PII** (Ley 21.719).

If tests are red for a reason other than a deliberate probe, iterate up to 3 times, then report
`STATUS: BLOCKED`. **If any two criteria conflict, STOP and report `STATUS: FINDINGS`.**

Do **not** write `LEDGER.md`. Output exactly this and nothing after it:

```
## EXECUTOR REPORT — Z2 (Sol re-review remediation) round r29
STATUS: COMPLETE | BLOCKED | FINDINGS
CONTEXT PRESSURE: comfortable | tight | ran out — <one line: at what point?>
BRANCH: feat/zoom-sess   COMMITS: <sha…>
FILES CHANGED: <path (+x/-y) …>
WHAT I DID: <bullets, one per acceptance criterion, mapped to R1…R8>
THE M1 INTERLEAVING: <how you drove it, and the end state you asserted>
TEST COMMAND: <verbatim>
TEST OUTPUT: <verbatim tail — five gates AND the mandatory e2e list with the guard>
MUTATION PROBE: <the mutation, real failure output, blob-hash revert check>
ACCEPTANCE CRITERIA: <R1…R8: met / not met / partially — with reason>
DEVIATIONS FROM PROMPT: <or "none">
ASSUMPTIONS MADE: <or "none">
NOT DONE / OPEN:
```
