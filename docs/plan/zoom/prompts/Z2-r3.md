SESSION: ZOOM · Z2 · r3 · EXEC

You are the executor for the Zoom integration in the GENERA repo, **phase Z2, chunk
Z2-1, round r3**. You have no prior context. Everything you need is below.

**Read this first: r2 was not a failed round.** It stopped with `STATUS: FINDINGS` on a
contradiction between two acceptance criteria in the r2 prompt, and stopping was the
correct call — the PM verified the contradiction is real and it was the PM's drafting
error, not an executor mistake. This round carries the ruling that resolves it. The
work itself is small and fully specified.

---

## 0. Where you work

**BRANCH: `feat/zoom-sess`, head `6d1f58b`** (unchanged — r2 committed nothing). The
worktree is at `/Users/brentcurtis/Documents/fne-zoom-sess`, clean at that head. Work
there and commit on top. Do not rebase, amend, or force-push.

A candidate diff from r2 is parked at
`/private/tmp/claude-501/-Users-brentcurtis-Documents-fne-lms-working/03b134e7-afca-4dbf-a702-b039b9ea905b/scratchpad/z2r2-literal-ruling.diff`
(`pages/api/sessions/[id]/index.ts`, +38/−0). **It implements the ruling below and the
PM expects it to be substantially correct** — you may apply it as a starting point, but
you own what you commit: read every line against this prompt rather than trusting it.
If the file is gone, write the change from scratch; it is ~38 lines.

Three rules that override the generic `/exec` skill:

1. **DO NOT write `docs/plan/zoom/LEDGER.md`.** The ledger is PM-only here.
2. **Never pipe a gate command through `tail`** — it reports `tail`'s exit code and has
   masked real failures on this project twice.
3. Never merge or push to `main`. Do not open a PR.

**Round cap.** This is round 3 of a maximum 3 for this chunk. If it cannot be finished
clean, report `STATUS: BLOCKED` or `STATUS: FINDINGS` with the real reason — do not
improvise a fourth round's worth of scope to get to green.

---

## 1. THE RULING — Reading A, and the two assertions you are authorised to edit

r2's finding, verified by the PM against the code:

- The r2 prompt's **R1** required the managed-ON transition to write `meeting_link:
  null` and `meeting_provider: 'zoom'` "whatever the stored link was".
- Its **R4** required every r1 assertion to pass untouched.
- Two r1 assertions pin the transition's update payload exactly, on a fixture whose
  stored link is already null:
  `__tests__/api/sessions/session-managed-link-guard.test.ts:211` and `:221`, both
  `expect(state.updates[0]).toEqual({ is_zoom_managed: true })`.

Unconditional forcing adds two keys to that payload, so R1 and R4 cannot both hold.
**R4 was drafted too broadly. Its purpose was to stop assertions being weakened or
deleted to manufacture green — not to freeze a payload literal that the ruling
deliberately changes.**

### Ruling: **Reading A. Force unconditionally.**

Reading B (force only fields whose stored value differs) is rejected, on the executor's
own argument plus one the PM weighed:

- It is a third rule that POST does not have (`pages/api/sessions/index.ts:141-145`,
  `:219-221` force unconditionally), so the two entry points would diverge again —
  which is the thing §2 of r2 set out to end.
- It reopens B1 through a race: between the row `SELECT` and the `UPDATE`, a concurrent
  PUT can set a `meeting_link`, and a difference-only rule would then leave it in place
  on a managed row. Unconditional forcing closes that without needing a lock.

The cost is real and accepted: a toggle on an already-null-link row writes two no-op
fields, and because `fieldsChanged` drives the audit entry at
`pages/api/sessions/[id]/index.ts:389-400`, `session_activity_log` will record
`meeting_link: null → null` and `meeting_provider: 'zoom' → 'zoom'`. That is cosmetic
noise in an audit trail, weighed against a correctness hole, and the PM chooses the
noise. **Do not "fix" the logging** — filtering no-op fields out of `fieldsChanged`
would change shared behaviour for every field on this route and is out of scope.

### Exactly what you may edit in r1's tests — and nothing else

**Authorised:** the payload literal in those two assertions only, at
`__tests__/api/sessions/session-managed-link-guard.test.ts:211` and `:221`, updated to
the three-key shape the ruling produces. Keep both tests' `expect(res._getStatusCode()).toBe(200)`
lines. Keep both test names. Add a one-line comment at each site recording that the
three-key payload IS the Z2-1 r3 ruling, so the next reader does not mistake it for
drift.

**Not authorised:** any other change to any r1 assertion, fixture, or test name in any
file. If you believe another one must move, **stop and report `STATUS: FINDINGS`** —
that is the case this rule exists for, and r2 proved the loop works.

---

## 2. SCOPE

All of it lands in `pages/api/sessions/[id]/index.ts`, in the guard block r1 added at
`:265-315`, plus the two authorised test-line edits and the new tests in §4.

**S1 — force the managed shape on the ON transition.** When the stored
`is_zoom_managed !== true` and the request sets it to `true`, the update carries
`is_zoom_managed: true`, `meeting_link: null` and `meeting_provider: 'zoom'`,
atomically. Route it through the existing `updateData`/`fieldsChanged` copy so the audit
entry sees it — do not write a second, parallel update. Comment the intent: discarding
the manual link IS the semantic of "the platform now owns this meeting", not data loss.

**S2 — mirror POST's modality rule on the transition.** Refuse the ON transition when
the modality is neither `online` nor `hibrida`: **400**, es-CL, wording consistent with
`pages/api/sessions/index.ts:121-125`. Order it **before** S1's forcing so a refused
request writes nothing.

**PM rulings on the two judgment calls you raised in r2 — both ACCEPTED as you
implemented them:**

- **Effective modality.** Gate on `req.body.modality ?? session.modality`, not the
  stored value. Mirroring POST means validating the modality the row will *have*, so a
  PUT that sets `modality: 'online'` together with the toggle is allowed and one that
  sets `presencial` with the toggle is refused. Correct.
- **Transition-scoping.** Both S1 and S2 fire only on the transition (stored `!== true`
  AND request sets `true`), so a re-send of `true` on an already-managed row is never
  newly refused and forces nothing. Correct, and it is what keeps R2 below true by
  construction.

---

## 3. EXPLICITLY OUT OF SCOPE

Everything r1 shipped is accepted and stays: the migration, the eligibility seam,
`lib/zoom/provisioning-intent.ts`, both approve hooks, the POST create path, the
feature-flag entry, the pgTAP additions, and the r1 guards at `:265-315` other than the
two additions above. Also out: the `fieldsChanged` no-op logging (see §1); turning
managed **OFF** (leave the link null — an online session with a null link is already
reachable today); and everything on the Z2-1 out-of-scope list — no UI, no join API, no
`/meet` page, no reschedule/cancel/delete sync, no notifications, no hours-consumer
audit, no `community_meetings` column.

**Logged by the PM as backlog from your r2 report, NOT for this round:** PUT can move an
already-managed session's modality to `presencial` with no guard (inert today — the
eligibility gate refuses on modality, so nothing provisions). Correctly identified;
leave it.

---

## 4. ACCEPTANCE CRITERIA

- **[R1]** A PUT transitioning a session from unmanaged to managed writes
  `is_zoom_managed: true`, `meeting_link: null`, `meeting_provider: 'zoom'` in one
  update — proven on a fixture whose stored `meeting_link` is **non-null** and whose
  stored `meeting_provider` is **not** `'zoom'` (the shape r1 never exercised).
- **[R2]** A PUT re-sending `is_zoom_managed: true` on an already-managed row forces
  nothing extra: the payload contains only what the request itself carried.
- **[R3]** PUT toggling managed ON with an effective modality of `presencial` → 400
  es-CL, nothing written. Plus the companion: `modality: 'online'` sent together with
  the toggle on a stored-`presencial` row is **allowed**.
- **[R4]** Every r1 assertion passes, with the sole exception of the two payload
  literals authorised in §1. Any other r1 edit ⇒ stop and report FINDINGS.
- **[R5]** **Fail-on-old proof**: revert `pages/api/sessions/[id]/index.ts` alone to
  `6d1f58b`, re-run `session-managed-link-guard.test.ts`, paste the real failure output.
  The new R1/R3 tests must be among the failures — if they pass against r1's code they
  are testing nothing.
- **[R6]** All four gates green plus `test:db`. PM-measured baselines at `6d1f58b` you
  must meet or exceed: type-check 0 · lint 0 (`--max-warnings=0`) · **4298 passed / 264
  files** · build 0 · **pgTAP 338, `Result: PASS`, 8 files**. Your new tests push the
  test count up; nothing may push it down.

TEST COMMAND (verbatim, from `/Users/brentcurtis/Documents/fne-zoom-sess`):

```bash
npm run type-check && npm run lint && npm test && npm run build
```

```bash
npm run test:db
```

---

## 5. REVIEW-REQUEST FILE

Append an r3 section to the existing `docs/plan/zoom/reviews/fase-5-review-request.md`
— do not rewrite r1's content. It needs: the B1 fix as shipped, the R1/R4 contradiction
and how it was resolved (this is exactly the kind of history an independent reviewer
should see), the commit(s), the fail-on-old output, and one honest line on what a
reviewer should still distrust about a guard block that now does four things.

---

## 6. RULES AND REPORT

Surgical changes only. Match existing patterns. User-facing copy es-CL; code and
comments English. Do not grade your own work in prose — show the diff and the raw
output. No new time construction. NO DEPLOYMENTS. No migrations this round. If tests are
red, iterate up to 3 times, then report `STATUS: BLOCKED` with the real error.

Do **not** write `LEDGER.md`. Output exactly this and nothing after it:

```
## EXECUTOR REPORT — Z2 (chunk Z2-1) round r3
STATUS: COMPLETE | BLOCKED | FINDINGS
CONTEXT PRESSURE: comfortable | tight | ran out — <one line: at what point?>
BRANCH: feat/zoom-sess   COMMITS: <sha…>
FILES CHANGED: <path (+x/-y) …>
WHAT I DID: <bullets, one per acceptance criterion, each mapped to R1…R6>
TEST COMMAND: <verbatim>
TEST OUTPUT: <verbatim tail, unedited — including npm run test:db>
FAIL-ON-OLD PROOF: <the file, the reverted source, the real failure output>
ACCEPTANCE CRITERIA: <R1…R6: met / not met / partially — with reason>
DEVIATIONS FROM PROMPT: <or "none">
ASSUMPTIONS MADE: <or "none">
NOT DONE / OPEN:
```
