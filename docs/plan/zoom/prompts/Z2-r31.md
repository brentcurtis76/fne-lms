SESSION: ZOOM · Z2 · r31 · EXEC

You are the executor for the Zoom integration in the GENERA repo, **phase Z2, chunk Z2-S11,
round r31**. You have no prior context.

Phase Z2 is **code-complete and independently approved** (Sol, `APPROVE WITH NOTES`). PR **#45**
is open — and **GitHub cannot run CI on it, because the PR is `CONFLICTING`.** `main` has moved
**136 commits** since this branch forked. **This round merges `main` in and resolves the
conflicts. It changes no feature behaviour.**

---

## 0. Where you work

**BRANCH: `feat/zoom-sess`, head `4be9f7d6`**, already pushed. Worktree at
**`/Users/brentcurtis/dev/wt/zoom-sess`**. Never put a checkout under `~/Documents`.
Live clone: `/Users/brentcurtis/dev/fne-lms`.

Confirm first: `git rev-parse HEAD` must print `4be9f7d6…`. If not, **stop and report**.

**This round is the ONE exception to the standing no-merge rule: you MUST merge `origin/main`
into `feat/zoom-sess`.** That is a merge **into the feature branch**, never the reverse.
**Never merge or push to `main`. No rebase. No force-push. Do not close or re-target PR #45.**
Never run `vercel`. **NEVER TOUCH THE PRODUCTION DATABASE.**

PM baselines at `4be9f7d6`: type-check 0 · lint 0 · **4742 passed / 284 files** · build 0 ·
**`test:db` Files=11, Tests=466, PASS** · mandatory e2e **88 passed / 7 specs**, guard green.

---

## 1. THE CONFLICTS — three files, and all three are shared CI fixtures

`git merge-tree` (PM-run, nothing mutated) reports exactly three:

```
scripts/ci/e2e-fixtures.json
scripts/ci/e2e-mandatory.mjs
scripts/ci/seed-e2e.mjs
```

`.github/workflows/ci.yml` auto-merges cleanly and needs no help.

**Both workstreams added to the same files.** Z2's r28 added a Zoom-managed session fixture and
registered `tests/e2e/zoom-managed-join.spec.ts` as mandatory. The pasantías/A8 workstream added
its own admin-leads fixtures and registered `tests/e2e/pasantias-leads-admin.spec.ts`.

**THE RULING — the resolution is a UNION, not a choice.** Keep **both** sides' fixtures, **both**
sides' seeded rows and **both** sides' mandatory specs. **Neither workstream's coverage may be
dropped, renumbered out, or silently reordered away.**

**`scripts/ci/e2e-mandatory.mjs` is the file to be most careful with.** It is the list of specs
that *must* run in CI, and it exists precisely so a green gate cannot mean "the specs were
skipped". A resolution that keeps only one side leaves a file that **still parses, still runs,
and still reports green** while another team's required coverage has silently vanished. That is
the exact failure this repo built that guard to prevent, reintroduced by a merge.

**If the two sides genuinely cannot both be satisfied** — a real semantic clash rather than two
additions in one place — **stop and report `FINDINGS`.** Do not pick a winner.

---

## 2. WHAT ELSE THE MERGE BRINGS

Expect these to arrive from `main`, and **do not undo them**:

- **`PLAN.md` §5's owner-authored amendment** (commit `50c28855`), which the branch's code headers
  already cite. This merge is what closes Sol's **m6**.
- The **binding process rule** that every phase opens a PR before merge.
- ~136 commits of other workstreams' work — pasantías, A6/A7/A8. **None of it is yours to
  review, adjust or tidy.**

---

## 3. SCOPE

**S1** — `git merge origin/main` on `feat/zoom-sess`; resolve the three conflicts per §1.
**S2** — re-run every gate on the merged tree (§5). A merge that breaks a gate is a broken merge.
**S3** — if the merge changes behaviour anywhere, **report it; do not adjust the feature to suit
the merge.**

---

## 4. EXPLICITLY OUT OF SCOPE

- **No feature changes.** This phase is approved; the diff Sol reviewed must not move. The only
  source changes permitted are conflict resolutions.
- **Do not touch anything the merge brings in from other workstreams.**
- The standing unruled items, item 12b, the production migrations — all unchanged.
- **Do not open, close, edit or merge PR #45.** Pushing the branch updates it automatically.

**If you find a defect, report it — do not fix it.**

---

## 5. ACCEPTANCE CRITERIA

- **[T1]** `feat/zoom-sess` contains `origin/main`: `git merge-base --is-ancestor origin/main
  HEAD` succeeds.
- **[T2]** **Union preserved, proved by name.** `scripts/ci/e2e-mandatory.mjs` lists **both**
  `tests/e2e/zoom-managed-join.spec.ts` **and** `tests/e2e/pasantias-leads-admin.spec.ts`, plus
  every spec either side listed before. Print the final list in the report.
- **[T3]** `scripts/ci/e2e-fixtures.json` carries both sides' fixtures — the Zoom managed session
  **and** the pasantías rows — and `seed-e2e.mjs` seeds both. Its local-only refusal is intact.
- **[T4]** **All five gates green on the merged tree**: type-check 0 · lint 0 · `npm test` ·
  build 0 · `test:db`. Counts may rise, because `main` brings its own tests; **nothing that
  passed before may fail.**
- **[T5]** **The full mandatory e2e list runs and passes, with the no-skip guard green** — now
  covering both workstreams' specs. This is the criterion that proves `[T2]` is real rather than
  textual.
- **[T6]** The Z2 feature diff is unchanged by the merge: `git diff` of `lib/zoom/**`,
  `lib/utils/meeting-*`, `lib/services/{billable-hours,session-lifecycle-notifications,
  school-hours-report,hour-tracking}.ts`, `pages/api/meet/**`, `pages/meet/**` and
  `supabase/migrations/**` against `4be9f7d6` is **empty**. Prove it with the command.
- **[T7]** After pushing, **PR #45 reports mergeable** and GitHub Actions has **started** a run
  on the branch. Paste `gh pr checks 45` output.

TEST COMMAND (verbatim, from `/Users/brentcurtis/dev/wt/zoom-sess`):

```bash
npm run type-check && npm run lint && npm test && npm run build
```

```bash
npm run test:db
```

```bash
CI=1 npx playwright test $(node scripts/ci/e2e-mandatory.mjs --list) --project=chromium
node scripts/ci/e2e-mandatory.mjs --check test-results/e2e-results.json
```

**Note:** the e2e gate needs `.env.local` pointed at the local stack. Back it up, rebuild it the
way `.github/workflows/ci.yml` does, seed, run, then **restore it and prove the restore by
checksum** — the practice r28–r30 established. `.env.local` holds production credentials: never
commit it, never print it.

---

## 6. REVIEW-REQUEST FILE

Append a short **Merge with `main` — round r31** section to
`docs/plan/zoom/reviews/fase-5-review-request.md`. **Append; do not rewrite.** Record the three
resolutions, the final mandatory-spec list, and anything the merge changed that you did not expect.

---

## 7. RULES AND REPORT

Surgical resolutions only. English for commits. **Synthetic data only — never real PII.**

If a gate is red after the merge, **that is the finding** — report `STATUS: BLOCKED` with the
real output rather than adjusting the feature to make it pass. **If the two sides of a conflict
cannot both be honoured, STOP and report `STATUS: FINDINGS`.**

Do **not** write `LEDGER.md`. Output exactly this and nothing after it:

```
## EXECUTOR REPORT — Z2 (merge with main) round r31
STATUS: COMPLETE | BLOCKED | FINDINGS
CONTEXT PRESSURE: comfortable | tight | ran out — <one line: at what point?>
BRANCH: feat/zoom-sess   COMMITS: <sha…>
FILES CHANGED: <the conflict resolutions only>
THE THREE RESOLUTIONS: <what each side contributed, and how you kept both>
FINAL MANDATORY SPEC LIST: <every entry>
WHAT I DID: <bullets, one per criterion, mapped to T1…T7>
TEST COMMAND: <verbatim>
TEST OUTPUT: <verbatim tail — five gates AND the mandatory e2e list with the guard>
FEATURE DIFF CHECK: <the T6 command and its empty output>
PR STATUS: <gh pr checks 45>
ACCEPTANCE CRITERIA: <T1…T7: met / not met / partially — with reason>
DEVIATIONS FROM PROMPT: <or "none">
ASSUMPTIONS MADE: <or "none">
NOT DONE / OPEN:
```
