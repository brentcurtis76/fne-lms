SESSION: ZOOM · Z2 · r9 · EXEC

You are the executor for the Zoom integration in the GENERA repo, **phase Z2, chunk
Z2-3b, round r9 — a remediation round**. You have no prior context.

**Round r8's work is accepted. You were right, and the PM was wrong.** You implemented
[A10] as written, flagged that it contradicts plan §14, and asked for a ruling instead of
silently choosing. That is exactly the correct behaviour and it caught a real defect —
one the PM introduced by writing a criterion that contradicted the frozen plan. This
round applies the ruling. **It is a small, surgical change: one function, plus tests.**

**Your report IS the deliverable** — a round that pushes without reporting is invisible
to the ledger and has already cost this phase a full extra round.

---

## 0. Where you work

**BRANCH: `feat/zoom-sess`, head `0666b996`**, already pushed. Worktree at
`/Users/brentcurtis/dev/wt/zoom-sess`. Never put a checkout under `~/Documents`.
Confirm first: `git -C /Users/brentcurtis/dev/wt/zoom-sess rev-parse HEAD` must print
`0666b996…`; if not, stop and report. Commit on top — no rebase, no amend, no
force-push, no PR.

**Do not write `docs/plan/zoom/LEDGER.md`** (PM-only). **Never pipe a gate through
`tail`.** Never push to `main`. No deploys.

`npm run test:db` still needs Docker, whose engine is down on this host. Attempt it; if
it fails, report it as NOT RUN with the real error, exactly as you did in r8. **Do not
adopt the `Files=9, Tests=374` figure — it remains unreproduced.** This round adds no
migration.

---

## 1. THE RULING — §14 wins, and it is narrower than you might expect

The PM re-read `PLAN.md:292` verbatim before ruling:

> `FEATURE_ZOOM_MEETINGS` … **When flipped off**: no new provisioning; join/signature
> routes return 503 …; **webhooks continue to be accepted** …; recording-transfer,
> retention, and reconcile jobs **continue** …; **`meeting_delete` jobs for cancellations
> continue**. I.e. the kill switch stops *new meetings and joins*, never cleanup.

and `PLAN.md:296`:

> `ZOOM_SCHOOL_ALLOWLIST` (csv of school_ids; empty = all) — **checked at provision
> time**. **School removed after provisioning**: existing future meetings are NOT
> auto-deleted …

The plan is explicit and you read it correctly. **[A10] was wrong for `meeting_delete`.**
Your one-directional consequence was the right one to name: with the master flag off
during an incident, a session cancelled in that window would leave a live meeting on a
booked host — the precise failure §14's "never cleanup" sentence exists to prevent.

### Ruling, in two parts — apply exactly this and no more

**(a) `meeting_delete` is ungated by both flags.** `checkCleanupGate`
(`lib/zoom/provisioning-intent.ts:171-183`) must drop **both** the
`FEATURE_ZOOM_MEETINGS` check and the `ZOOM_SCHOOL_ALLOWLIST` check, keeping **only**
`is_zoom_managed !== true → { reason: 'not_managed' }`. The flag is explicit in §14. The
allowlist follows from "checked at provision time" — a cancellation is not a provision,
and a school leaving the wave must never strand a live meeting.

**(b) `meeting_sync` is unchanged.** It keeps `checkProvisionGate` exactly as it is
today — master flag, delegated eligibility, allowlist. §14 does not name sync among what
continues, and the allowlist's documented behaviour for a removed school is precisely
that its existing meetings **freeze** ("NOT auto-deleted … a reconcile notice lists
still-provisioned meetings so an admin can bulk-cancel deliberately"). Freezing is the
plan's stated intent, so leaving sync gated is the reading that follows §14's words
rather than extending them.

**Do not generalise beyond (a).** If you find yourself changing `checkProvisionGate`,
the sync enqueue, or any handler, stop — the ruling is one function.

---

## 2. SCOPE

- **S1** — `checkCleanupGate`: remove the feature-flag and allowlist branches; keep the
  managed-intent check. Update its doc comment to state **why** cleanup is ungated,
  citing §14's "never cleanup", so the next reader does not re-add a flag check thinking
  it was an oversight. `CleanupGateRefusal` narrows to `{ reason: 'not_managed' }`;
  remove the now-unreachable variants rather than leaving dead shapes.
- **S2** — update the tests that assert the old behaviour, and **add** assertions for the
  new: with the master flag unset/`'false'`, and with the school outside a non-empty
  allowlist, **all three delete enqueue points still enqueue** (single cancel, series
  cancel, modality flip). Keep the corresponding **sync** assertions unchanged and
  passing — they are ruling (b) and must not move.

---

## 3. OUT OF SCOPE

Everything else in r8 is accepted and stays: both handlers, the registry entries, the
dedupe keys, all four enqueue points, the `sessionStartsAtIso` extraction, the response
invariants. No new migration — proposing one is a stop-and-report. No new lifecycle
points; the edit-request modality-flip gap you flagged stays open and logged (see §5).

---

## 4. ACCEPTANCE CRITERIA

- **[R1]** `checkCleanupGate` consults neither flag; the only refusal is `not_managed`.
- **[R2]** With `FEATURE_ZOOM_MEETINGS` unset **and** with it `'false'`, all three delete
  points still enqueue exactly one `meeting_delete` with the correct dedupe key.
- **[R3]** With the school outside a non-empty `ZOOM_SCHOOL_ALLOWLIST`, all three delete
  points still enqueue.
- **[R4]** An unmanaged session still enqueues nothing at any delete point.
- **[R5]** Sync behaviour is **unchanged**: master flag off ⇒ no `meeting_sync`; school
  outside the allowlist ⇒ no `meeting_sync`. Both asserted, both still passing.
- **[R6]** Every other r8 assertion passes unchanged. If one must move that is not a
  cleanup-gate flag assertion, **stop and report FINDINGS** — that would mean the ruling
  and the shipped behaviour disagree, which is the PM's call.
- **[R7]** Four TS gates green. PM-measured baselines at `0666b996`: type-check 0 · lint 0
  (`--max-warnings=0`) · **4475 passed / 274 files** · build 0. Nothing may fall.
  `test:db` per §0.
- **[R8]** **Mutation probe** proving the new tests bite: re-add the feature-flag check to
  `checkCleanupGate`, show [R2] failing with real output, revert, and prove the source
  byte-identical by blob hash. Declare it a mutation probe — there is no fail-on-old for
  a behaviour that is being deliberately reversed.

TEST COMMAND (verbatim, from `/Users/brentcurtis/dev/wt/zoom-sess`):

```bash
npm run type-check && npm run lint && npm test && npm run build
```

```bash
npm run test:db
```

---

## 5. PM RULINGS ON YOUR r8 DEVIATIONS AND ASSUMPTIONS — recorded here because a fresh session cannot read the ledger

**Deviations — all three ACCEPTED:**

1. The [A10]/§14 conflict — resolved above. You were right to implement the criterion,
   confine it to one line, name the one-directional consequence, and escalate rather than
   choose. That is the third time an executor on this workstream has caught a PM error by
   refusing to resolve a contradiction alone.
2. Enqueues placed **before** the Z2-3a hours sync (both still after the session update
   commits) — **correct, and better than the prompt's ordering**. A 500 from the ledger
   must not leave Zoom on the old time. Keep it.
3. Touching `meeting-provision.ts` to extract `sessionStartsAtIso` — **correct**.
   `TZDate.toISOString()` renders the zoned form, so re-deriving the instant in a new
   module would have produced dedupe keys that silently stopped matching. Behaviour-
   identical, and the 98 provision tests passing unchanged is the proof.

**Assumptions — all five ACCEPTED:**

1. 23P01 on a sync reservation move is terminal (`sync_host_busy`), not retryable — no
   backoff frees a host, and untyped throws would have spun forever. Right.
2. `meeting_sync` accepts only `status='provisioned'`; a NULL meeting number is a
   **completion**, because `meeting_provision` re-proves its reservation against the
   current schedule and creates at the new time. Right.
3. `meeting_delete` refuses a numberless row parked under `ambiguous_create_outcome`,
   mirroring the provisioner. Right — the alternative frees a host that may be occupied.
4. `deleted` (not `cancelled`) as the terminal status for a meeting actually removed at
   Zoom; both map to public `cancelled`. Right.
5. `missing`/`not_publishable` from the projection RPC are terminal anomalies. Right.

**Logged as backlog, NOT this round:** the edit-request route can carry a `modality`
change and currently enqueues only `meeting_sync`, so an edit-request that flips a session
to `presencial` leaves the meeting alive. You flagged it rather than widening scope, which
was correct; the PM has recorded it and will schedule it deliberately.

---

## 6. RULES AND REPORT

Surgical changes only. es-CL for user-facing copy; English for code and comments. Do not
grade your own work in prose — show the diff and the raw output. If tests are red for a
reason other than the deliberate probe, iterate up to 3 times, then report
`STATUS: BLOCKED`. If the ruling in §1 is wrong against the code, **STOP CODING** and
report `STATUS: FINDINGS`.

Append an r9 note to `docs/plan/zoom/reviews/fase-5-review-request.md`; do not rewrite
earlier sections. Do **not** write `LEDGER.md`. Output exactly this and nothing after it:

```
## EXECUTOR REPORT — Z2 (chunk Z2-3b) round r9
STATUS: COMPLETE | BLOCKED | FINDINGS
CONTEXT PRESSURE: comfortable | tight | ran out — <one line: at what point?>
BRANCH: feat/zoom-sess   COMMITS: <sha…>
FILES CHANGED: <path (+x/-y) …>
GIT DIFF STAT: <verbatim `git diff --stat 0666b996..HEAD`>
WHAT I DID: <bullets, one per acceptance criterion, mapped to R1…R8>
TEST COMMAND: <verbatim>
TEST OUTPUT: <verbatim tail, unedited — and for test:db either the real result or the real error>
MUTATION PROBE: <[R8]: the mutation, the real failure output, the blob-hash revert check>
ACCEPTANCE CRITERIA: <R1…R8: met / not met / partially — with reason>
DEVIATIONS FROM PROMPT: <or "none">
ASSUMPTIONS MADE: <or "none">
NOT DONE / OPEN:
```
