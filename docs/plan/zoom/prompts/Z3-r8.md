SESSION: ZOOM · Z3 · r8 · EXEC

You are the executor for the Zoom integration in GENERA (`fne-lms`), phase **Z3 —
Embedded experience**, **round 8**. You have no prior context.

r7 diagnosed the last open defect and the owner has ruled on it. **This round implements
that ruling.** Read `CLAUDE.md` first — its hard rules outrank this prompt.

## BRANCH AND WORKTREE

**Continue on `feat/zoom-embed`** at **`137a6120`**, worktree
**`/Users/brentcurtis/dev/wt/zoom-embed`**. PR
[#47](https://github.com/brentcurtis76/fne-lms) head is `137a6120`.

```bash
git worktree list
git -C /Users/brentcurtis/dev/wt/zoom-embed status --short   # expect empty
git -C /Users/brentcurtis/dev/wt/zoom-embed log --oneline -1 # expect 137a6120
```

**Baseline, PM-verified: 7008 tests / 302 files · pgTAP 484 · type-check, lint, build clean.**

**Environment facts, established across r6–r7 — do not rediscover them:** `.env.local` points
at **PRODUCTION** Supabase and must not be edited (export local URL/keys in your shell; Next
gives real env vars precedence). Local stack `supabase_db_sxlogxqzmarhqsblxmtj` on **54322**;
a casa-web stack shares the daemon; **never `npx supabase db reset`**. All eight `ZOOM_*` keys
present. Meeting `81229544181`, `join_before_host:false` so it must be started first (r6/r7
used a scratchpad host-starter with role 1 + ZAK); **leave it, never delete it**. Real-browser
work uses `--use-fake-device-for-media-stream --use-fake-ui-for-media-stream`.

---

## THE DIAGNOSIS YOU ARE ACTING ON

r7 established, and the PM confirmed in code:

> Zoom's Client View shows a pre-join screen and waits for the user to press **«Entrar»**.
> `sdk.join(...)` is wrapped in `awaitClientViewCall`, which carries `SDK_CALL_TIMEOUT_MS`
> = 45 s (`JoinMeetingButton.tsx:495`). **`join`'s success callback fires when the *person*
> enters, not when the *machine* finishes** — so the deadline expires in front of a healthy
> Zoom screen and yanks a user who simply had not clicked yet.

Ruled out already, do not re-test: devices present or absent makes no difference; `init({disablePreview:true})`
does not suppress the screen; a human click completes the join in **5.9 s**. Component View is
a widget rather than an app takeover, has no such screen, and joins in 4.2 s with its callback
firing normally.

**OWNER RULING (Brent, this round): Option A — Zoom's pre-join screen is the intended UX.**
Mobile and Firefox users see it, press «Entrar», and join embedded. **It is not a defect to be
worked around; it is the vendor's device-check screen, and it is better than the one we built.**

---

## SCOPE

### 1. The deadline must bound the machine, never the human

**This is a property, not a mechanism. The PM has been wrong about this mechanism twice and
is not going to name a third one — you determine the signal and prove it.**

The contract the round must satisfy, both halves:

- **A machine failure still degrades to the link within a bounded time.** Script hangs, the SDK
  never renders, Zoom calls neither callback — everything M3 and r6/r7 bought stays bought.
- **A human deliberating in front of a rendered, interactive Zoom UI is never interrupted.**
  No deadline, no teardown, no yank to a link, for as long as they want to think about it.

The natural shape is that the clock runs until Zoom's UI is up and interactive, and stops
there — after that the time is the user's, not ours. **Find the signal that says "Zoom has
rendered and is waiting for a person", cancel the deadline on it, and show your evidence that
it is reliable.** If you cannot find a trustworthy signal, say so and report FINDINGS rather
than approximating one with a longer timeout.

**Do not remove the timeouts from `init`, `i18n.load`, the CDN downloads, or the frame load.**
Those bound machine work and they are correct.

### 2. A GENERA way out, while the embed is up

r7 surfaced this and correctly left it: during those seconds Zoom's screen occupies the full
viewport with **no GENERA affordance on it**, so a user who would rather just open the link
cannot reach it. With the deadline gone that becomes permanent rather than 45 seconds long.

Give the user a persistent, visible way to reach the link while the embed is mounted, in es-CL.
It reuses the existing `{fallback:'link'}` path — **no new server surface.** The iframe sits
inside our page, so you own the space around it.

### 3. Reconcile `PreJoinCheck` with Zoom's own preview screen

On the **Client View** path our `PreJoinCheck` now sits in front of a Zoom screen that does the
same job natively and better. Two device checks and two clicks to join one meeting is worse
than one.

**PM ruling: on the Client View path, Zoom's screen is the preflight — do not show ours as
well. On the Component View path `PreJoinCheck` stays exactly as it is**, because Component
View has no preview screen and r3 built ours for exactly that gap.

If that collides with something you find in the code, **report FINDINGS rather than forcing
it** — r3's `PreJoinCheck` was built to a stated contract and this changes where it applies.

## EXPLICITLY OUT OF SCOPE

- `lib/utils/meeting-join-policy.ts`, `lib/utils/meeting-zak-policy.ts`, the join route's gate
  order, the §9 rule, the audit write, the migration, `supabase/tests/`.
- Sol's M1, M2, M4 and r7's CSS fix — all landed and verified. Anything you notice there:
  **report, do not fix.**
- `tests/e2e/zoom-join-authz.spec.ts` — `git diff --stat origin/main..HEAD -- tests/e2e/` must
  stay empty; paste it.
- **No migration. No `package.json` change. No new feature work.**
- Never merge to `main`. Never deploy. Never touch the production database.

## ACCEPTANCE CRITERIA

- **[U1]** A real framed Client View join completes **with no deadline pressure**: the user
  presses «Entrar» when they choose to, and joins. Demonstrate with a deliberate pause
  **longer than 45 s** before clicking, and show the join still succeeds.
- **[U2]** A machine failure still reaches the link within a bounded time — script hang, SDK
  that never renders, callbacks that never fire. Fake-timer tested, and the bound stated.
- **[U3]** The signal you use to cancel the deadline is evidenced as reliable, not assumed.
- **[U4]** A GENERA control reaches the link while the embed is mounted, es-CL, reusing
  `{fallback:'link'}` with no new server surface.
- **[U5]** `PreJoinCheck` no longer double-gates the Client View path; it is unchanged on the
  Component View path.
- **[U6]** Everything r6/r7 proved still holds — re-verify, do not assume: Component View
  joins; es-ES before render on both views; SDK failure → link; flag off → link; both Zoom
  stylesheets 200 with `data-loaded="true"`; a blocked stylesheet warns and the join still
  completes; both mobile triggers reach Client View.
- **[U7]** All five gates green, baseline **7008/302** met or exceeded, no credential in the diff.
- **[U8]** `docs/plan/zoom/reviews/fase-6-review-request.md` updated — Sol re-reads it — with
  the ruling, what changed, and the new bounded/unbounded split stated plainly.
- **[U9]** `git diff --stat origin/main..HEAD -- tests/e2e/` empty; paste it.

## GATES

```bash
npm run type-check && npm run lint && npm test && npm run build
```

Plus `npm run test:db`. Never pipe a gate through `tail`.

**Fail-on-old** for [U2]: remove the bound you keep on a machine step and prove the test fails.
Revert, prove byte-identity by hash.

## RULES

- **No real person's data.** r6 caught that a camera-off participant renders the licensed
  host's real profile photo; r7 confirmed the host tile is a name card when the starter joins
  through the SDK. **Check each screenshot before committing it rather than assuming.**
- No credential in any committed file; no passcode, signature or ZAK in a screenshot.
- `FEATURE_ZOOM_EMBED` on only in your own local env.
- Do not grade your own work in prose. Show the diff and the raw output.
- **If this prompt is wrong, or no reliable signal exists for [U3], STATUS: FINDINGS.** Five of
  this phase's findings rounds were the PM's error, not the executor's; r7's falsified the PM's
  own hypothesis. **That is the system working.**
- Do not write to `LEDGER.md`. **Do** update the review-request per [U8].
- If `git push` is refused by the permission classifier, commit anyway and say so; the PM pushes.

## WHEN DONE

```
EXECUTOR REPORT — Z3 r8 (owner ruling A: the preview screen is the UX)
STATUS: COMPLETE | BLOCKED | FINDINGS

BRANCH / HEAD SHA: feat/zoom-embed @ <sha>   (base 137a6120)
COMMITS: <sha> <subject>

THE BOUNDED / UNBOUNDED SPLIT
<what is still deadlined, what is not, and the signal that separates them —
 with your evidence that the signal is reliable>

ACCEPTANCE CRITERIA
[U1] … [U9]  — [U1] must state the pause you took before clicking

GATE OUTPUT (raw, unpiped, per-gate exit codes)
type-check | lint | test (N/M) | build | test:db

FAIL-ON-OLD PROOF
<[U2] probe: change, failing output, revert, hashes>

E2E UNTOUCHED
<git diff --stat origin/main..HEAD -- tests/e2e/>

CREDENTIAL / PII HYGIENE
<what you checked; what the screenshots show>

DEVIATIONS / ASSUMPTIONS / NOT DONE
<numbered>

WHAT I AM LEAST SURE OF
<the PM reads this hardest, and Sol reads it after that>
```
