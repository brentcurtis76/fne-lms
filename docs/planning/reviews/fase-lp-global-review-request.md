# Review request — fase LP-GOV-01: learning-path global-template governance correction (`docs/lp-global`)

**Branch:** `docs/lp-global` · **Base:** exactly `72f7beed45b87aeb2fa0be7ba7f74fe5b78e944d` (`origin/main` at execution time, verified against the live remote) · **Commit count: exactly 1.** The final SHA is that single commit's hash; a file cannot contain the hash of the commit that introduces it, so it is recorded in the executor's completion report and is verifiable as `git log --oneline docs/lp-global ^72f7beed` (exactly one commit, whose parent is the base SHA).

**Kind:** documentation-only governance correction. No application code, API, migration, RLS policy, or test-suite implementation; no Supabase call; no database query or mutation; no production access; no push, PR, merge, or deployment performed by the executor.

---

## 1. Objective and scope

**Objective.** Record Brent's authoritative 2026-08-29 owner decisions — learning paths are **global FNE templates** with **literal-admin-only management** — and correct every governance artifact that still encoded the rejected per-school-ownership model: preserve the W-PC-06 evidence while superseding its inference (effective classification **A**), retire the never-executed W-B2d-01 backfill truthfully, reclassify W-B2c-01 as a class-2 security/RLS correction with three explicit prerequisites, freeze B2c's scope only after a corrected repository inventory, record the intended access model and future acceptance criteria, reconcile all totals, and harden the validator against regression.

**In scope:** the nine files of §3; the `SUPERSEDED` status-vocabulary extension; validator checks 18/21/22 (and the additive constants they read); protocol revision 8 → 9; normalization report §14; evidence record §8; PROJECT_STATE Meta entry LP-GOV-01.

**Out of scope (verified untouched):** the frozen claim snapshot `docs/reviews/santa-marta-claims.csv` (SHA-256 re-verified `d598f29b…`) and the archived legacy ledger (SHA-256 re-verified `009f14ab…` — the validator's frozen pin); `santa-marta-work-claim-map.csv` (152 links unchanged); every historical review request; `W-PC-01`…`W-PC-05`, `W-B2a-01`, `W-B2b-01`, `W-B10a-01`, D-RLS-01/02/03; AGENTS.md, CLAUDE.md; all application code, API routes, services, migrations, grants, policies, functions, RLS, pgTAP/Vitest/Playwright suites, and CI configuration. No W-B2c implementation of any kind — the correction *documents* what the future implementation must do (record §7) and authorizes none of it.

## 2. Authoritative owner decisions (Brent, 2026-08-29)

(1) `learning_paths` rows are global FNE templates. (2) No school owns a learning path. (3) Learning paths are not generation-specific. (4) NULL `school_id`/`generation_id` on existing rows is intentional global scope, not missing or corrupt ownership data. (5) A path may be assigned through the assignment matrix to users or groups in any school. (6) Assignment affects availability/use; it does not make the path school- or generation-owned. (7) Only the literal RBAC role `admin` may create, edit, delete, assign, or unassign learning paths. (8) No other role may perform those management actions — expressly including `equipo_directivo` and `consultor`. (9) A non-admin with a valid user/group assignment may consume the assigned path and update only their own permitted progress. (10) The decisions authorize no implementation and no production access.

## 3. Files changed, grouped by risk

**Higher risk (enforcement and ledger substance):**
- `scripts/check-ledger.mjs` — `SUPERSEDED` added to the status enum; `CLAIMS_SHA256` and `LP_GOV_DOC` frozen constants; B2C sets corrected to 4 tables + 8 functions (check 18 labels/sums follow); check 21's forward-state pins revised deliberately (see §7); new check **[22 semántica global]**. No unrelated check weakened or renumbered.
- `docs/reviews/santa-marta-work-items.csv` — three rows: `W-B2c-01` (title/gate/notes rewritten to global-template + literal-admin semantics, `clase_migracion` BLOCKED→2, three prerequisites), `W-B2d-01` (status BLOCKED→SUPERSEDED, gate/notes marked historical-must-not-run, non-execution recorded), `W-PC-06` (dated semantic-supersession sentence appended to notes; classification-B history kept verbatim).

**Medium risk (governing prose that must reconcile):**
- `docs/reviews/santa-marta-release-protocol-2026-08-25.md` — revision 9 header; status table 29/57/**17**/3/**1**; §0 SUPERSEDED sentence; §3 B2c bullet rewritten; §4 B2c/B2d rows + branch-inventory note; §8 pilot gate B2a–**c** and re-sequenced RLS axis; §9 check-6 row + superseding paragraph (historical paragraph preserved).
- `docs/reviews/santa-marta-ledger-normalization-report-2026-08-25.md` — new §14 (decisions, superseded inference, ledger/validator effects, corrected-inventory summary, totals table, non-authorization).
- `docs/reviews/w-pc-06-learning-path-data-classification-2026-08-28.md` — §§1–7 byte-identical; dated §8 appended (superseding owner-semantics decision; effective classification A).
- `PROJECT_STATE.md` — one new authoritative Meta bullet (`- **LP-GOV-01 …`), above and not touching the pinned historical `- **W-PC-06 — CLOSED` entry.

**Lower risk (annotation and new standalone records):**
- `docs/reviews/santa-marta-combined-plan-2026-08-25.md` — banner now points at revision 9; row 0.4-bis gains the dated `[Corregido 2026-08-29 — semántica global]` annotation (existing convention; prior annotations kept).
- `docs/reviews/w-b2c-01-learning-path-governance-correction-2026-08-29.md` — **new**: the anchor record (decisions, preserved-vs-superseded analysis, access model, full surface inventory, corrected W-B2c definition, twelve future acceptance criteria).
- `docs/planning/reviews/fase-lp-global-review-request.md` — this file.

## 4. Preserved historical evidence vs. superseding interpretation

Preserved byte-identical: the W-PC-06 evidence record §§1–7 — the authorization chronology (Brent queries 1–4 in the production SQL Editor; Codex's one read-only Management API metadata call; Codex's query 5 as the sole explicitly authorized Management API database-query call), all aggregate/schema results, and classification **B — DATA TRANSFORMATION REQUIRED** as reached on 2026-08-28. The queries were correctly executed and correctly reported; nothing rewrites history to pretend classification B was never reached, and the literal classification string stays pinned by check 21. Superseded — by dated addenda only (evidence §8, protocol §9 paragraph, report §14, W-PC-06 notes): the *inference* that NULL scope columns plus multi-school assignment dispersion proved broken per-school ownership. Under the owner's semantics the same aggregates describe healthy global templates, so the **effective conclusion is classification A: no existing learning-path ownership data transformation is required**.

## 5. Status-vocabulary judgment for W-B2d-01

The governed enum (`SCHEDULED`/`ACTIVE`/`BACKLOG`/`BLOCKED`/`DONE`) cannot truthfully represent W-B2d-01's end state: `DONE` would claim a backfill ran (it never did — misusing DONE is expressly prohibited), `BLOCKED` would claim work is still pending behind a gate (nothing is; the premise was rejected), and deleting the row would erase audit history and break check 21's shape pins. The narrowest coherent representation is **one additive terminal value, `SUPERSEDED`** — "retired unexecuted because the owner rejected its premise." Every affected count and check was updated: protocol status table and prose, report §14 table, validator enum, and checks 21/22 (which now fail any DONE/AUTHORIZED representation and any status other than SUPERSEDED for this item). No existing status value was renamed or re-meant; an invented value (e.g. `RETIRED`) still fails check 07 (mutation M23).

## 6. Repository surface inventory and unresolved implementation questions

Full inventory (file:line anchors) in `docs/reviews/w-b2c-01-learning-path-governance-correction-2026-08-29.md` §6, performed read-only against the committed repository at the base SHA. Headline corrections to the old "two tables and six functions" claim: **four tables** (adding `learning_path_assignments` — RLS enabled but four permissive `USING (true)` policies with no `TO` clause, so effectively open to every role including `anon` — and `learning_path_progress_sessions`); **eight SECURITY DEFINER functions** (adding `increment_path_assignment_time` and `update_session_heartbeat`, both `anon`-executable progress mutators); caller-supplied actor IDs (`p_created_by`/`p_updated_by`/`p_assigned_by`/`p_user_id`) used as authorization inputs; the `course_enrollments` auto-enroll side effect with an unassign asymmetry; the `courses_learning_path_member_view` alternate access path; summary relations absent from all migrations plus five dead RPC references; 16 learning-path API routes with management gated on `admin|equipo_directivo|consultor`; 2 maintenance routes (one service-role with **no auth guard at all**, one `CRON_SECRET` fail-open), unscheduled in `vercel.json`; 4 assignment-matrix routes (one building a service-role client to bypass RLS); browser-side direct table reads at three code points; UI-vs-API gap (middleware blocks non-admin from the `/admin` pages, the APIs do not).

**Unresolved implementation questions, explicitly deferred to the authorized implementation (and to Privacy's matrix where noted):**
1. Whether analytics/reporting reads (`analytics.ts`, `enhanced-progress`, `user/[userId]`, assignment-matrix routes) remain available to `equipo_directivo`/`consultor`: the owner decisions govern **management** actions and assigned-user consumption; read-only reporting access is for the Privacy-approved actor-by-operation matrix to settle.
2. Whether `unassign` should also remove auto-created `course_enrollments` rows (today it does not).
3. Whether the two maintenance routes are retained-and-authenticated or retired, and how the five dead RPC references and the missing summary relations (interplay with `W-B6d-01`) are resolved.
4. Whether `group-assignments.ts`'s service-role RLS bypass is re-justified or replaced with policy-compatible reads.
5. The `created_by`-owner branch in `update_full_learning_path`/`canManagePath`: under decision 7, creator-ownership confers no management authority — the implementation must remove it, unless Brent explicitly rules otherwise.

## 7. Validator controls and mutation evidence

**Deliberate check-21 revision.** Check 21's own comment required that "a legitimate future state change must revise these pins deliberately, in its own independently reviewed correction" — this is that correction. Kept intact: the literal `B — DATA TRANSFORMATION REQUIRED` pin, the five-query inventory pins, the Management API and metadata-read disclosures, the overbroad-phrase rejections, the PR #61 closure pins, the durable-wording pins on the historical PROJECT_STATE entry, and W-B2d-01's shape pins (lote/rama/mode/class/single mapping). Replaced: the ordered B2d→B2c dependency (now its prohibition: B2d `DONE` fails outright), W-B2d-01 BLOCKED→`SUPERSEDED` (UNAUTHORIZED retained), W-B2c-01 `BLOCKED`/`BLOCKED` → `BLOCKED`/class `2`.

**New check 22** pins: the frozen claims snapshot SHA-256 (`d598f29b…`); the correction record's existence, its five owner-decision literals, and ten named inventory surfaces (assignment/progress tables, the two extra functions, `course_enrollments`, routes, service, maintenance routes, `courses_learning_path_member_view`); evidence §8 and the effective-classification-A conclusion; W-B2d-01's truthful state (SUPERSEDED, UNAUTHORIZED, notes carrying SUPERSEDIDO / never-executed / not-represented-as-completed / not-a-prerequisite); W-B2c-01's three prerequisites, the anti-reintroduction regex for the retired ordered dependency, the rejected-ownership-language ban (`aislamiento por colegio`, `aislamiento de tenant`, `rol × tenant`) on its row, the `plantillas globales` and `course_enrollments` gate pins; the W-PC-06 supersession note; and the decision needles in protocol, report, and PROJECT_STATE (including the `- **LP-GOV-01` entry).

**Mutation evidence (disposable copies only — the real worktree was never mutated).** Harness: `mutation-harness.mjs` (scratchpad; reproduced in §10 note below), copying the 13 governed files to a scratch tree per case, applying exactly one mutation, running the copied validator, asserting the targeted failure. Results — **25/25 as expected**:
- `CONTROL` — pristine copy fails on exactly the 67 pre-existing `[16 propiedad]` ownership findings and **no other category** (matching the base run recorded before any edit, and the post-edit run on the real tree: identical 67-line failure list, `diff` clean).
- M01 claims-CSV byte change → 22a SHA failure · M02 record deleted → 22b · M03 "No school owns a learning path" removed → 22b · M04 inventory loses `increment_path_assignment_time` → 22b · M05 evidence §8 heading removed → 22c · M06 effective-A conclusion removed → 22c · M07 B2d back to BLOCKED → 21+22d · M08 B2d DONE → 21 ("DONE está prohibido") + 22d · M09 B2d AUTHORIZED → 21+22d · M10 never-executed note removed → 22d · M11 B2c class back to BLOCKED → 21 · M12 B2c leaves BLOCKED → 21 · M13 prerequisite 3 removed → 22e · M14 ordered dependency reinserted → 22e · M15 `aislamiento por colegio` reinserted → 22e · M16 `plantillas globales` removed from gate → 22e · M17 W-PC-06 supersession removed → 22f · M18 protocol loses the decision → 22g · M19 LP-GOV-01 entry renamed → 22g · M20 gate list drops `learning_path_assignments` → 18 · M21 foreign B2b table smuggled into the list → 18 (both the set-equality and belt-and-braces token scan) · M22 protocol misstates the SUPERSEDED count → 14 · M23 invented status `RETIRED` → 07 · M24 B2c notes stop naming W-B2d-01 → 21.

## 8. Test and gate evidence

Executed at the head of the single commit's tree (validator/mutations above; standard gates below):

<!-- GATE-EVIDENCE:BEGIN -->
- **Santa Marta validator** (`node scripts/check-ledger.mjs`): exit 1 with **exactly the 67 pre-existing `[16 propiedad]` ownership failures and no other category** — the failure list is byte-identical (`diff` clean) to the control run captured at the untouched base before any edit; checks 01–15 and 17–22 all pass; figures recomputed from the files: 160 claims / 36 P0 / 107 work items / 152 links / 59 P0 links / 36 linked P0 / 28 batches; modes 90/10/6/1; statuses 29/57/17/3/1.
- **Mutation harness:** 25/25 as expected (control + M01–M24, §7), disposable copies only.
- **Environment note (material to how the gates ran):** the harness worktree's `node_modules` was found **iCloud-evicted** (dataless placeholders; `tsc` blocked in Node startup — ~53 min with 7 s of CPU). Remedy: the 2,483 source/docs files (verified 0 dataless) were rsync-copied to the non-iCloud runner `/Users/brentcurtis/dev/lp-global-gates` (`diff -rq` verified byte-identical) with a fresh `npm ci` (1,375 packages); the real worktree then ran with that materialized dependency tree symlinked in as `node_modules` (gitignored; no commit impact). The evicted tree was parked outside the worktree.
- **`npm run type-check`**: **exit 0 in the real worktree** (and exit 0 in the copy).
- **`npm run lint`** — the repository's exact supported zero-warning form (`eslint --ext .js,.jsx,.ts,.tsx --max-warnings=0 .`): **exit 0, zero warnings, in the non-nested copy**. In the nested `.claude/worktrees/` checkout ESLint is unrunnable by a **pre-existing, documented limitation** (no `root: true`, so the config cascades into the parent checkout and resolves plugins from its `node_modules` — here additionally evicted, turning the known dual-resolution failure into a stall); the project's recorded procedure for exactly this case is to run the *unmodified* lint from a non-nested checkout, which the diff-verified copy is.
- **`npm test`** (full Vitest): **exit 0 in the real worktree — 369/369 files, 8,423 passed / 11 skipped (8,434 tests)**, including `__tests__/security/no-phantom-audit-table.test.ts` 9/9 (this suite shells out to `git grep` and therefore required the real worktree; an interim run in the git-less copy failed only that suite, only with `fatal: not a git repository` — environmental, and resolved by running where `.git` exists, not by modifying anything).
- **`npm run build`**: **exit 0 in the copy**, with command-scoped synthetic `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`/`NEXT_PUBLIC_BASE_URL` values (this worktree has no `.env.local`; synthetic command-scoped env is the established practice for fresh worktrees, and no production env file was touched or read).
- **`git diff --check`**: clean.
- **Frozen-artifact hash verification**: `docs/reviews/santa-marta-claims.csv` SHA-256 `d598f29b39d8d5ac9c1289a7c030221c93a3c8897c91f19e395f99486c68cce7` and `docs/reviews/archive/santa-marta-promise-ledger-legacy-161.csv` SHA-256 `009f14abccec97d7ada4b559c9aaeb24ac5b7aab54563a5c1151e511dc2c7fe9` verified at base and re-verified after all edits; `santa-marta-work-claim-map.csv` untouched per `git status`. *(Task-prompt reconciliation: the executor prompt attached the `009f14ab…` pin to `santa-marta-claims.csv`; in the repository that pin belongs to the archived legacy CSV — as the normalization report and the validator's `LEGACY_SHA256` record — and it verifies exactly there, while the claims snapshot is preserved byte-for-byte at its own hash, now also pinned by check 22.)*
<!-- GATE-EVIDENCE:END -->

**`test:db` and `e2e` deliberately not run — rationale:** this phase contains **no** DB, RLS, API, or UI implementation: no migration, policy, grant, function, route, component, or test file changed (the diff is seven governance/prose files, one governance script not wired into CI, and this review request). pgTAP would exercise an unchanged schema and Playwright an unchanged UI; both remain mandatory for the future, separately authorized W-B2c implementation, as its acceptance criteria (record §7) already require. The proportionality rule in AGENTS.md (`+ test:db/e2e when DB/UI touched`) therefore excludes them here.

## 9. Hardest independent-review areas (in order)

1. **The check-21 revision** (`scripts/check-ledger.mjs`) — verify that only the forward-state pins changed and every historical pin survived; a subtle weakening here would undermine the PC-06 closure anchors. Diff hunk-by-hunk against the base version.
2. **The B2c scope expansion inside check 18** (2 tables + 6 functions → 4 + 8) — I judged that Brent's 2026-08-29 decisions plus the explicit instruction to correct the inventory authorize amending the 2026-08-27 approved sets; confirm that judgment and that the two added functions/two added tables are genuinely part of the same security surface (baseline anchors in record §6).
3. **The `SUPERSEDED` vocabulary extension** — whether it is truly the narrowest truthful representation, and whether every count/prose site was updated (protocol table+header, report §14, validator enum, checks 07/14/16/21/22 interactions; note check 16 required no change because W-B2d-01 keeps a real `dueno`).
4. **Access-model fidelity (record §5)** — verify it adds nothing beyond the ten decisions plus the task's required actor rows; the deliberately open reporting-reads question (§6.1 above) is flagged, not silently decided; `service_role` bounds are descriptions of current code, not new authorizations.
5. **Truthfulness of the rewritten CSV rows** — the W-B2d-01 gate/notes now carry supersession framing around the preserved historical proposal text; check that no historical fact was dropped or distorted (compare against the base row and report §13.7).

## 10. Limitations and deferred work

- The W-B2c **implementation** is entirely deferred and unauthorized: three prerequisites stand (this correction independently approved and merged → Privacy approves the actor-by-operation access matrix → Brent's separate explicit authorization). The twelve acceptance criteria in record §7 are requirements for that future work, not work performed.
- The actor-by-operation access matrix itself is **not** drafted for Privacy here beyond the model in record §5; producing the formal matrix is part of prerequisite 2.
- The five unresolved questions in §6 are recorded, not decided.
- Validator string pins are needle-based (fast, deterministic, mutation-proven) rather than semantic parsing; a determined editor could rephrase around them — the pins are a regression tripwire, not a substitute for review.
- The mutation harness lives in the session scratchpad (its full case list and results are reproduced in §7); it is deliberately not committed, since committing a tool that copies governed files could invite running mutations inside the repository. Reviewers can reconstruct it from §7's case descriptions.
- `lint`/`build` environment caveats, if any, are recorded verbatim in §8.

## 11. Confirmation of boundaries

No Supabase call of any kind; no database query or mutation (the validator and mutation harness read local files only); no production access; no application/API/migration/RLS/test implementation; no push, PR, merge, deployment, or GitHub/Vercel setting mutation; no history rewritten or amended; exactly one local commit; frozen artifacts preserved byte-for-byte and hash-verified (`d598f29b…` claims snapshot, `009f14ab…` legacy archive).

— Prepared 2026-08-29 on branch `docs/lp-global` for independent review (reviewer side: `docs/planning/review-protocol.md`). Merge decision remains Brent's.
