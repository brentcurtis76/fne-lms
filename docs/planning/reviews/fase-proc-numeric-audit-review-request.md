# Procesos de Cambio — numeric audit evidence closure review request

Status: **AUDIT EVIDENCE CLOSED (documentation only) — no arithmetic defect found in the audited scope; five findings routed, none repaired. UI_NOT_RUN for the numeric surface (PROC-01's browser evidence covers a different surface). Broader E2E unresolved. PHASE_NOT_CLOSED. Numeric feature acceptance remains open. Publication not authorized.** This request is a records consolidation, not self-approval, not release authority, and not acceptance of the frequency feature.

## What this closes, and what it does not

Four distinct things are routinely conflated; only the first is closed here.

| Level | State after this unit | Why |
| --- | --- | --- |
| **Documentation audit closure** — the independent arithmetic audit is consolidated into the repository with its attribution and its open findings routed | **closed by this request** | the audit ran, was independently reviewed over four rounds, and its evidence is preserved and hash-bound |
| **Implementation acceptance** — the numeric implementation commit `650273ba` is correct and accepted | **not claimed** | the audit found no defect *in the tested scope*; that is a negative result over a bounded surface, not an acceptance |
| **Feature acceptance** — the frequency numeric feature is accepted end to end | **open** | F1–F5 are unresolved and browser acceptance (F4) has never been executed |
| **Phase closure / publication** — Procesos de Cambio numeric phase is done, releasable | **open, unauthorized** | the literal full E2E suite has not been run at this lock; the historical run was RED |

The workstream ledger reflects this split: `PROC-NUMERIC-AUDIT-CLOSURE` (this unit) is separate from `PROC-NUMERIC-VALIDATION`, which stays **held**. Do not mark the parent item done on the strength of this document.

## Identity, lock and commit count

Repository worktree: `/home/brent/migration-staging/all-projects-20260915-attempt1/worktrees/genera-procesos-cambio`. Branch: `codex/proc-e2e-auth`. Audit lock HEAD: `e800696249e4508d16f3853a7c42aed745e2d62a`.

The lock was verified before and after authoring with the same four commands the audit used: `git rev-parse --abbrev-ref HEAD` → `codex/proc-e2e-auth`; `git rev-parse HEAD` → `e800696…`; `git status --porcelain | sha256sum` and `| wc -l` → `1a031e5a44db85ea1c0298062425009f824663716f544e1b856942c98b64d8be` / 22 entries at entry, and `9ed5c62c33c014bde38d0b4321d5cc9b0901436baf5e432cc9c13579b47768a8` / 23 entries at exit and at the correction round's entry — this file, untracked, is the only delta; `git diff | sha256sum` and `| wc -l` → `ef2615205f45d54e1311adfa63956f8a2de351d03400abab83f1e0d1ec16b274` / 786 lines, identical at entry and exit, which is itself the proof that no tracked file was modified. The diff lock differs from the one in the audit itself only because of intervening PM-owned ledger edits; the 28 frozen source hashes below are what fixes the audited state, and they are unchanged.

**Commit count, stated precisely.** This unit contributes **zero commits at authoring time**. The PM commits exactly one records-only commit after independent review, and the review range for *this unit* is therefore `e800696…..<that single commit>`. That commit is **not** “this file plus one ledger row”. It carries this file together with the cumulative PM-owned ledger state that is already pending in the working tree — a rewritten `## Now` section, twelve modified item rows, one new item row and fourteen appended event lines, most of which is authorized planning, replan, status and `Now` work plus preserved triage of *unrelated* workstream items. The breakdown is in *Changed paths* below. None of it was written or edited by this unit.

**Historical orientation, separately.** The numeric implementation commit `650273ba9ee68c4312127993ab9501ebadf6e5d2` (its own base `b17a68393dc7cd3a6ccfed9fffa3252c7e97fb46`) **is** an ancestor of the lock — `git merge-base --is-ancestor` exit 0 — and 23 commits separate that numeric base from the lock, **`650273ba` itself being the first of those 23**. Stated exactly: `git rev-list --count b17a6839..650273ba` → **1**, which is the numeric implementation commit; `git rev-list --count 650273ba..HEAD` → **22**; `git rev-list --count b17a6839..HEAD` → **23**; 1 + 22 = 23. The “no later numeric change” claim therefore applies to **the 22 commits after `650273ba` only**, and it is exactly what the evidence supports: `git log 650273ba..HEAD -- <7 paths>` is empty and `git status --porcelain -- <7 paths>` is empty. Those 22 belong to the unrelated `codex/proc-e2e-auth` line of work; the remaining one is `650273ba`, the numeric work itself. Calling all 23 unrelated — as an earlier draft of this record did — is wrong, and would silently exclude the implementation commit from the very range it is supposed to be judged in.

## Objective and scope

Objective: consolidate the already-reviewed arithmetic audit into a durable repository record and route its unresolved findings to bounded destinations.

In scope: this review request; and, riding in the same records-only commit, the PM-owned cumulative ledger state (planning, replan, status and `Now` updates, one new item row for this unit, and preserved triage of unrelated items) — not one row, and not written by this unit. Out of scope, explicitly: any source, test, fixture, schema, migration, configuration or `PROJECT_STATE.md` change; any further numeric research; any database contact, read or write; any server, browser or fixture execution; dependency, account, Git-mutation, deployment, provider or publication action; and any design of the browser acceptance protocol. No repair authority of any kind is carried by this unit, and none is implied by recording a finding.

## Evidence and attribution

No test, probe, suite or browser session was executed by this unit. Everything below is prior evidence, attributed to whoever produced it. Counts are quoted from the durable result files, not from prose.

| Evidence | Produced by | Count | Durable source |
| --- | --- | --- | --- |
| Independent oracle comparisons against the arithmetic module | **executor** (Claude Code `claude-opus-5` / `high`, REPORTED) | **4264 comparisons, 4264 agreements, 0 disagreements** | `runs/PROC-02/evidence/numeric-probes.cjs` → `evidence/result.json`; summarised in `runs/PROC-02/numeric-audit.md` §A2 |
| The four focused suites re-run | **PM** (independent re-run) | **513 passed / 0 failed / 0 skipped, exit 0** | `runs/PROC-02/pm-focused.log`, `pm-focused-result.json` (`total: 513`) |
| Boundary assertions | **PM** | **23 checks** | `runs/PROC-02/pm-counterexamples-r0.json` (`checks: 23`) |
| Correction probes | **PM** | **38 total / 38 passed / 0 failed** | `runs/PROC-02/pm-correction-checks-r1.cjs` → `pm-correction-results-r1.json` |

The executor's own §A3 re-run of the same four suites reported the identical 513 with `delta_vs_historical_513 = 0`; that is a reproduction of the historical claim, not a second independent body of evidence, and it is not added to the PM's 513.

Durable parent records (machine-local, outside the repository — see limitations): `runs/PROC-02/numeric-audit.md` (the audit, r1), `runs/PROC-02/pm-review-r3.md` (the final independent review), `runs/PROC-02/completion-plan.md` (the approved split), `runs/PROC-02/review-source-hashes.json` and `runs/PROC-01/protected-hashes.json` (the 28 frozen hashes), `runs/PROC-02/executor-report-r0..r3.md` and `pm-review-r0..r3.md` (the four retained rounds). All `runs/…` references above and below resolve against the evidence root `/home/brent/Projects/pm-workflow` (so `runs/PROC-02/numeric-audit.md` is `/home/brent/Projects/pm-workflow/runs/PROC-02/numeric-audit.md`; the root does not itself end in `runs/`).

**Three provenances, never merged.** (1) *Historical numeric* — the numeric review request's own full `npm test`, type-check, lint, build, `supabase test db` and the two literal E2E runs, including **256 passed / 73 failed / 27 skipped, RED on both candidate and exact base with zero candidate-only failures**; that describes the tree at `650273ba` (2026-09-09) and is **not a current baseline**. (2) *Inherited PROC-01 gates* — the unit/type-check/lint/build evidence carried in from PROC-01, describing a later state of `codex/proc-e2e-auth`, cited only as inherited. (3) *Produced at this lock* — the executor's 4264 comparisons and the PM's 513/23/38. The historical full-E2E result and the inherited PROC-01 gates are different things and are not interchangeable.

**No new execution is claimed here. No browser pass is claimed here.** `runs/PROC-02/ui/` is empty. UI_NOT_RUN here is scoped to the **numeric audit and numeric acceptance**: no browser session has ever exercised the numeric contract, and none is claimed. It is not a statement about the workstream as a whole — PROC-01 ran and recorded real browser evidence for the archived-evaluations surface (`runs/PROC-01/`, ledger item `PROC-LIFECYCLE-ARCHIVED-LIST`, done locally). Neither fact waives the other.

## Changed paths, by risk

| Risk | Path | Change |
| --- | --- | --- |
| Low — records only, no runtime reach | `docs/planning/reviews/fase-proc-numeric-audit-review-request.md` | new (this file) |
| Low — records only, PM-owned write | `docs/ledger/procesos-de-cambio.md` | **cumulative pending PM state, not one row** (`git diff --stat`: 29 insertions, 13 deletions): `## Now` rewritten to the approved-split narrative; **12 item rows modified**, of which only `PROC-NUMERIC-VALIDATION` (`todo` → `held`, unit PROC-02) is numeric — the other 11 are preserved triage of unrelated items (B5 versioning / concurrency / pgTAP renumber, consultor C0 / C1 / dashboard integration, archived-list note, lifecycle cancellation, full-suite publication, historical data, pilot); **1 new item row** `PROC-NUMERIC-AUDIT-CLOSURE` `in-progress` (unit PROC-03), whose status the PM moves on approval; **14 appended event lines**. Written by the PM before this unit and not edited by it |

No other repository path is written. The 28 protected paths (7 numeric audit sources + 21 inherited PROC-01 paths) were re-hashed at entry and exit of this unit: **28 checked, 0 mismatch**.

## Findings F1–F5 and their destinations

None of the five is in the frequency arithmetic itself. None is repaired here; each destination is a *proposal*, and each needs its own approval, authority and — where marked — authority over a frozen path.

| ID | Severity | Finding | Bounded destination |
| --- | --- | --- | --- |
| **F1** | medium | `year_N_expected` is never cross-checked against the same indicator's `min`/`max`/`step`, and its three writers disagree: `scripts/pilot-provisioning/manifest.mjs` `validateExpectation` (five non-negative integers, no upper bound), `expectations/[indicatorId].ts:144-151` (hard `0..4` cap), `expectations/index.ts:794` (`>= 0`, no upper bound). The shipped fixture's `eva-1b/ind-fre-1` declares `expected: [1,2,3,4,5]` with `max=10` — valid for two writers, rejected by the third | **owner decision first** on what the column means (the `0..4` cap reads like a *profundidad* scale applied to a frequency column), *then* a bounded consistency repair. Straddles the freeze: `manifest.mjs` is one of the seven frozen paths |
| **F2** | medium | The stricter rule can refuse a `frequency_config` that the previous permissive rule would have published; `responses.ts:141-150` then refuses the **whole** PUT with `422 invalid_snapshot_frequency_config`, losing the save for every other indicator in the request. The fail-closed behaviour is deliberate and tested; the operational half is missing — no detection, report or backfill path exists | **Whether any such snapshot exists is unknown.** No database was inspected and no data audit was authorised. Destination: a separately authorised **read-only** audit to establish whether the affected population is empty, before anything else. No production access is granted by this document |
| **F3** | low, pre-existing | Two raw `U+0000` bytes in `scripts/pilot-provisioning/manifest.mjs` (lines 306, 384, in `manifest.syntheticMarker ?? '<NUL>'`) make it a binary file to `grep -I`-class search, so caller sweeps silently skip a file that is itself part of the numeric contract. Present in `650273ba^`, `650273ba` and at HEAD — not introduced by the numeric commit | **Semantics must be settled, not substituted.** The NUL is a *fail-closed* sentinel: `startsWith(NUL)` is false for every real name, `startsWith('')` is true for every string, so an empty-string fallback would flip both marker checks from always-fail to always-pass. Destination: a future scoped change that preserves the intended semantics and lands with tests. Frozen path |
| **F4** | informational | `FrecuenciaInput.tsx:84-86` hands the snapshot constraints to a native `<input type="number">`, whose `min + n*step` stepping is a **third** arithmetic unrelated to the server's dual-model rule. Server refusals do reach `[data-testid="assessment-save-status"]`, but `handleManualSave` simultaneously raises the generic `'No se pudieron guardar tus respuestas. Revisa tu conexión…'` toast, misattributing a rejected value to the network; the specific message is gated off for completed instances, non-`ready` drafts and pending recovery drafts. `tests/e2e/assessment-draft.spec.ts` covers only happy-path values against an echoing mock | Numeric **browser acceptance**, which follows a reviewed offline fixture model and an explicitly approved local fixture grant. See the caution below — the existing plan is **not** approved |
| **F5** | low | `public.assessment_responses.frequency_value` is `numeric(10,2)` (`supabase/migrations/00000000000000_baseline.sql:6198`, unaltered since). The documented JS alias `0.30000000000000004` is accepted by the API and stored as `0.30`, so it cannot round-trip; any configured `step` finer than `0.01` admits grid points the column cannot represent, and nothing refuses such a config at publish time | A separate **source-only** persistence diagnosis that covers **scale, anchors and ranges**, not step alone. Any schema or data repair needs its own approval and its own DB flow. Not claimed to affect stored data — no database was inspected |

**Caution on the browser work.** `runs/PROC-02/browser-acceptance-plan.md` is **UNAPPROVED — REPLAN_REQUIRED**. Its residual blocking finding is **R02-SCHEMA** (P1, confidence 1.0): the recovery/verification protocol still references absent columns and the wrong state — `assessment_instance_assignees` has neither `created_at` nor `updated_at` (`assigned_at` is its timestamp; `baseline.sql:6036-6046`), `assessment_template_source_revisions` has only `template_id`/`revision`, yet line 210 still requires `WHERE id` on all ten tables, and line 144 expects J6's instance already `in_progress` while §5 resets it to `pending` first. Link it for context; **do not read it as accepted instructions.** The approved boundary is `runs/PROC-02/completion-plan.md`, whose step 2 (PROC-04) is an **offline** ownership/recovery/state model that must resolve R02-SCHEMA *before* any local database write permission is even requested.

## Scrutinise these hardest

1. **Does the audit's negative result cover what it appears to cover?** 4264 agreements say the implementation matches an oracle over a constructed input space. Reviewers should push on whether that space reaches the configurations real schools produce, not only ULP-scale and randomised ones. A clean oracle can be clean about the wrong region.
2. **The attribution split between PM and executor (§ Evidence).** The 513 appears three times in the source records — historical claim, executor reproduction, PM re-run — and it would be easy to present it as three confirmations. This document counts it once, as the PM's. Check that I have not inflated it, and that the 4264 stays the executor's.
3. **The deliberate asymmetry between F2/F5 and the rest.** Both are recorded with an *unknown* population because no database was inspected. That is honest but unsatisfying: a reviewer may reasonably judge that shipping an audit closure while "are any published evaluations now unanswerable?" is unanswered understates operational risk. I chose to keep the unknown visible rather than soften it; disagree if you think the closure should have been blocked on it.
4. **The four-level distinction at the top.** It is the load-bearing claim of this document, and it is prose, not a gate. Nothing mechanically prevents someone quoting "audit closed" as "numeric done". Check the wording is tight enough to survive being quoted out of context.
5. **The F1/F3 freeze straddle.** Both findings' repair surface includes `manifest.mjs`, a frozen path. Recording them here without authority is correct, but it means two findings have destinations that cannot be entered without first re-opening a freeze — check that no downstream reader will mistake "routed" for "actionable now".

## Known limitations and what would close them

- **Machine-local evidence.** Every durable record cited lives under `/home/brent/Projects/pm-workflow/runs/`, outside this repository, on one machine. The repository keeps the conclusions; it does not keep the probes, logs or result JSON. *Closed by* a future decision to vendor a hash-bound evidence bundle into `docs/`, which is not authorised here.
- **No current full-suite baseline.** The literal full E2E has not been run at lock `e800696…`. The RED 256/73/27 result describes a 2026-09-09 tree, and the inherited PROC-01 gates are a different provenance again. Neither RED nor green is a current fact. *Closed by* a full-suite run at this lock under its own authority.
- **UI_NOT_RUN (numeric surface).** No browser verification exists for any part of the numeric contract, and F4 is precisely the unverified surface; PROC-01's archived-evaluations browser evidence is a different surface and closes nothing here. *Closed by* PROC-04's offline model, then an explicitly approved fixture grant, then browser acceptance — in that order.
- **F2 and F5 populations unknown.** *Closed by* the single read-only audit proposed for both.
- **Attempt history.** PROC-02 exhausted initial 1 + remediation 3; all four rounds are retained. This unit is new audit-consolidation work and resets no cap, revives no historical implementation attempt, and inherits no fixture, database or browser grant.
