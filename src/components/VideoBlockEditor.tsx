import React from 'react';

const VideoBlockEditor = () => {
  return (
    <div>
      <h2 className="text-xl font-semibold">Video Block Editor Component</h2>
      <p>Content for the video block editor will go here.</p>
      {/* Future: Add form fields for video URL, title, description, etc. */}
    </div>
  );
};

export default VideoBlockEditor;
